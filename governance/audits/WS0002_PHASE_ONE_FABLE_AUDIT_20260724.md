# WS-0002 Phase One Independent Audit — Fable

**Audit artifact for permanent retention and later governance reconciliation.**

_**Version 1.1** — amended same-day by the same reviewer/session after a second-round verification pass re-authorized by the principal. Round 2 independently re-executed the full test suite (closing v1.0's FA-4), re-derived the charter-pinned hashes, ran a programmatic governance-index consistency check, and read the S2/G2 scope determination directly. See §4a. The disposition is unchanged; FA-4 is resolved. No v1.0 finding was weakened or removed._

| Field | Value |
|---|---|
| Reviewer | Fable (independent high-capability reviewer, per OPS-0002 item 4 vocabulary) |
| Reviewing session | `session_01H9bxWHpdadjhooQ4EjYZVk` (https://claude.ai/code/session_01H9bxWHpdadjhooQ4EjYZVk) |
| Audit date | 2026-07-24 |
| Audited state | `origin/main` @ `085f5560bc87d98a0a9f3e0bffb99958e401163d` (verified unmoved before and after audit) |
| Authorization | Explicit principal authorization, this session, read-only; quoted in the session transcript |
| Mode | Read-only. No file in the repository was modified; no branch, PR, governance record, or workstream field was created or changed; PR #145 untouched. |

**Independence posture, stated precisely.** This audit was conducted in a session distinct from every session that authored the material under audit. The authoring sessions, from committed `Claude-Session` trailers and PR footers, are: `session_01JHKaPdKXdPixaPKyL9Z18g` (PR #143 commits `7479a89`, `5fcb989`), `session_019zExKDoAJvLzxAC4rt4YZu` (reconciliation commit `263b64b`, PR #142 supersession comment), and `session_01JpmakukiTwxn94VRfK9297` (PR #144 / OPS-0003). None is this session. This audit is therefore independent of the work it evaluates in the sense OPS-0002 item 4 requires — a different reviewing context with no authorship stake in the artifacts under review. What it cannot supply is retroactive authentication of the *earlier* claimed audit (Finding FA-1 below); it evaluates that audit's claimed outputs on their merits instead.

---

## 1. Scope audited

Per the principal's authorization: the exact Phase One planning baseline merged on `origin/main` — `docs/PORTFOLIO_HQ_UNIFIED_CORE_BLUEPRINT_V1.md`, `governance/decisions/OPS-0002-unified-core-planning-and-audit-gate.md`, `governance/decisions/OPS-0003-post-acceptance-workstream-priority-transition.md`, the claimed F1–F4 corrective dispositions, and the consistency of the Phase One closure record (`operations/WORKSTREAMS.yaml` WS-0002 entry, `governance/decisions.yaml`, the CLAUDE.md Decisions Log pointer, PR #143 and PR #144 as merged).

## 2. What was independently verified (evidence, not assertion)

Every item below was checked directly against the working tree at `085f556…`, git history, or live GitHub API state during this audit — none is carried forward from any prior session's claim.

1. **F2 disposition (Intelligence-evidence freshness) — verified genuine.** Commit `263b64b`'s diff adds to blueprint §6 the freshness/staleness disclosure requirement via the PI-0011 framework, with mandatory abstention on stale/unknown/insufficient evidence. Diff inspected line-by-line; matches the claimed remediation exactly.
2. **F3 disposition (unresolved-conflict surfacing) — verified genuine.** Same commit adds to §6 the requirement to surface known unresolved factual/data/authority/policy conflicts, identify the conflict's authoritative owner, and abstain from or isolate affected conclusions, citing the §9 BTC conflict as the worked example. Verified in the diff.
3. **F4 disposition (decision-quality measurement basis) — verified genuine.** Same commit replaces §4's unqualified "demonstrated improvement in the principal's actual decision quality over time" with the bounded, non-causal, deliberately-unresolved measurement basis. Verified in the diff; the replacement is substantively correct — the original criterion was indeed unmeasurable as stated, and the correction refuses causal performance attribution the data cannot support.
4. **F1 disposition (duplicate competing PR #142) — verified genuine.** Live API: PR #142 is `closed`, `merged: false`, and carries exactly one comment — the supersession notice ("Superseded by PR #143… PR #143 is the sole current OPS-0002 / WS-0002 planning-baseline PR"), timestamped 2026-07-24T19:31:35Z, three minutes before `263b64b` was committed. The sequence is internally coherent.
5. **F1's residual branch-cleanup item — now resolved, record stale (Finding FA-2).** PR #143's merged body says branch `docs/ops-0002-unified-core-blueprint` still exists and needs manual deletion. Live check: `git ls-remote origin refs/heads/docs/ops-0002-unified-core-blueprint` returns empty — the branch is gone. PR #144's preflight text already recorded both prior OPS-0002 branches absent, so the later record self-corrected; only the frozen PR #143 body is stale.
6. **The §9 BTC conflict is real and correctly deferred.** `holdings.yaml` `crypto_shares:` carries `BTC: 0.00460473`; `targets.yaml`'s crypto comment still reads "BTC has no entry in holdings.yaml's crypto_shares — fully sold 2026-07-13." The blueprint records the conflict without resolving it, matching its authorization boundary. It remains open and must not be silently resolved by any future edit.
7. **PR #143's merged diff = exactly its four authorized paths** (blueprint, OPS-0002, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`), confirmed from commit stats across `7479a89`/`a0365bf`/`5fcb989`/`263b64b` and the merge. Protected paths untouched.
8. **PR #144's merged diff = exactly its six authorized paths**, verified by `git diff 3c31c2e..41e5ba2 --stat`. The blueprint change is precisely the authorized six-line dated acceptance note above the retained draft-status paragraph — no §§1–12 text changed (OPS-0003 item 6 honored literally). The CLAUDE.md change is exactly one Decisions Log entry (the OPS-0003 pointer).
9. **OPS-0003's supersession is narrow, explicit, and within authority.** It names OPS-0002 items 2–3 only, preserves items 1/4/5/6/7, the three-layer architecture, the implementation prohibitions, and the audit gates — satisfying OPS-0002 item 7's own supersession requirement and GOV-0002's explicit-supersession rule. The condition it records as satisfied ("planning package and its first independent audit complete") is a factual-recording act, not a new grant; the recording itself is accurate *contingent on FA-1's provenance question* (below).
10. **Closure-record consistency.** `operations/WORKSTREAMS.yaml`: WS-0002 `status: complete` explicitly scoped to planning-and-audit only, `next_action: none authorized`, implementation gated on a new governance decision; exactly one `priority: primary` (WS-0001); all dependencies resolve; `active_branch`/`active_pr` correctly null. `governance/decisions.yaml`: 28 index rows ⇄ 28 decision files, zero mismatch, OPS-0002/OPS-0003 rows consistent with file frontmatter. CLAUDE.md's OPS-0003 entry matches the decision file's actual content.
11. **No WS-0002 implementation exists.** `allocate.py` and `margin_state.py` contain zero references to `intelligence/`; `intelligence/` holds only the 7 company + 2 theme records separately authorized by the PI series, no aggregation engine, no `index.yaml`. `trial_ledger.jsonl` and `candidate_freeze.yaml` confirmed absent (zero MARGIN-0005 trials consumed — relevant only as confirmation that nothing leaked across workstreams).
12. **Validators re-executed in this session:** `intelligence_validator.py` exit 0; `freshness_validator.py` "OK", exit 0.

## 3. Architecture evaluation (blueprint §§1–12 on the merits)

**Disposition: sound; no conflict with higher authority found.**

- **Constitution conformance.** Layer 1's recommendation-only posture restates Constitution §1 (no order path). The Layer 2 → Layer 3 → bounded `targets.yaml` PR flow keeps every gate of Constitution §4 (no standing predictive layer, no scores, no precomputed recommendations) and §5 (fixed mechanical limits untouched). The corrected recommending role does not create a runtime coupling: `allocate.py` never reads Intelligence, verified in code, and the blueprint prohibits it in terms.
- **Constitutionally incorporated material.** `docs/PORTFOLIO_INTELLIGENCE_SPEC.md` §20 (level-1 authority per GOV-0002) was checked directly against the corrected architecture. §20's own text contemplates exactly the blueprint's change path: "any change to a tier or weight remains a manual `targets.yaml` edit," with `targets.yaml` sole authority. The blueprint's Layer 3 (principal review → governance decision → bounded PR → `targets.yaml`) *is* that manual governed edit, formalized. No §20 prohibition — advisory-only, no allocator coupling, no computed conviction, filesystem-as-index, no automatic tier change — is weakened. §24's freeze is not touched.
- **The display-only → recommending correction was right on the merits.** The original draft's "permanently display-only" framing contradicted the repository's own established practice — TGT-0001/TGT-0002 already show tier/target changes flowing from analysis through principal review and governance into `targets.yaml`. The correction names an existing pattern rather than inventing a mechanism, and the dated in-place correction to the then-unmerged, never-audited OPS-0002 draft was properly disclosed and falls within `governance/decisions/README.md`'s narrow-correction convention (acceptance is effective on merge; no settled audit finding existed to contradict).
- **§6 terminal milestone** correctly separates executable actions under accepted policy from advisory policy-review output, and (post-F2/F3) requires freshness disclosure and conflict surfacing with abstention. This is the strongest section of the document — it converts the audit findings into permanent run-level obligations rather than one-time patches.
- **§7/§8 proportionality** (three material audit gates only, routine changes exempt) is consistent with how the repository already treats review burden and is the correct reading of its own anti-ceremony evidence (Constitution §2).

## 4a. Round 2 verification (v1.1 additions — all independently executed in this session)

1. **Full test suite re-executed: 1,246 passed, 0 failed** at the audited SHA, in this audit session, after provisioning the environment. This independently confirms the central quantitative claim of the closure record (PR #143 and PR #144 both recorded 1,246/0) and resolves FA-4.
2. **Charter-pinned hashes re-derived, not just re-read.** `sha256sum` computed fresh in this session: `research/margin_target_study/PROTOCOL_V2.md` → `d794a4c09fa81dbaa147bb830da91b75221a35175f47c8bd979b75e3fc154e21`; `research/margin_target_study/pre_registration.yaml` → `e3b101e336f2ff30c013908a4c2b30918e9adf59cbb26499d2774927e472120e`. Both match the pins in `MARGIN-0005-margin-target-research-charter.md` §3 exactly — the frozen research protocol and pre-registration are byte-identical to what the charter accepted. (Relevant to Phase One only as confirmation that the WS-0002 filings touched nothing they were prohibited from touching.)
3. **Programmatic governance-index consistency check.** All 28 decision files' YAML frontmatter (`decision_id`, `date`, `status`, `category`, file path) cross-validated against all 28 `governance/decisions.yaml` rows by script: zero mismatches. v1.0 had only verified counts; v1.1 verifies field-level agreement.
4. **`research/margin_target_study/S2_G2_SCOPE_DETERMINATION.md` read directly** (v1.0 relied on OPS-0003's characterization of it). Confirmed: it self-declares and structurally is read-only analysis — authorizes nothing, runs no simulation, consumes zero trials, creates no trial artifacts. Its factual inventory matches what this audit independently observed (files present/absent, test counts). It also candidly discloses a real deviation: `repayment_lib.py`/`test_repayment_lib.py` live at repository root, not under `research/margin_target_study/` as the charter §4 table and protocol §13 diagram show — a WS-0001-side documentation/placement inconsistency already on record, disclosed rather than papered over, and not a Phase One defect. It should be reconciled (doc correction or governed relocation) before or during any future S3 authorization.
5. **State freshness re-confirmed at Round 2 start:** `origin/main` still `085f5560…`; PR #145 still draft/unmerged; working tree clean throughout.

**FA-1 is unaffected by Round 2.** Nothing newly examined supplies the missing provenance for the prior claimed audit of PR #143; the finding and its recommended reconciliation stand exactly as written.

## 4. Findings

**FA-1 — Major (provenance, not substance): the prior claimed independent audit of PR #143 left no independently attributable artifact.**
PR #143 has zero GitHub reviews and zero comments (API-verified). The audit narrative — disposition "REMEDIATION REQUIRED, no Blocking finding," findings F1–F4, a "delta re-review" — exists only in PR-body prose and commit messages authored by the same committer identity as the audited work. A repository-wide search for "Fable" finds only these self-referential mentions. Nothing this audit found *contradicts* the claim that an audit occurred — the F1–F4 dispositions are real, coherent, correctly sequenced in time, and substantively good, which is consistent with a genuine review having driven them — but the closure record cannot substantiate its own audit claim from retained evidence. **This artifact closes that gap prospectively for this audit; it cannot close it retroactively for the prior one.** Recommended disposition: a narrow governance reconciliation filing that (a) retains this artifact as the durable independent-audit record for the Phase One baseline, evaluated at the merged state `085f556…`, and (b) adds a dated note to the record clarifying that the original PR #143 audit's provenance is unretained and that Phase One's audit obligation is evidenced by disposition-verification-on-the-merits plus this artifact — rather than leaving "independently audited" resting on unretained prose. Future audit gates (OPS-0002 item 4's two remaining checkpoints) should require a retained, attributable artifact as a completion condition, not narrative alone.

**FA-2 — Minor (stale frozen record): PR #143's body overstates the F1 residual.** The branch it flags for manual deletion no longer exists; PR #144's preflight already recorded the corrected fact. No action needed — merged PR bodies are frozen history, and the live record is already correct — but any future reader of PR #143 alone should be aware its "manual cleanup still required" line is obsolete.

**FA-3 — Minor (open item, correctly handled): the BTC `holdings.yaml`/`targets.yaml`/CLAUDE.md conflict remains live and unresolved.** Verified present at the audited SHA. It is properly recorded (§9), properly covered by the F3 abstention rule for any qualifying run it would affect, and properly excluded from every authorization to date. It must receive its own separately verified factual-reconciliation change before any crypto-sleeve rebuild logic or Standing Queue wording is next touched — this remains the repository's own stated requirement, restated here so it is not lost.

**FA-4 — RESOLVED in v1.1 (was: verification limitation).** v1.0 could not re-execute the full test suite (no `pytest` in the audit environment). In Round 2 the audit environment was provisioned (`pytest`, `pandas`, `yfinance`, `curl_cffi` and dependencies installed — environment-only changes; no repository file touched) and the full suite was re-executed against the merged baseline at `085f556…`: **1,246 passed, 0 failed**, matching the closure record's claim exactly. The test-count claims in PR #143/#144 are now independently confirmed, not self-reported.

**No Blocking finding.** No architecture defect, authority violation, silent supersession, scope breach, or implementation-under-planning-authority was found. The diffs of both merged PRs match their authorizations exactly — which is the strongest structural evidence in the record.

## 5. Audit disposition

**PASS WITH FINDINGS — no Blocking finding; one Major provenance finding (FA-1) requiring a narrow governance reconciliation; two Minor. (v1.1: the former Observation FA-4 is resolved — test counts independently confirmed.)**

On the merits, the Phase One planning baseline is architecturally sound, internally consistent, consistent with all higher authority (Constitution, GOV-0002 hierarchy, constitutionally incorporated spec §20/§24), and its closure record accurately describes what was done — with the single exception that the "independently audited" claim was, until this artifact, unsupported by retained evidence. This audit does not declare Phase One complete (that declaration is the principal's, per the authorization's own terms); it states that with this artifact retained and FA-1's reconciliation filed, no evidentiary obstacle to that declaration remains.

## 6. Constraints honored

Read-only throughout: no repository file modified, no branch/PR/governance/workstream change, PR #145 not reviewed, modified, or advanced (confirmed still draft/unmerged at audit close), no implementation begun, no later phase authorized or implied.
