# CLAUDE.md — portfolio operational synthesis (current parameters, workflow, decision index)

## Identity & Role
You are my portfolio decision-support advisor. This tool **NEVER places orders** — recommendations only; order methods are stripped from `alpaca_client.py` (do not re-add). I execute manually on Robinhood. Combine tool output with judgment, and **push back when I break my own rules**.

**Governance architecture (added 2026-07-18, hierarchy clarified 2026-07-20 — GOV-0002):** immutable philosophy lives in `constitution/INVESTMENT_CONSTITUTION.md`, including material it expressly incorporates (`docs/MARGIN_DOCTRINE.md` in full; `docs/PORTFOLIO_INTELLIGENCE_SPEC.md` §20/§24); accepted decisions going forward get a file under `governance/decisions/` (see `governance/README.md`) and operate within constitutional bounds. This file is operational synthesis, not superior authority — current parameters, workflow, the agent entry point, and the decision index stay exactly here, unchanged and un-migrated, but this file cannot override the Constitution, an accepted governance decision, or governing accepted/frozen doctrine. See `governance/decisions/GOV-0002-operational-precedence-hierarchy.md` for the complete precedence hierarchy and conflict-resolution rules.

## Portfolio Doctrine
- Book = **net equity** (gross `holdings.yaml` total minus current margin debt; recompute both from source, never assume). `holdings.yaml` is this repository's source of truth for the most recently synced gross value, margin debt, and net equity — no point-in-time figure is hardcoded in this document; always recompute from `holdings.yaml` directly.
- **Margin (revised 2026-07-13):** allowed within a fixed **1.8x gross/equity structural leverage cap** (the level in place at revision — no forced paydown, no further levering up). **30% margin-buffer floor, hard cutoff** — before any margin-funded buy, the system checks the most recently synced, Robinhood-displayed buffer against the 30% floor; if that synced buffer is already below 30%, the buy is blocked outright (no partial taper) and it's treated as a forced de-lever signal, same severity as the cash-tight guardrail: trim or pay down margin, don't wait it out. Independently of the buffer check, requested margin is clipped to the 1.8x leverage cap. **The system cannot calculate Robinhood's exact projected post-trade buffer from the data available to it** — there is no projected-buffer formula anywhere in this codebase; only the currently-synced buffer is checked, never a post-trade estimate. (`holdings.yaml`'s `margin.buffer_pct` is this repository's source of truth for the most recently synced buffer — no percentage is hardcoded in this document.) Because only the synced figure is checked, margin data must be freshly synced from Robinhood before it's relied on for any margin-funded decision, and synced again immediately after execution — a stale buffer is checked as if it were current, so sync discipline is load-bearing, not optional. Buffer is a **capacity ceiling, not a timing throttle** — it tells you how much more you can safely borrow right now, not whether it's a good time to. Never read a high/rising buffer as a signal to add more leverage: buffer rises mechanically when positions gain value, which is exactly when pro-cyclically levering up (buy high, get hit hardest on the reversal) is most dangerous. Margin is not a timing tool for increasing leverage: discretion may never lever up beyond a governed recommendation or the fixed cap, and never bypass the buffer floor or a repayment rule — but the operator may always decline a recommendation, use less margin than allowed, or repay more or sooner than required. Whether a rules-based, evidence-gated conditional rule can outperform this fixed posture is a separate, open research question (`GOV-0003`) — not yet adopted, not operational, and no license for market-view-driven lever-up in the meantime. The cap is fixed and mechanical, same posture as the crypto sleeve's conviction-sizing model. Margin interest (~5% APR, first $1,000 free) is a guaranteed cost, not something to optimize around — it's a hurdle rate, not a variable.
- Robinhood. Deposits and margin (within the cap above) are fuel.
- **Canonical destination architecture** (`targets.yaml`'s `destination:` list is config truth, PHQ-2026-02, superseding the prior five-tier T1/T2/ETF/band/spec structure): 37 rows, each with its own explicit `target_pct` — canonical v1.30, principal-approved under `PHQ-2026-01`. Seven names (SNPS, ICE, SPGI, WM, RKLB, TSLA, SPCX) are gated — see `gates.yaml` — their destination weight stands but the allocator never buys them; that capital sits as ordinary cash, never redistributed. BTC/ETH/SOL each carry their own destination weight (2.0/1.5/0.5%), replacing the prior aggregate 10% crypto sleeve.
- Crypto sleeve: **ETH, SOL, BTC only.** HYPE removed from targets (July 2026 decision, consolidated 2026-07-12). BTC was the sleeve's designated liquid reserve; fully sold 2026-07-13 to trim the sleeve back toward target (see Open Items) — sleeve is now ETH/SOL only until BTC is rebuilt.

## Workflow (the only loop)
1. I deposit money and report buying power (or report available margin — check whether it's cash or margin before running the allocator; margin doesn't grow the book/target baseline the way cash does).
2. Positions are **live-priced automatically** (2026-07-13, extended to crypto 2026-07-14, extended to SKHY 2026-07-14): `holdings.yaml`'s `shares:` block (ticker → share count) and `crypto_shares:` block (coin → qty) are the source of truth for every normal position and coin — value = qty × latest Alpaca price, recomputed on every run, no manual resync needed for price moves. After a real buy/sell/trim changes a share count, paste `TICKER qty` lines → run `update-shares`; for a coin, `update-crypto-shares` (**merge mode; never `--replace` without my explicit confirmation**). BTC is manual until the reserve is rebuilt (no qty to track at $0) — paste screenshots or `TICKER value` lines → run `update-holdings`. Paste the margin screen too when it's changed → run `update-margin <debt> <buffer_pct>` (use Robinhood's own **displayed** buffer %, never derive it — see `allocate.py`'s `write_state()` comment for why).
3. Run `python allocate.py --cash X` for a cash deposit, or `--margin X` for margin-funded buying power (the two are gated differently — margin is clipped to the leverage cap and blocked below the buffer floor).
4. Present the recommendation table; I execute manually and confirm fills.
5. Sync share counts (and margin, if it changed) after execution.

**"Run a fresh allocation check" defaults to last-synced buying-power/margin state (2026-07-13).** Don't ask for a new buying-power/margin screenshot on every check — use the buying-power figure and margin state (debt/buffer) already on record in `holdings.yaml` unless I explicitly say something changed. I will proactively report deposits, withdrawals, margin draws/paydowns, or buffer changes when they happen; absent that, assume nothing moved. Portfolio value (gross/net equity) is computed fresh from all holdings on every run, **and since 2026-07-13 that includes live re-pricing** via `shares:` — no separate resync needed for ordinary price moves, only for a real share-count change (buy/sell/trim) or the BTC/margin manual paths above.

## Git sync — automatic, not a request
This repo is worked from multiple sessions (laptop, phone, browser). No session's local copy is authoritative — GitHub (`origin/main`) is. To keep every session honest without me having to say "push":
- **Start of every session**: `git pull` before reading `holdings.yaml`/`targets.yaml`/CLAUDE.md or doing anything — do this unprompted, first action, before touching state.
- **After any commit-worthy change** — classify the request first under `governance/decisions/OPS-0014-routine-operational-sync.md`. A read-only request (Class 0) makes no mutation, branch, or commit. A routine fact (Class 2 — exact share/crypto quantities, margin debt, a confirmed deposit/withdrawal/execution/non-execution) goes on its own narrow branch with the exact delta previewed first, and opens a draft PR under `OPS-0014`/`OPS-0009`'s standing procedure — it is never committed or pushed directly to `main`, and still requires my exact-head acceptance before merge (independent review may be waived only where `OPS-0009` §2's Lane M exemption applies). A material correction or any tier/target/gate/margin/allocator/policy change escalates to the fuller Class 3/4 lifecycle, with independent review required. No repository code ever places, routes, or submits a brokerage order. Don't wait to be asked to open that branch/PR — do it unprompted, and don't batch multiple unrelated changes into one commit — but never write directly to `main`.
- If `git pull` finds local changes not yet pushed from a prior session, stash-or-resolve and push those FIRST before proceeding — never silently overwrite unpushed state from another session.
- `.env` and anything git-ignored never gets committed — this rule is about tracked state only (YAML configs, CLAUDE.md, reports, logs).
- **After normal preflight, a substantial session reads `operations/WORKSTREAMS.yaml`** (the Portfolio-HQ workstream register — see `governance/decisions/OPS-0001-portfolio-hq-workstream-register.md`) and verifies its cached fields against live repository and GitHub state before acting on them. It never substitutes for this preflight or for live-state verification, and it's updated only after a material workstream change (status, milestone, blocker, authority, or next-action) — not on routine read-only checks.

Allocation logic (as implemented in `allocate.py`, migrated to the canonical destination architecture by PHQ-2026-02): fill **largest dollar gaps first**, $25 minimum lot, gated in order — **actionable gate** (`gates.yaml` — a gated name is never a buy candidate; its target capital simply isn't spent, never redistributed) → **RESERVE/CASH** (never a buy candidate, definitionally satisfied) → **200-SMA trend** (adds blocked unless RSI(14) < 30; skipped for crypto) → **7-day earnings blackout** (skipped for crypto) → **caps** (correlated-cluster caps, ≤ their own % of book each — see below, now binding on only their surviving canonical members) → **8% effective-issuer / 40% AI-platform common-driver no-add ceilings** (`issuer_lookthrough.yaml` — clip or block a buy, never a trim). Trims: a cluster-cap breach trims mechanically with **no RSI gate** (risk-limit breach, not a timing call) — largest-own-target-overweight-first, floored at each name's own destination target. **Retired by PHQ-2026-02**: the band/spec RSI-gated opportunistic trim and the T1/T2 1.5x concentration-ceiling trim (`gates.t1t2_trim_mult`) — both were defined in terms of tiers the canonical architecture doesn't have; no equivalent per-name trim rule was invented in their place. A ticker in more than one cluster needs room in all of them to buy. `--review` = no-cash rebalance check; `--levels` = buy-rung staging; `--health` = read-only portfolio risk-proximity snapshot showing margin state, leverage/buffer context, cluster-cap proximity, and 8%/40% no-add ceiling proximity. It adds no cash or margin, changes no allocator decision, writes no state, produces no composite health score, and places no orders.

**Correlated-cluster caps** (`targets.yaml` → `caps.clusters`, config truth): **semis** (ASML/TSM/NVDA/AVGO/AMD/MU/MRVL/KLAC/LRCX/AMAT/WDC/INTC/SKHY) ≤ 25% of book — chip names crash together, correlation-defined not market-cap-defined. **power_infra** (GEV/ETN/VRT/PWR) ≤ 20% of book, added 2026-07-14 — AI data-center power/electricity buildout theme, 0.560 avg pairwise correlation (trailing ~1yr) vs semis' 0.542 baseline. CAT and TSLA also correlated with this group in the same scan but were deliberately excluded — weaker fundamental fit, more likely bull-market beta than the specific mechanism (see Decisions Log). **oil** (XOM/CVX) ≤ 20% of book, added 2026-07-14 — same commodity driver, 0.819 avg pairwise correlation, the strongest found all session; only 3.46% of book when added, cap level derived from a stress test (see Decisions Log) not picked. **Membership unchanged by PHQ-2026-02** (not re-scoped — that's a separate future decision), but most prior members exited the canonical v1.30 roster in the transition, so each cap now binds on only its surviving canonical members: semis → ASML/TSM/NVDA/AVGO/KLAC; power_infra → ETN/GEV/PWR; oil → none remaining.

**T1/T2 concentration ceiling — retired 2026-07-31 (PHQ-2026-02).** Formerly: `targets.yaml` → `gates.t1t2_trim_mult`, default 1.5 — any T1 or T2 name above 1.5× its own tier target trimmed mechanically to target, no RSI gate. Defined entirely in terms of the T1/T2 tiers PHQ-2026-02's canonical-destination migration removed; no equivalent per-name trim rule was invented in its place — see the Decisions Log entry below for the historical record of why the rule existed.

## Standing Queue
1. **Rebuild BTC reserve** (decided 2026-07-13) — direct future crypto-sleeve buys to BTC specifically, not ETH/SOL, until it reaches **~25% of the crypto sleeve (~2.5% of book, currently ~$139, scales with book)** — matches BTC's prior share before it was fully drained to fix the July 2026 sleeve overage. Once there, resume manual coin-split discretion. `allocate.py` still only computes the aggregate sleeve gap (coin split is manual); this is an execution instruction, not a code change.
~~2. VMC full exit~~ — **done 2026-07-13** (sold 0.389582 sh @ $292.93, $114.12 net, consolidated into MLM).

~~3. PWR rebuy~~ — **done 2026-07-13** (bought $87, cash-funded).
~~4. SPCX trim~~ to 1.0% spec target — **done 2026-07-13** (trimmed same session as PWR; further drifted down on its own sharp price decline afterward).
~~5. HYPE + BTC dust consolidation~~ — **done 2026-07-12** (HYPE sold, BTC reserve rebuilt to ~$247). Robinhood's unsellable sub-cent dust (ZORA/WIF/BONK/PEPE, $0.00) is display noise — permanently ignored, never synced.
~~6. LHX full exit~~ — **done 2026-07-13** (confirmed absent from every holdings sync since).

## Decisions Log (do not relitigate without new evidence)
- **June 2026** — Band-overlay backtest: 227% vs **422% buy-and-hold** on the same basket → **NO-GO on automated trading**. Edge lives in the Phase 1 EMA base signal only; elaborate analysis layers were anti-predictive.
- **June 2026** — Stack & Rotate / 5-Point Filter / 3-tier TP system scrapped. Old BRK.B/UBER stops and TP ladders are dead.
- **July 2026** — Manual execution with decision support chosen over agentic trading.
- **July 2026** — Crypto = 10% target, ETH/SOL/BTC; HYPE out.
- **July 2026** — S/R levels (swing extrema, volume nodes, pivots) ruled computable and eligible for `--levels` rungs **only if a backtest beats the current SMA−ATR rungs**. Chart-pattern reading (flags, H&S, wave counts) permanently excluded — not computable, not backtestable.
- **July 2026** — **Crypto 10% reaffirmed as conviction-sizing, not a timing call.** The sleeve rebuilds through the gap machine only — it competes in the allocator's ranking like any underweight, with no timing gates (no trend/RSI/earnings; "cheap vs expensive" views don't size it). No lump-sum adds on directional views.
- **July 2026** — **Margin doctrine revised.** Discovered ~$4,423 in existing margin debt (~1.8x gross/equity) contradicting the prior "no margin" rule. Formally allowed going forward within a **fixed 1.8x structural leverage cap** and **30% buffer floor** (forced de-lever guardrail below that). Explicitly rejected: any margin-timing model ("borrow more when conditions look good") — same category as the band-overlay backtest and the multi-timeframe/Fable-5 research proposals floated the same session. Leverage amplifies whatever edge already exists (or doesn't); it is not itself a source of edge, and is not backtestable as one.
- **July 2026** — **Rung backtest verdict: NO CHANGE** (`reports/rung_backtest.md`). Three arms, 2021–2026, 65 tickers, pre-committed 1.0pp threshold: market-buy 30.12% > current rungs 29.35% > S/R rungs 29.05% annualized TWR. S/R rungs REJECTED (lost to both). C−A gap +0.77pp — rung-waiting cost money but inside noise, so `--levels` survives as staging aid. **Question closed; no variants, no re-runs without a new regime in the data.**
- **July 2026** — **AMZN kept at T2 (not promoted to T1).** At the time of that decision, T1 was 7-of-9 names in the identified AI-infrastructure grouping (ASML/TSM/NVDA/MSFT/GOOGL/META/GEV), which had been flagged as a concentration risk in a system deep-critique the same session. Sizing up another mega-cap while that finding sat unaddressed would have compounded the risk rather than fixing it. COST was later promoted to T1 under TGT-0002 as a distinct consumer-membership driver; AMZN remains T2 under its existing unsuperseded reasoning.
- **July 2026** — **Regime-gate backtest verdict: DROP THE GATE** (`reports/regime_backtest.md`). Two arms, 2021–2026, 63 T1-spec tickers (no crypto), pre-committed 1.0pp threshold, weight scheme and trim rule held at current production values so only the QQQ 200-EMA gate varied: current (gate ON, held 15 of 62 deposits as cash) 25.49% vs no-gate 28.05% — a 2.56pp/yr gap, more than double the threshold. **Removed**: `allocate.py`'s `render()` no longer blocks/zeroes deployment when QQQ is below its 200-day EMA; deposits always invest immediately based on gaps. Regime status is still computed and shown in every check (informational only, e.g. "ABOVE 200-EMA (risk-on)") — just no longer gates anything. Unlike the trim decision, no leverage-based counter-argument applies here: deploying a cash deposit during a downturn doesn't increase leverage (debt is fixed, both gross and equity grow by the deposit amount, so leverage ticks down slightly, not up) — the concrete danger that overrode the trim backtest's raw result doesn't have an analog for this one. Two honest caveats on record: this window (2022) contains one real but not prolonged/severe bear stretch, the gate's actual test case; and a behavioral case (forced pause during a confirmed downtrend reduces panic-sell risk) is real but not something a TWR backtest can price. **Question closed under the same pre-committed discipline as trims/rungs/weights; no variants, no re-runs without a new regime in the data.**
- **July 2026** — **Trend-gate backtest verdict: NO CHANGE** (`reports/trend_backtest.md`). Two arms, 2021–2026, 63 T1-spec tickers (no crypto), pre-committed 1.0pp threshold, weights/trims at production values, no regime gate in either arm (dropped from production before this test): current (gate ON, 930 buy attempts blocked/redirected) 27.64% vs no-gate 28.05% — a +0.41pp gap for no-gate, inside noise. This closed the system audit: it was the last untested gate in the buy path. Key contrast with the regime gate (dropped the same day at 2.56pp): a regime-gate block held whole deposits as *cash* (pure drag), while a trend-gate block only *redirects* money to the next-ranked gap, still fully invested — which is exactly why its measured cost is ~6x smaller and lands inside noise. The earnings blackout remains untested and is accepted as **untestable** (historical earnings dates for 63 tickers over 5 years aren't available on the free feed); its mechanism is the same money-redirect as the trend gate, so its cost is bounded by the same logic. **Audit complete — every mechanical rule in the buy path now tested exactly once; question closed; no variants, no re-runs without a new regime in the data.**
- **July 2026** — **Weight backtest verdict: NO CHANGE** (`reports/weight_backtest.md`). Four arms, 2021–2026, 63 T1-spec tickers (no crypto), pre-committed 1.0pp threshold, band/RSI trim rule held constant so only tier weighting varied: current (28.05%) vs equal-weight (28.41%), flatter gradation (27.83%), steeper gradation (28.80%) — all within 0.75pp, noise. The tier-weight *structure* has no measurable return edge over this window in either direction; weighting exists to express conviction, not to chase performance, and this test confirms there wasn't a performance difference being left on the table. **Question closed; no variants, no re-runs without a new regime in the data.**
- **July 2026** — **Third concentration cap added: oil (XOM/CVX), 20% of book — cap level derived, not picked.** Same-commodity mechanism (crude oil price drives both almost entirely) produced the strongest correlation of any group scanned all session: 0.819 avg pairwise, vs semis' 0.542 and power_infra's 0.560. Only 3.46% of book when added — nowhere near binding on size alone, so instead of picking a round number the level was derived via stress test: this dataset's own actual historical max drawdown for XOM/CVX (~30%, cached 2020–2025 window, not a real-world citation) applied at the cap ceiling produces ~6% of book max impact, matching power_infra's already-accepted risk budget (20% cap × 30% crash = 6%; semis' looser 25% cap implies 7.5%, justified by 13-name internal diversification vs oil's 2). Landed at the same 20% as power_infra from two offsetting effects: oil's correlation is higher and longer-established (argues looser) but only 2 names means less internal diversification against a company-specific shock (argues tighter). `targets.yaml`'s `caps.clusters` extended to a third entry — no code changes needed, the generalized cluster-cap mechanism built for power_infra handles this natively.
- **July 2026** — **Tier-fit scan: AAPL promoted band → T2; AVGO stays at T2 (not promoted to T1).** A one-time structural scan (objective company scale/sector facts, not fundamental research or return prediction — see the declined "rationalize all 65 holdings" request the same session) found two roster placements worth a real look, same lens as AMZN. **AVGO**: already a semis-cluster member: promoting it to T1 would compound the flagged T1 AI-infra concentration more directly than AMZN would have, since AVGO is itself semis-correlated — kept at T2, same verdict and reasoning as AMZN. **AAPL**: promoted band → T2. Unlike AVGO, this doesn't touch the flagged T1 concentration (AAPL isn't moving to T1), and T2 stays diversified either way (Info Tech goes from 1-of-14 to 2-of-15, not a new single-theme problem). AAPL's profile — scale, stability, blue-chip dominance — fit T2's then-existing "quality compounder" character (alongside COST/WMT/MA/BRK.B at the time; COST was later promoted to T1 under TGT-0002) better than band's mixed cyclical/smaller-cap company. Rest of the 65-name roster scanned and found to be reasonable fits for their tiers; no other placements flagged.
- **July 2026** — **VMC consolidated into MLM, VMC exited.** Both are direct aggregates-industry peers (crushed stone/sand/gravel; same infrastructure-spending/housing/rate-cycle drivers) — holding both wasn't real diversification, just redundant sector exposure. MLM (T2, 1.65%) carries the exposure alone going forward.
- **July 2026** — **Trim backtest verdict: KEEP 1.25x / RSI 60, no amendments** (`reports/trim_backtest.md`). Five arms, 2021–2026, 37 band/spec tickers, pre-committed 1.0pp threshold. Parameter variants (1.15x/55, 1.40x/65, mechanical no-RSI-gate) all within ±0.26pp of current → noise; nothing to re-tune; per-ticker tailored parameters rejected as overfitting. Never-trim (control) won the window by +6.65pp/yr — recorded honestly as the pre-committed "trims are drag" outcome — but did it by letting RKLB (spec tier) balloon to 12.6% of the sleeve (~3.6x intended size) with -28.6% vs -24.6% MaxDD. Under the account's ~1.6x leverage that is ~-46% vs ~-39% on net equity — deep enough to threaten the 30% buffer floor and force selling at the bottom, converting temporary drawdown into permanent loss; the unlevered backtest got to hold through its worst moment, a levered account may not. Trims are the enforcement mechanism of tier sizing (risk control), not a return layer; the 6.65pp is the insurance premium in a bull-only sample. **Question closed; no variants, no re-runs without a new regime in the data.**
- **July 2026** — **Second concentration cap added: power_infra (GEV/ETN/VRT/PWR), 20% of book.** Full-book sector audit found three single-name "sectors" (Materials/MLM, Utilities/CEG, Real Estate/EQIX, each <2.2% of book — thin but immaterial) and zero fixed-income exposure (correct given ~5% margin cost, not a gap to fill — GLD does the ballast job bonds would). No new tier warranted; the existing six (T1/T2/ETF/band/spec/crypto) already span the full conviction spectrum. Separately, a one-time correlation scan (trailing ~1yr daily returns, pairwise Pearson, same rigor as the trim/rung backtests) found GEV/ETN/VRT/PWR/CAT/TSLA correlated at 0.560 avg — comparable to the semis cluster's own 0.542 baseline (which is capped). Built the same mechanical no-RSI-gate cap for GEV/ETN/VRT/PWR only (real shared driver: AI data-center power/electricity buildout demand). CAT and TSLA were deliberately excluded from the cap despite testing correlated — weaker fundamental fit (general industrials; EV/energy storage), more likely riding bull-market beta than the specific power-buildout mechanism, and including weak-fit names would dilute the cap's meaning the way the semis cap's own comment warns against ("measured honestly so the cap actually binds"). Cap set at 20% (tighter than semis' 25%, reflecting a shorter track record and narrower evidence base — one bull-market-year sample) with room above the 10.5% level the 4-name group sat at when added, so it doesn't fire off a diagnostic finding alone. `allocate.py`'s `plan()` generalized from a single hardcoded semis cluster to a `caps.clusters` list — any ticker in more than one cluster needs room in all of them to buy; each cluster's mechanical trim runs independently, largest-own-target-overweight-first. Declined a broader "track correlation across everything continuously" system in the same conversation — same rejected pattern as every other standing-analysis-layer proposal; this cap is a bounded, one-time, actionable finding, not an ongoing signal. Also from the same scan: the enterprise-software group (CRM/MSFT/NOW, 0.650 correlation) was considered and declined — fails both the mechanism test (generic "software sector" beta, not a narrow specific driver like power-buildout) and the size test (6.8% of book, but 4.8% of that is just MSFT's existing T1 position; ex-MSFT weight is ~2%, immaterial). Cluster caps are also confirmed **ceiling-only by design, no target/floor** — a ticker's own tier weight already carries its conviction sizing; a cluster target would mean buying toward a thematic exposure goal, which is a different (and here, wrong-direction) decision than "these move together, cap the downside risk," especially with T1 already flagged as AI-infrastructure-concentrated.
- **July 2026** — **T1 AI-infra cluster cap: scanned and declined.** An external doctrine review correctly flagged that T1's long-standing AI-infrastructure concentration (7-of-9 names: ASML/TSM/NVDA/MSFT/GOOGL/META/GEV, previously only ever cited as a reason to *decline* new adds like AMZN/AVGO, never addressed directly) had never actually been correlation-scanned like semis/power_infra/oil. Ran the same one-time scan (trailing ~1yr daily returns, pairwise Pearson): **avg 0.302 (all 7), 0.373 excluding MSFT** — well below every existing cap (semis 0.542, power_infra 0.560, oil 0.819) and below the 0.650 enterprise-software group that was already declined for weak mechanism fit. This cluster is the same category, one level more diffuse: rate-expectations/index-flow beta across mega-cap tech, not a narrow shared driver like power-buildout demand or crude oil price. **Declined on the same bar as CRM/MSFT/NOW — no cap added.** Live check at scan time: T1 was 42.1% of book vs its 30.15% target (9 × 3.35%), with the 7-name AI-infra subset alone at 31.2% — confirming the concentration is real, just not correlation-cap-shaped. **Root cause identified instead:** T1 and T2 have no trim rule at all — only `band`/`spec` tiers trim on overweight in `plan()` — so T1 positions compound past target with nothing mechanical pulling them back. That's a bigger doctrine question (a new T1/T2 trim rule) requiring the same backtest discipline as every other rule here (see `trim_backtest.md` precedent) — flagged, not implemented same-session. **Question closed on the cluster-cap approach; no re-scan without new evidence.** GEV's multi-cluster membership (would have been the first case, alongside power_infra) stayed hypothetical — GEV remains in exactly one cluster.
- **July 2026** — **T1/T2 trim backtest verdict: NO CHANGE, no trim rule adopted** (`reports/t1t2_trim_backtest.md`). Direct follow-up to the T1 AI-infra cluster-cap decline above — the cluster-cap approach failed on correlation, but the underlying drift (T1 42.1% of book vs 30.15% target, nothing mechanical correcting it) was real, so this test asked the question directly: should a core-conviction T1/T2 position's overweight ever get capped, separate from any cluster question. New test, not a rerun of `trim_backtest.md` (that one covered band/spec, RSI-gated, opportunistic trims on names that ran hot — a different failure mode than capping a conviction position mid-drawdown). Four arms designed, 63-ticker T1-spec universe (no crypto, 24 in T1/T2), weight scheme/band-spec trim rule/trend gate held at production values, pre-committed threshold 1.0pp **plus** a MaxDD tolerance of 1.0pp (a TWR win bought with a materially deeper drawdown doesn't count — direct application of the `trim_backtest.md` precedent, where never-trim won on raw TWR but was rejected on leverage/forced-selling grounds): A — current (no T1/T2 trim) 27.64%, -20.3% MaxDD; B — mechanical 1.5x threshold, no RSI gate, 27.66%, -20.1% MaxDD; C — same 1.5x but skipped while the name is below its own 200-SMA, 27.66%, -20.1% MaxDD (**identical to B** — a name over 1.5x of target is, in this window, never simultaneously in a confirmed downtrend, so the two arms never diverge; a real, sensible result, not a bug). Arm D (AI-infra cluster-relative cap) was pre-committed to run only if B and C diverged by >1.0pp; they didn't (0.00pp gap), so D never ran — exactly the conditional discipline it was designed under. Gaps vs A: B/C +0.02pp TWR, MaxDD *improves* by 0.15pp — both trivially inside the noise threshold. **Verdict scope, precisely: this closes the *return* case for a T1/T2 trim rule, not the *capital-impairment* case** — same distinction the regime-gate entry drew between "cash drag, fully priced by TWR" and "capital impairment, not priced by TWR." The aggregate MaxDD numbers above are portfolio-level, the same quantity that was *not* where `trim_backtest.md`'s override case lived (that one was built on RKLB's own single-name drawdown, not the portfolio's). Ran the same single-name decomposition here: under Arm A's no-trim control, **NVDA peaked at 2.14x its T1 target with its own real -66.4% peak-to-trough (its actual 2021-2022 correction)** — at the account's current 1.44x leverage, that's severe enough that the linear drawdown×leverage translation (the same one used for RKLB) breaks down as a literal number; the honest read is that NVDA at that size sits in forced-liquidation/buffer-floor-breach territory on its own historical worst case, not a diversified-portfolio-level event. **The no-change verdict stands for the return question only. Current (no T1/T2 trim) stays in production; whether T1's real-world 42.1% concentration deserves a trim on capital-impairment grounds is a separate, still-open judgment this backtest surfaces the numbers for but does not itself settle** — flagged as its own Open Item rather than folded into this closed question. Sample-size caveat carried over from every prior backtest: one window, one mostly-rising sample with a single real bear stretch (2022) — this window's NVDA drawdown IS that stretch, not a hypothetical. **The return question is closed; no re-runs without a new regime in the data. The capital-impairment question is not closed and isn't a backtest question — see Open Items.**
- **July 2026** — **Semis cluster cap: leverage asymmetry identified, denominator left as net equity (book), no code change.** Margin debt is fixed while gross assets move, so the cap's book denominator shrinks *faster* than assets in a broad decline (can nudge the cluster over cap with zero real dispersion — no semis-specific move needed) and grows faster than assets in a broad rally (quietly loosens the cap even while semis itself gains). Verified against live numbers both directions. Real relative outperformance (semis specifically rallying vs. the rest of the book) still correctly triggers the cap **and** correctly grows every other position's target from the resulting bigger book — that rebalancing behavior (sell the concentrated winner, fund the underweight laggards) is sound and left untouched. Switching the cap's denominator to gross would remove the decline-side asymmetry but would understate true leverage-adjusted concentration risk during rallies, which is worse. Documented as a known structural property of a net-equity-denominated risk cap under leverage, not a bug to fix.
- **July 2026** — **Portfolio Intelligence Engine Version 2 frozen as canonical specification** (`docs/PORTFOLIO_INTELLIGENCE_SPEC.md`; structured record `decision_log.yaml` PI-0001). A future, optional layer for per-company thesis/risk/catalyst/conviction/review records, sitting **above** the allocator, never coupled to it: **advisory-only**, cannot modify `holdings.yaml`/`targets.yaml`/`allocate.py`, cannot alter any allocator recommendation, and no production code reads it under this specification. **The filesystem is the index** — an explicit `intelligence/index.yaml` was evaluated on seven criteria (single source of truth, git merge behavior, discoverability, reporting, validation, scalability, implementation complexity) and rejected on all seven; this is frozen doctrine, reconsiderable only through a new documented architectural decision backed by materially changed evidence, same standard as every other frozen call in this log. Each covered company gets **at most one structured YAML and one thesis Markdown file** — risks, catalysts, conviction rating, review cadence, and review history all live inside the company YAML, not scattered across topic-keyed files. `portfolio_role_ref` stores only the tier label, never a numeric weight — **`targets.yaml` remains sole authority for tier weights and allocation policy**, full stop. Coverage is opt-in; a missing company file is never an error. **Freezing this specification authorizes nothing beyond the document itself** — no company files, validators, reports, or allocator integrations exist yet, and any future allocator-visible integration (even a display-only footnote) requires its own separate research, justification, testing, and approval, the same bar every existing gate in `allocate.py` was held to.
- **July 2026** — **Portfolio Intelligence Phase 1 implementation authorized, bounded** (`decision_log.yaml` PI-0002). An implementation-readiness review returned REVISE on the first Phase 1 proposal solely for lacking this authorization — PI-0001 froze the design but explicitly didn't authorize building anything. PI-0002 authorizes exactly one thing: a standalone, read-only `intelligence_validator.py` (validating synthetic-only company YAML against the schema already frozen in the spec's §9/14/16/20/21) plus its own isolated test file. Missing/empty `intelligence/` coverage must return success with zero companies — absence stays a non-error. **No real company file, thesis content, or template is created by this authorization** — the spec's own suggested "one hand-authored file first" step is a human editorial act, not implementation work, so it's deliberately excluded here. Symbol resolution is also deferred — moot at this scope since no real ticker filenames exist yet to resolve. Validator must have zero import coupling with `allocate.py`/`margin_state.py` in either direction and must never write anything. **This is a bounded experiment, not authorization for the broader Intelligence subsystem** — PI-0001 remains the governing architectural decision, the implementation lands on its own separate branch/PR, this governance entry does not itself authorize merging that future PR, and any scope expansion needs its own new documented decision.
- **July 2026** — **Portfolio Intelligence Phase 2 pilot authorized, bounded** (`decision_log.yaml` PI-0003). A same-session readiness review found the Phase 1 validator faithful to the frozen spec with no blocking defects and confirmed a single human-authored company is the narrowest reversible next step in the spec's own incremental sequence. **One company beats every alternative considered**: a template file doesn't fit the spec's ticker-only directory model (same reasoning PI-0002 already applied); more synthetic examples add nothing Phase 1's 28 tests didn't already cover, since the whole point of a first real file is testing the schema against actual human judgment; **multiple companies are intentionally deferred** — same smallest-reversible-step discipline as every other gate in this log (rungs, trims, weights, the T1/T2 ceiling), letting the schema and workflow get reviewed before any roster-wide pattern is set; and reporting infrastructure comes strictly after real content exists to report on, not before. **Allocator integration stays out of scope for the same reason it always has** — any allocator-visible integration is gated behind its own separate research/justification/testing/approval, and this pilot doesn't attempt that bar; it's a content experiment, not an integration one. PI-0003 authorizes exactly: `intelligence/companies/`, one real `TICKER.yaml`, one matching `TICKER.md`, human-authored only, validated read-only via the existing validator — **no second company, no allocator integration, no generated conviction score, no symbol resolution, no dashboards/reports/rollups/`index.yaml`, no `holdings.yaml`/`targets.yaml` change**. `portfolio_role_ref` doctrine is unchanged from the frozen spec, refined for separation of concerns, not broadened: it is **descriptive only** — a human's own characterization of a company's role, fixed at authoring time, that does not authorize or derive any allocation behavior. Its meaning does not float with whatever `targets.yaml` currently contains — **changes to allocator implementation (sleeves, bands, tiers, weights, any allocation mechanics) never retroactively change the meaning of an existing Portfolio Intelligence record.** This decision deliberately does **not** enumerate a specific role list in Portfolio Intelligence governance text, since hardcoding one would smuggle an allocator-internal implementation detail into a layer that's supposed to stay independent of it; if role semantics ever need to change, that's its own future, separately-documented governance decision, never silently inherited from an allocator-side change. **This remains a reversible architectural checkpoint** — the pilot lands on its own branch/PR, gets reviewed before any second company is considered, and can be discarded or revised without touching anything outside `intelligence/`, since nothing else reads it.
- **July 2026** — **Portfolio Intelligence Phase 2 pilot review closed; expansion conditionally authorized** (`decision_log.yaml` PI-0004). COST (PR #71) implemented and merged, validator and full test suite passing, followed by an architecture retrospective — but PI-0003 left "reviewed" undefined as a completion criterion, so the pilot gate wasn't actually closeable until this decision. **Defines "pilot reviewed"** as a reusable five-part completion criterion: implementation merged to main, validator passing, full test suite passing, an architectural retrospective completed, and any governance ambiguities the retrospective surfaces explicitly resolved by governance — the fifth condition is satisfied by this very decision. **Freezes `conviction.rating`** to a closed four-value vocabulary — `Low`, `Medium`, `High`, `Very High`, nothing else, no numeric scale, no hyphenated hybrid — required now (not deferred) because it's one of the schema's few explicitly human-judgment fields and the ambiguity was demonstrated, not hypothetical: COST's own drafting hit this directly (a proposed "Medium-High" value fit neither of the frozen spec's two illustrative example scales). COST's existing `High` rating already conforms — no migration needed. **Deliberately leaves `risk.severity`/`risk.status` open** — one company record is insufficient evidence to justify a closed vocabulary for either field, unlike conviction; revisit once 3-5 records exist and real usage patterns are observable, same anti-overfitting discipline as `trim_backtest.md`'s rejection of per-ticker tailored parameters. **Note on enforcement:** the conviction vocabulary is closed as doctrine, not as code — `intelligence_validator.py` is unchanged by this decision and does not yet enforce it; that would be its own future, separately-authorized validator change. **This decision authorizes nothing beyond itself** — no second company, no schema change, no validator change; it closes the PI-0003 review gate and states that a future expansion phase (a second company, or a validator enforcement change) may now proceed through its own separate authorization, preserving the same Governance → Implementation → Validation → Retrospective → Governance cadence this log has followed throughout.
- **July 2026** — **Second Portfolio Intelligence company authorized: XOM** (`decision_log.yaml` PI-0005). A same-session, read-only candidate review screened all 61 eligible current holdings on architecture/workflow suitability (not investment attractiveness) and recommended XOM — its screening score isn't restated here as a company attribute, only the completed review is referenced. **XOM is the only company PI-0005 authorizes** — exactly the future creation of `intelligence/companies/XOM.yaml` and `intelligence/companies/XOM.md`, nothing else; adopting this decision does not itself create those files. XOM adds real novelty beyond COST without touching schema/validator/vocabulary: a different `portfolio_role_ref` (`band`, the corpus's first non-T2 value, still descriptive-only per PI-0003's unchanged doctrine), commodity/cyclical risk framing distinct from COST's structural/operational profile, a different catalyst shape (capex/dividend cycles vs. expansion guidance), and a multi-segment business that stays reviewable without forcing schema complexity — unlike bank or holding-company candidates the same review screened out. **Requires the same human approvals COST required** — `portfolio_role_ref`, `conviction.rating` (must be exactly one of PI-0004's four frozen values, never computed/scored/inferred), conviction rationale, review cadence, final thesis/risks/catalysts, and source-access disclosure — before any implementation PR opens. **`risk.severity`/`risk.status` stay exactly as unfrozen as PI-0004 left them** — two records still doesn't meet PI-0004's own 3-5 revisit trigger, so this decision doesn't advance that question. **No schema, validator, allocator, `targets.yaml`/`holdings.yaml`/margin, reporting, or `COST.yaml`/`COST.md` change is authorized, and no third company is authorized** — a successfully implemented XOM record does not itself authorize further expansion; that needs its own future decision, same pattern as every prior phase in this log. Implementation requires its own separate branch/PR and its own separate final pre-merge review before merge, same discipline as PR #71 and PR #72.
- **July 2026** — **Theme Intelligence data model frozen (`decision_log.yaml` PI-0006), no implementation authorized.** Follows from PI-0005, precisely stated: PI-0005 authorized a second Company Intelligence record (XOM) to test generalization beyond COST, but that implementation is not merged to `main` — `intelligence/companies/` currently holds only `COST.yaml`/`COST.md`. This freeze is grounded on what's actually merged: one working company record validating cleanly, isolation tests passing, and PI-0004's conviction vocabulary now enforced in code. It does not claim the workflow is proven across two companies. Freezes the flat data model: one `theme_id`, two files per theme, one-way authority only (company → theme, never the reverse), no stored sibling index or graph — "the graph is a reporting view, not a stored data model," the same filesystem-as-index discipline PI-0001 applied to Portfolio Intelligence. Themes carry evidence/risks/catalysts and a closed `lifecycle` vocabulary (`Emerging/Established/Mature/Declining/Archived`) but **no conviction rating and no numeric score**. Roadmap position: Theme Intelligence precedes Portfolio Intelligence aggregation, still fully deferred. **This is a freeze only** and does not itself authorize or permit any implementation. See the companion PI-0007 entry, which separately authorizes the one pilot slice conforming to this freeze.
- **July 2026** — **Theme Intelligence bounded pilot authorized (`decision_log.yaml` PI-0007).** Follows from PI-0006's freeze. Authorizes exactly one theme, `ai_infrastructure`, referenced by two new company records, **NVDA and GEV** — both already T1 holdings in `targets.yaml`. CLAUDE.md's own "T1 AI-infra cluster cap: scanned and declined" entry directly names both as part of the same 7-name AI-infrastructure subset it scanned (31.2% of book, correlation 0.302, cap declined) and states that concentration was "previously only ever cited as a reason to decline new adds... never addressed directly." That no record anywhere documents the *shared reasoning* behind the concentration is this decision's own inference from the current absence of any Theme Intelligence record — not a claim the cited entry makes itself. The pair also crosses two real correlation clusters — NVDA in `targets.yaml`'s `semis`, GEV in its `power_infra` — deliberately, testing whether cluster and theme membership coexist without collision. **Eight files total, two categories**: six intelligence content files plus two implementation files (`intelligence_validator.py`, `test_intelligence_validator.py`), limited to the minimum schema-enforcement change. **No change to `docs/PORTFOLIO_INTELLIGENCE_SPEC.md` is authorized, including no new section.** Regression requirement is merge-order-independent and does not depend on XOM having merged (it has not as of this decision): COST.yaml must remain valid and unmodified; if XOM.yaml exists on `main` when implementation begins, it must also remain valid and unmodified. Explicitly prohibited: a second theme, a third company, Portfolio Intelligence aggregation, any allocator/targets/holdings/margin touch, any change to COST/XOM, any stored index. **Authorizes future implementation only.**
- **July 2026** — **Theme Intelligence validator hardening retroactively recorded (`decision_log.yaml` PI-0008).** PR #79 (commit `205bb3c`) made `lifecycle` and `review` required fields on every theme record (previously only checked when present) and added a check that a theme's declared `theme_id` matches its own filename — both already merged before this entry was written. Validator-only, backward-compatible with the existing `ai_infrastructure` theme. **Closes a governance-recording gap, not a new authorization.**
- **July 2026** — **Second Theme Intelligence pilot retroactively recorded: life_sciences_tools_medtech, ISRG + TMO (`decision_log.yaml` PI-0009).** PR #80 (commit `16f7817`) — already merged before this entry was written — added one theme (`life_sciences_tools_medtech`, lifecycle `Established`) and two company records: ISRG (T2, conviction `High`) and TMO (T2, conviction `Medium`). **SYK and DHR were considered and explicitly deferred, not rejected.** The theme's own content refers to its approval process as the "PI-0009 Human Approval Record," which does not correspond to any committed file identified in this repository — this entry is the actual governance record. **Closes a governance-recording gap, not a new authorization** — no third theme or company is authorized.
- **July 2026** — **Margin risk-state classifier (`margin_state.py`) retroactively recorded (`decision_log.yaml` MARGIN-0003).** `classify_margin_state()`/`concentration_risk_score()` run on every `allocate.py` invocation other than `--levels`/`--performance` and are displayed in `render()`'s Margin section (a "Margin risk state: NORMAL/CAUTION/RESTRICTED/FORCED_DELEVER" line) whenever margin debt or a margin request is present — confirmed live in production, not proposed. **This entry records existing behavior; it does not authorize anything new.** The classifier runs strictly after `plan()` returns and cannot alter a buy/trim/block decision — verified by tracing the call site. In practice only `NORMAL` and `FORCED_DELEVER` are reachable today: `targets.yaml`'s `margin:` block defines no `states:`/`concentration_adjustment:` config, so every `CAUTION`/`RESTRICTED` threshold parameter is unset and those escalation paths never fire; `FORCED_DELEVER` continues to come from the pre-existing, unmodified `margin_capacity()` hard check. No prior Decisions Log or `decision_log.yaml` entry existed for this module — a repository-wide planning review (2026-07-18) found the gap and this entry closes it. **The repository's root commit introduced this module, its live call site, and four Phase 2 planning docs describing it as an unimplemented, unapproved proposal, all in the same commit** — no reconstructable history establishes an approval sequence, and none is claimed here. Corrected separately: `docs/PHASE2_IMPLEMENTATION_PLAN.md`, `docs/PHASE2B_REVIEW.md`, `docs/PHASE2D_INTEGRATION_PLAN.md`, `docs/MARGIN_INTELLIGENCE_DESIGN.md` (see status corrections). **This decision records the current implementation state. Future enhancements to the Margin Intelligence Engine — threshold calibration, `targets.yaml` state values, and repayment recommendations — remain subject to their own governance and are outside the scope of this entry.**
- **July 2026** — **Governance architecture adopted** (`governance/decisions/GOV-0001-governance-architecture-adopted.md`). Establishes `constitution/INVESTMENT_CONSTITUTION.md` and a forward ADR decision-record system under `governance/decisions/` for all new decisions from this point on. No allocator, `targets.yaml`, `holdings.yaml`, or Portfolio Intelligence behavior changed — documentation-governance only. Full rationale in the linked decision record, not restated here.
- **July 2026** — **Portfolio Intelligence specification reconciled with Theme Intelligence** (`governance/decisions/PI-0010-theme-intelligence-spec-reconciliation.md`). Codifies the Theme Intelligence schema and rules already frozen by PI-0006 and implemented by PI-0007/PI-0009 into `docs/PORTFOLIO_INTELLIGENCE_SPEC.md`, which previously had no mention of themes. No new capability, allocator authority, or aggregation authorized. Full rationale in the linked decision record, not restated here.
- **July 2026** — **Investment Ontology reference vocabulary adopted** (`governance/decisions/ONTO-0001-investment-ontology-freeze.md`). First repository formalization of an economic-systems/capital-type/company-review vocabulary previously discussed and directionally approved in investment-committee discussion, receiving final approval 2026-07-19 — deliberately narrower than the "Project B" proposal GOV-0001 excluded (no reorganization, no capital-type-driven reallocation, no scorecard database). Authorizes only the existence of `docs/INVESTMENT_ONTOLOGY.md`; no roster classification, scoring, ranking, next-dollar recommendation, tier, weight, target, cap, or allocator change. Full rationale in the linked decision record, not restated here.
- **July 2026** — **Intelligence Operations V1 authorized, bounded** (`governance/decisions/PI-0011-intelligence-operations-v1.md`). Authorizes a new, independent, source-read-only reporting/checking module (`intelligence_report.py`) and its test module — a generated staleness report (`intelligence/reports/staleness_report.md`, company records only, evaluating `review.next_due` and `pending`-status lapsed catalysts) plus stdout-only portfolio-role-drift, theme-reference-integrity (reusing `intelligence_validator.py`'s public API only, not a second validator), and filesystem coverage output. Role drift is advisory human-review output only — a mismatch never invalidates or rewrites a company record, and never rewrites `targets.yaml`, which remains authoritative. Zero import relationship with `allocate.py`/`margin_state.py` in either direction; the module is permitted to overwrite exactly one generated artifact and no other file; no ontology integration; Theme Intelligence staleness explicitly deferred. Full rationale in the linked decision record, not restated here.
- **July 2026** — **TSM committee-review pilot authorized, bounded** (`governance/decisions/PI-0012-ai-compute-committee-review-pilot.md`). Authorizes operational use of the Investment Ontology's qualitative review vocabulary strictly for a one-company TSM pilot, together with a TSM-specific evidence standard and future creation—after explicit human approval—of exactly `intelligence/companies/TSM.yaml` and `TSM.md` through a separate implementation PR. "AI Compute" is only the committee initiative's non-authoritative label, not a theme, schema value, system classification, sleeve, cluster, or allocator concept. No scoring, ranking, second company, new theme, schema, tier, target, holdings, or allocator change authorized. Full rationale in the linked decision record.
- **July 2026** — **PI-0012 TSM committee-review pilot closed** (`governance/decisions/PI-0013-tsm-pilot-review-closure.md`). Applies `decision_log.yaml` PI-0004's five-part "pilot reviewed" test to TSM: implementation merged, validator passing, full test suite passing (independently re-verified, not merely cited), retrospective completed. Resolves the one demonstrated ambiguity narrowly — any *future* decision that explicitly adopts PI-0012's eleven-question method for another company must also require a repository-fact reconciliation (against `targets.yaml`, `holdings.yaml`, the Intelligence inventory, and relevant theme references) before final approval — TSM's own shipped record needs no change and no schema/validator/code change is made now. The eleven-question method stays TSM-only. Tier/weight/share-count/ownership/cluster facts may be reported as context but are never quality, conviction, or priority evidence; PI-0013 does not otherwise define next-company selection criteria — that's left to a future, separate proposal. Authorizes no second company and no external research. Full rationale in the linked decision record.
- **July 2026** — **Bounded candidate-evidence review authorized for INTC, SYK, and DHR** (`governance/decisions/PI-0014-intc-syk-dhr-bounded-evidence-review.md`). Conversation-only, read-only fact-gathering on each name's own committed open question — TSM.md's unresolved TSM/INTC overlap question for INTC; the theme file's segment-reorganization timing question for SYK; whether DHR's FY2025 mixed segment picture has changed in subsequent reporting, explicitly excluding any revisiting of the existing TMO-redundancy finding. Not a PI-0012 pool-wide sweep and not an adoption of PI-0012's eleven-question method. Selects, ranks, and authorizes no company record or theme conclusion.
- **July 2026** — **Research Freshness Framework accepted, initial roster inert** (`governance/decisions/AUTO-0001-freshness-planner-v1.md`). Authorizes a reusable, three-layer system (deterministic monitor / bounded read-only refresh executor / human-reviewed incorporation) for keeping Company Intelligence records current against new filings and earnings, reconciled with PI-0011's existing staleness-reporting fields rather than duplicating them — the Company Intelligence specification and each company's own record remain sole owner of `review.cadence_days`/`last_reviewed`/`next_due`/`log`; PI-0011 remains the reporting consumer of those fields and the source of the strict `review.next_due < as_of_date` overdue rule, not their owner. All seven initial enrollment rows (COST/PI-0003, XOM/PI-0005, NVDA+GEV/PI-0007, ISRG+TMO/PI-0009, TSM/PI-0012) start `monitoring_enabled: false`; all seven checkpoints start `checkpoint_status: pending` with no baseline filing value asserted. `PI-0014` (INTC/SYK/DHR) is identified as the framework's intended first Path-B pilot, but `PI-0014`'s own accepted scope remains exactly what it already was — one bounded, conversation-based, read-only exercise with conversational findings and no repository artifact; this decision does not convert it to unattended execution or GitHub issue output, and does not itself authorize any GitHub Actions workflow, executable code, live adapter endpoint, or runtime prompt/schema — persistent issue output, unattended execution, scheduling, and activation for `PI-0014` (or any Path-B task) each require their own later, separate implementation or activation authorization. Full rationale and complete mechanical specification in the linked decision record and `docs/FRESHNESS_PLANNER_V1_SPEC.md`, not restated here.
- **July 2026** — **AUTO-0002 accepted: local-only validation/evaluator/identity foundation authorized for the Research Freshness Framework** (`governance/decisions/AUTO-0002-freshness-local-foundation.md`). Authorizes a later, separate implementation PR only — the governance filing itself implements no code — for exactly three pure, deterministic, non-operational modules (`freshness_validator.py`, `freshness_state.py`, `freshness_identity.py`) plus their three corresponding test files, implementing `AUTO-0001`'s registry/checkpoint schema, §9 precedence rule, and a canonical-JSON/SHA-256 identity contract for the cadence fingerprint and task-instance ID. Filing-derived fingerprint computation and replacement-authorization validation are both deferred — `AUTO-0001` doesn't fix enough of either to implement without inventing new content. Grants no network, GitHub, Claude-invocation, monitoring-activation, allocator, margin, or `PI-0014` authority. Full rationale in the linked decision record, not restated here.
- **July 2026** — **AUTO-0003 accepted: Cadence Trigger Core (Local, Deterministic)** (`governance/decisions/AUTO-0003-cadence-trigger-core.md`). Governance filing only — the filing itself adds no code. Authorizes a later, separate implementation PR containing exactly `freshness_cadence.py` plus `test_freshness_cadence.py`, implementing one pure function (`compute_due_cadence_fingerprint`) that gates on `monitoring_enabled`/`checkpoint_status` and, only when a ticker is enabled, verified, and overdue, delegates to the existing `freshness_identity.compute_cadence_fingerprint`. Resolves `AUTO-0001`'s open `PI-0011` strict-overdue-rule (`review.next_due < as_of_date`) reuse-or-equivalence obligation through the equivalence path — `freshness_cadence.py` does not import or modify `intelligence_report.py`; `test_freshness_cadence.py` proves strict overdue-boundary equivalence, not whole-function equivalence, against its real `collect_staleness_findings` for schema-valid synthetic records through a shared parametrized fixture. No operational monitoring, adapter, scheduling, GitHub, or Claude-invocation authority is granted by this filing.
- **July 2026** — **TGT-0001 accepted: Additive Target-Budget Policy for Roster and Tier Changes** (`governance/decisions/TGT-0001-additive-target-budget-policy.md`). Future tier/roster changes (promote, demote, add, remove) are additive by default — no universal offsetting demotion required — but every change stays individually authorized, and several proposed changes must be assessed cumulatively. Every future proposal must disclose before/after nominal target totals, implied fully-filled gross-exposure multiples, and cluster-target totals; a new ex-ante governance rule blocks any proposal that would push a cluster's configured target total above its existing cap, and overall leverage capacity never overrides that rule. No separate nominal-target ceiling was adopted, and that absence is not permission for automatic or unreviewed cumulative expansion. A promotion never requests, authorizes, or recommends margin — target configuration and margin capacity/usage remain distinct concepts. Future proposals changing AMZN, AVGO, or AAPL must identify and explicitly supersede the applicable prior reasoning from PR #18 or PR #58; TGT-0001 does not itself change or supersede those current placements. No allocator, `targets.yaml`, margin, cluster-cap, trim, or crypto behavior changed.
- **July 2026** — **COST promoted T2 → T1** (`governance/decisions/TGT-0002-cost-promotion-t2-to-t1.md`). The principal determined that COST independently possesses the qualities necessary for T1 placement. The decision does not re-rank or reconsider existing T1 holdings, adopt a general T1 admission doctrine, or authorize another promotion. Filed under TGT-0001; no configured cluster is affected, and no margin, trade, or live allocation action is authorized.
- **July 2026** — **GOV-0003 accepted: margin-timing principle narrowed, conditional research permitted, hard limits unchanged** (`governance/decisions/GOV-0003-margin-conditional-research-permitted.md`). Constitution §3's absolute "no timing, ever, full stop" claim is narrowed: discretionary/predictive margin timing remains permanently excluded, but whether a rules-based, pre-registered, evidence-gated conditional deployment or repayment rule outperforms the current fixed posture is now an open empirical question, researchable only under its own separate charter and adoptable only through its own separate governance decision. Constitution §4 gains a narrow carve-out for exactly this kind of bounded, retrospective backtest — no standing predictive layer, opportunity map, price target, opaque score, continuous signal, or precomputed recommendation is authorized. **The 1.8x leverage cap and 30% buffer floor are unchanged**; any future conditional rule operates strictly inside them; a high buffer or low maintenance ratio is never automatic permission to borrow. Operator discretion is asymmetric: always free to decline, use less, or repay more/sooner; never free to exceed a governed limit or bypass a hard safeguard. Authorizes no research charter, no code, no allocator change, and no resolution of the future revised MARGIN-0004 workstream by itself.
- **July 2026** — **MARGIN-0004 accepted: Phase 3–7A margin research closure reconciled against GOV-0003** (`governance/decisions/MARGIN-0004-phase3-7a-closure-reconciliation.md`). The 2026-07-13 "Margin doctrine revised" entry above remains valid and unedited for what it actually rejected — discretionary or predictive margin-timing — that rejection still stands. What's new: GOV-0003 later permitted bounded, pre-registered, evidence-gated conditional-rule *research* (not adoption) as a distinct, narrower question that entry never addressed. This decision formally reconciles the already-closed Phase 3–7A margin research program's closure (`docs/PHASE7_RESEARCH_PROGRAM_CLOSURE.md`) against that narrowing — recording what Phase 3–7A tested, what it deliberately left untested (any conditional/timing-based deployment or repayment rule was outside its charter, not an oversight), and correcting two stale passages in `docs/MARGIN_DOCTRINE.md` that had promised a dated update once evidence existed, when that evidence has existed since Phase 3–7A closed. Authorizes no new research charter, no shared data model, no code, no simulation run, and no production behavior — the 1.8x cap and 30% floor are unchanged.
- **July 2026** — **NUM-0001 accepted: Numeric Parameter Provenance and Calibration Standards** (`governance/decisions/NUM-0001-numeric-parameter-provenance-standards.md`, supporting audit `docs/NUMERIC_PARAMETER_PROVENANCE_AUDIT.md`). Establishes cross-domain provenance standards for consequential numeric parameters — six classes (externally imposed, mathematically derived, empirically calibrated, evidence-bounded governance selection, provisional governance guardrail, unsupported/unclassified) distinguishing calibration from evidence-bounded selection, provisional guardrails, and unsupported values. Changes and validates no existing number (1.8x leverage cap and 30% buffer floor remain unchanged, provisional, and binding); authorizes no research, code, validator, or allocator change.
- **July 2026** — **PI-0015 accepted: PI-0004 risk-vocabulary review closed, neither vocabulary frozen** (`governance/decisions/PI-0015-risk-vocabulary-review-closure.md`). Revisits `PI-0004`'s deferred `risks[].severity`/`risks[].status` question now that 7 Company Intelligence records exist (PI-0004's own 3-5-record trigger, met). Inventory: 34 total risk entries; `severity` shows only two values ever used (`moderate` 25, `low` 9 — `high` never appears); `status` shows exactly one value used 34/34 (`monitoring`). The six records added after the COST pilot introduced no severity or status value beyond COST's own original `moderate`/`low`/`monitoring` set — the corpus still lacks enough semantic differentiation to freeze either field. Neither field is frozen; no schema/validator/test change; no record migration. PI-0004's numeric "3-5 records" trigger is retired outright, replaced with no other record-count threshold. New triggers are evidence-driven only: `severity` revisits on a proposed value outside `moderate`/`low`, material reviewer disagreement over what severity measures or how a risk is labeled, or a proposed `risks[]` schema amendment; `status` revisits on an actual transition away from `monitoring`, a proposed alternative value, or a proposed schema amendment. A trigger opens review only — it never freezes a vocabulary by itself.
- **July 2026** — **PI-0016 accepted: standing, reusable one-company Investment Committee review methodology** (`governance/decisions/PI-0016-standing-company-review-framework.md`). Generalizes PI-0012's TSM-only eleven-question method and evidence standard into a standing methodology (15 narrative review dimensions, PI-0013's reconciliation gate applied by default, a bounded 2-5-comparator capital-priority comparison, and two independent conclusions — an advisory policy recommendation and a separate Intelligence-maintenance recommendation). Selects no company and authorizes no research: every future company review still requires its own separate, filed, repository-auditable research authorization naming one ticker and stating its own selection rationale — informal chat sign-off does not suffice. No automatic cadence, monitoring queue, or full-roster sweep is created. No Intelligence, tier, target, roster, cluster, cap, allocator, or code change is authorized by this decision alone.
- **July 2026** — **PI-0017 accepted: bounded NVDA committee-review research authorized** (`governance/decisions/PI-0017-nvda-committee-review-authorization.md`). The first company-specific authorization filed under PI-0016 — names NVDA as the sole review subject, adopts PI-0016's standing methodology and evidence standard by reference (no restatement, no redesign), and fixes TSM, ASML, AVGO, and MSFT as the exact capital-priority comparator set. Research-only: no research has been conducted, and this filing alone authorizes no research finding, Intelligence-record edit, policy/tier/target/roster/cluster/cap/allocator change, code change, trade, or order. Any downstream Intelligence update or policy change remains its own separate, future, separately-approved governance decision.
- **July 2026** — **PI-0018 accepted: bounded NVDA Company Intelligence refresh authorized** (`governance/decisions/PI-0018-nvda-intelligence-refresh-authorization.md`). The completed PI-0017 committee review (audited, revised, independently re-verified `READY FOR PRINCIPAL REVIEW`) concluded "Keep current policy" (advisory) and "Intelligence refresh recommended" (Intelligence-maintenance) — this decision is the separate authorization required to act on the second conclusion only. Authorizes a later, separate implementation PR to refresh evidence-based content only (newest reporting cycle, dividend/buyback, export-control, customer-concentration, competition/supply-chain evidence, source ledger, review dates) in `intelligence/companies/NVDA.yaml`/`NVDA.md`, requiring current primary sources wherever accessible. Explicitly preserves `conviction.rating: High` as baseline (a rating change needs its own separate human approval) and prohibits any tier/target/holdings/roster/cluster/cap/allocator/margin/code change or trade. This decision itself changes neither NVDA file. Implementation requires its own narrow branch/PR and validator/test/YAML-parse validation before merge.
- **July 2026** — **PI-0019 accepted: bounded GEV committee-review research authorized** (`governance/decisions/PI-0019-gev-committee-review-authorization.md`). The second company-specific authorization filed under PI-0016 — names GEV as the sole review subject (T1, 3.35% target, existing `Medium`-conviction Company Intelligence record from PI-0007, evidence base capped at FY2025/Q1 2026), adopts PI-0016's standing methodology and evidence standard by reference, and fixes ETN, VRT, and PWR — the other current-holding members of the governed `power_infra` cluster — as the exact capital-priority comparator set. Research-only: no research has been conducted, and this filing alone authorizes no research finding, Intelligence-record edit, policy/tier/target/roster/cluster/cap/allocator change, code change, trade, or order. Any downstream Intelligence update or policy change remains its own separate, future, separately-approved governance decision.
- **July 2026** — **PI-0020 accepted: bounded GEV Company Intelligence refresh authorized** (`governance/decisions/PI-0020-gev-intelligence-refresh-authorization.md`). The PI-0019 committee review was completed (corrected v4 packet, SHA256-pinned), independently acceptance-audited in the filing session itself — mechanical ledger recount, repository-fact re-verification, and fresh external corroboration of the Q2 Wind-call commentary all passing, verdict `READY FOR PRINCIPAL REVIEW` with zero findings — and both conclusions were then explicitly approved by the principal: "Keep current policy" (advisory; no change made — GEV stays T1/3.35%, `power_infra` member) and "Intelligence refresh recommended" (this decision is the separate authorization required to act on it). Authorizes a later, separate implementation PR to refresh evidence-based content only in `intelligence/companies/GEV.yaml`/`GEV.md`: Q2 2026 results and raised guidance, the 10-Q's debt ($2.6B) and tariff ($100M–$200M) disclosures, the Q2 call's maintained ~$400M FY2026 Wind-loss guide (mandatory secondary-reported provenance labeling unless a primary transcript is inspected first), and the omitted December 2025–February 2026 capital-allocation facts (dividend doubling, $10B buyback authorization, Prolec GE closing and financing). Committee arithmetic (~$657M H1 Wind losses, ~$257M implied H2) stays labeled as such, never as company guidance; the packet's §9.C exclusion list (Barclays note, EPS-vs-consensus, insider-selling reports, May 2026 permitting paraphrase, customer-concentration figures, turbine market share, share-price reactions) is binding and must not enter the record. Explicitly preserves `conviction.rating: Medium` as baseline (a rating change needs its own separate human approval) and prohibits any tier/target/holdings/roster/cluster/cap/allocator/margin/code change or trade. This decision itself changes neither GEV file; implementation requires its own narrow branch/PR and the full validator/test/YAML-parse/scope-review validation suite before merge.
- **July 2026** — **PI-0021 accepted: bounded COST committee-review research authorized** (`governance/decisions/PI-0021-cost-committee-review-authorization.md`). The third company-specific authorization filed under PI-0016 — names COST as the sole review subject (T1, 3.35% target, `High`-conviction Company Intelligence record originating as PI-0003's first pilot, tier later reconciled T2→T1 under TGT-0002, evidence base constrained by disclosed primary-source access failures), adopts PI-0016's standing methodology and evidence standard by reference, and names WMT, AMZN, BRK.B, and V as the exact capital-priority comparator set. Research-only: no research has been conducted, and this filing alone authorizes no research finding, Intelligence-record edit, policy/tier/target/roster/cluster/cap/allocator change, code change, trade, or order. Any downstream Intelligence update or policy change remains its own separate, future, separately-approved governance decision.
- **July 2026** — **PI-0022 accepted: bounded COST Company Intelligence refresh authorized** (`governance/decisions/PI-0022-cost-intelligence-refresh-authorization.md`). The PI-0021 committee review was completed (final v5 packet, SHA256-pinned, identity/measurements/recommendation text independently re-verified in the filing session) and the principal explicitly accepted both of its conclusions: "Keep current policy" (advisory; no change made — COST stays T1/3.35%, `conviction.rating: High`) and "Intelligence refresh recommended" (this decision is the separate authorization required to act on it). Authorizes a later, separate implementation PR to refresh evidence-based content only in `intelligence/companies/COST.yaml`/`COST.md`: current financial/membership evidence, the confirmed Q1–Q3 FY2026 digital-comparable-sales series, May/June 2026 sales (with the ex-gas/FX decomposition), the April 2026 dividend increase ($1.30→$1.47/qtr), current warehouse count (914→933) and expansion evidence, the 2024–25 labor-relations history and unresolved Local 174 status, the *Stockov v. Costco* tariff-refund litigation (allegations always kept distinct from established conduct), independently-supportable Kirkland evidence, risk/thesis-break factual status, source ledger/access limitations/review dates, and any later reporting period current at implementation time. Implementation must independently inspect current primary sources — the packet's prior external inspection is not a substitute; the flagged CEO tariff-refund pledge (packet C-15) and FCF/buyback detail (U-03/U-04) must not enter the record without primary confirmation, and unresolved items must stay marked unresolved rather than silently resolved or dropped. Explicitly preserves `conviction.rating: High` as baseline (a rating change needs its own separate human approval) and prohibits any tier/target/holdings/roster/cluster/cap/allocator/margin/code change or trade. This decision itself changes neither COST file; implementation requires its own narrow branch/PR, the full validator/test/YAML-parse/scope-review validation suite, and its own separate independent review and merge authorization.
- **July 2026** — **MARGIN-0005 accepted: Margin Deployment and Target Sizing research charter** (`governance/decisions/MARGIN-0005-margin-target-research-charter.md`). The research charter GOV-0003 anticipated and explicitly withheld: authorizes bounded, retrospective, pre-registered research (Studies A–D per the hash-pinned `research/margin_target_study/PROTOCOL_V2.md` and frozen `pre_registration.yaml`, filed in the same PR) into conditional margin deployment/repayment and target-structure sizing — the exact lanes MARGIN-0004 §B left untested. Hard bounds: 300-run trial ceiling (286 development + 10 untouched-test + 4 data-correction reserve, unused capacity lapses); untouched-test isolation (Track 2 development ends 2025-06-30, Track 3 ends 2020-12-31, ≤2/≤2/≤2 finalists frozen in append-only `candidate_freeze.yaml` before any untouched run); mandatory-vs-diagnostic stress split; Intelligence research strictly advisory (annotation / whole-cycle draw freeze / account-level repayment advisory only — no ticker-level recipient effects, no scores, prospective-shadow only). Research permission only: no simulation before the filing PR merges and pinned hashes verify, no data acquisition before its G1 gate, no production/allocator/target/margin/Intelligence change, no live signal, no trade or order — and no adoption of anything without its own separate future governance decision. The 1.8x cap and 30% buffer floor are unchanged; null results preserve the fixed posture. Full scope in the linked decision record, not restated here.
- **July 2026** — **OPS-0003 accepted: post-acceptance workstream priority transition** (`governance/decisions/OPS-0003-post-acceptance-workstream-priority-transition.md`). Narrowly supersedes OPS-0002 items 2 and 3 only, with respect to the WS-0001/WS-0002 priority ordering, the satisfied sequencing hold, and the completed WS-0002 planning-phase lifecycle state — OPS-0002 items 1, 4, 5, 6, 7, its three-layer architecture, implementation prohibitions, and audit gates remain fully effective. Records that PR #143 completed the WS-0002 planning package and its first independent audit; sets WS-0002 to `status: complete` (planning-and-audit phase only, no implementation authority) and `priority: secondary`; restores WS-0001 to `priority: primary`. Adds a read-only `research/margin_target_study/S2_G2_SCOPE_DETERMINATION.md` recommending a smallest coherent final S2 PR scope (dividend_ledger.py, validation_lib.py, overlay_lib.py, target_variants.py + tests) to literally close the G2 gate — authorizing no implementation, simulation, or trial consumption. Adds a short dated acceptance note to the blueprint; no change to its §§1–12 architecture.
- **July 2026** — **OPS-0004 accepted: WS-0002 Phase One audit-provenance reconciliation** (`governance/decisions/OPS-0004-ws0002-phase-one-audit-provenance-reconciliation.md`). A separate, later reviewing session (distinct from every session that authored PR #143/#144/OPS-0003), explicitly authorized read-only, independently re-audited the merged WS-0002 Phase One baseline at `085f556…` — PASS WITH FINDINGS, no Blocking finding; F1–F4 dispositions independently re-verified genuine; full test suite independently re-executed (1,246/0); `MARGIN-0005` charter hashes independently re-derived and matching. Its own Finding FA-1: `OPS-0003`'s claim of an "independent Fable audit" against PR #143 has zero retained, independently-attributable evidence behind it — PR #143 carries zero GitHub reviews/comments; the claim exists only as prose from the same committer identity as the audited work. FA-1 does not find the underlying dispositions false, only their provenance unretained. This decision retains the new audit artifact verbatim (`governance/audits/WS0002_PHASE_ONE_FABLE_AUDIT_20260724.md`, SHA-256 `7077d5ba...222db8`), establishes `governance/audits/` as the retained-audit-artifact convention, appends a narrow dated factual note to `OPS-0003` (substance otherwise unchanged), and sets a forward rule: `OPS-0002` item 4's two remaining audit checkpoints require a retained artifact going forward, narrative alone no longer suffices. No WS-0002 implementation, no WS-0001/`MARGIN-0005` change, no action on PR #145, no allocator/targets/holdings/margin change. Also recorded transparently: a `wc -w` recount of the retained artifact returns 2,216 words vs. an earlier-reported 2,267 — SHA-256 match (unaffected) is treated as the artifact's authoritative identity check, not either word count.
- **July 2026** — **OPS-0005 accepted: WS-0002 Phase Two reactivation, bounded to a read-only status/report layer** (`governance/decisions/OPS-0005-ws0002-phase-two-reactivation-authorization.md`). Narrowly supersedes OPS-0003 item 1 and OPS-0004 item 6's "no reactivation" clauses, exactly to the extent this filing is the new, explicit decision each anticipated — OPS-0002 item 5 is unaffected. Authorizes exactly one future implementation PR (one thin, stdout-only status/report module + its tests + a minimal doc pointer if convention requires one) displaying facts already computed by `allocate.py`, `margin_state.py`, `intelligence_report.py` (public API only), and `freshness_*` — margin/buffer/risk-state/staleness, market-data/earnings gaps, Intelligence freshness, and an explicit degraded-input list. No Phase Two code, no Phase Three, no WS-0003, no allocator/holdings/targets/margin change, and no S3/`MARGIN-0005` research authority; that future PR requires Fable Audit Gate #2 (blueprint §8 checkpoint 2, retained artifact per OPS-0004) plus explicit principal acceptance before it may merge. WS-0002's priority stays `secondary`, unchanged.
- **July 2026** — **OPS-0006 accepted: Portfolio Intelligence Completion, Relationship Mapping, and Zero-Based Tier Architecture Review workstream established (WS-0005), made primary; PR #149/WS-0002 operational reconciliation (F-2/F-3)** (`governance/decisions/OPS-0006-portfolio-intelligence-completion-and-zero-based-tier-review.md`). Establishes WS-0005 (`status: authorized`, `priority: primary`), governing purpose: "Understand every asset from first principles, understand how the assets interact, and use that evidence to design the simplest defensible portfolio-role, capital-priority, tier, target, and review framework." Existing tiers/targets/roles/caps/position sizes remain current operating policy in full force until formally superseded, and are explicitly never treated as presumptively correct evidence during this workstream's own research — a zero-based-research-discipline protocol (baseline preserved for comparison only, never as evidence; research from first principles; conclusions formed independently before formal comparison; explicit comparison only at a dedicated reconciliation milestone; explicit agreement/disagreement record; no silent inheritance of an old classification; accepted history never erased) governs it — stated as a research discipline, not a literal-information-blindness claim the repository can't actually enforce. Roadmap: nine sequential milestones (baseline/inventory; Intelligence coverage/freshness audit; Intelligence completion; portfolio relationship mapping, kept explicitly distinct from measured price correlation; zero-based classification/tier-architecture review; blind classification; baseline reconciliation; policy recommendation package; independent Fable review before any adoption). **Same-day pre-merge, pre-review amendment (2026-07-25, before any independent audit occurred on any version of this decision):** authorizes exactly Milestones 1 (baseline/inventory) and 2 (Intelligence coverage/freshness audit) to execute, normally as one bounded first audit PR, effective on merge — that PR may create one retained advisory audit artifact plus its dedicated tests/validators only where convention requires them, and must not complete or rewrite any company Intelligence record. **Milestones 3 through 9 remain unauthorized roadmap items; completing 1-2 does not itself authorize Milestone 3**, following `PI-0016`'s own per-company authorization discipline. The future Milestone-3 (Intelligence completion) and Milestone-4 (relationship mapping) work each gain an explicit, clearly separated margin-relevant-evidence requirement — per-asset cyclicality/leverage/balance-sheet/refinancing/macro-sensitivity/drawdown/gap/liquidity/concentration/thesis-deterioration/correlated-loss/borrowed-money-risk evidence, portfolio-level drawdown/stress/liquidity-cluster/substance-concentration mapping, and a future margin-relevance evidence register — all factual and advisory only, explicitly barred from recommending margin use, calculating a safe leverage level, ranking assets for margin deployment, modifying margin policy or allocator output, substituting for live broker/account state, authorizing `MARGIN-0005` S3, or consuming a trial. **Second same-day pre-merge, pre-review amendment (2026-07-25, PR #150 still zero GitHub reviews):** adds §9, "Zero-based future margin-policy review" — the 1.8x leverage cap and 30% buffer floor remain binding operational guardrails until formally superseded, while simultaneously being provisional, doctrine-status baseline values (per `NUM-0001`) that a future, separately authorized margin study must not treat as pre-defended assumptions or as constraints on the candidate policies it may examine on paper (no-margin operation, the current policy, alternative ceilings/floors, concentration-adjusted limits, deployment/repayment/deleveraging rules, broker mechanics, financing cost, liquidity/gap/recovery/permanent-loss risk), subject to the one Constitution §3 exception restated, not narrowed: no live system may exceed or disregard either value before a replacement policy is formally accepted and implemented. §9 authorizes no research, no `MARGIN-0005` S3 or successor study, no margin-policy recommendation, no production change, and consumes no trial; it changes neither parameter and does not supersede `MARGIN-0001` or any later margin decision — any actual retain/replace/tighten/loosen decision still requires its own separate accepted margin-governance decision after the relevant research and independent review. **Third same-day pre-merge, pre-review amendment (2026-07-25, PR #150 still zero GitHub reviews):** adds §§10-15 establishing Company/Theme Intelligence as a living evidence system, not a one-time archive. §10 states the lifecycle principle — a future governed refresh profile (evidence date, last/next review, cadence, freshness status, stale reason, material sources, event triggers, review ownership, information-age-vs-thesis-validity) — as a principle and future extension-candidate list only, explicitly preserving `AUTO-0001`'s and the Company Intelligence specification's existing field ownership (`review.cadence_days`/`last_reviewed`/`next_due`/`log`, `sources[]`, `freshness_registry.yaml`/`freshness_checkpoints.yaml`) and authorizing no schema/validator/record change. §11 extends the already-authorized Milestone 2 audit to a report-only freshness/lifecycle-infrastructure assessment (existing validators, evidence/review dates, event triggers, reuse-first recommendations) — no refresh, no policy change, no automation. §12 requires each future (still-unauthorized) Milestone-3 record to define a proportional, evidence-driven refresh plan with named candidate triggers (earnings, 10-K/10-Q/8-K, investor days, management changes, M&A/financing/credit events, competitive/regulatory/litigation changes, product/operational/security events, relevant macro/industry reports, thesis-break indicators) — explicitly no universal cadence. §13 records future scan design preferences (event-driven, low-maintenance, primary-source-preferring, reuse-existing-infrastructure-first) and illustrative advisory statuses reconciled with `docs/FRESHNESS_PLANNER_V1_SPEC.md` §9's existing frozen vocabulary, plus explicit prohibitions on any scan automatically rewriting Intelligence, changing tier/role/target/cap/holding, ranking assets, modifying allocator output, declaring a thesis broken without reviewed evidence, making a margin recommendation, or substituting summaries for primary sources. §14 requires disclosure of stale/insufficient evidence and permits abstention from a future recommendation, but states staleness alone never automatically triggers demotion, target reduction, trim, exit, or margin-policy change. §15 makes explicit that no automated scanner, recurring job, notification system, scheduled external-data collection, SEC/Federal Reserve/broker/news/market-data integration, generated research update, or production workflow coupling is authorized by this decision — any such implementation requires its own later, separate governance decision and bounded implementation PR. **Fourth same-day pre-merge, pre-review amendment (2026-07-25, PR #150 still zero GitHub reviews):** adds §16 (four subsections), the smallest complete progress-tracking, completion-evidence, and anti-duplication discipline for WS-0005, using the register's existing fields/status vocabulary — no new schema. §16.1: a milestone reaches `complete` only after every authorized deliverable exists, its PR merges, tests/validators pass, and `operations/WORKSTREAMS.yaml`/`governance/decisions.yaml` are synchronized — never from discussion, an edit, a commit, a push, or an open PR alone. §16.2: the future retained Milestones 1-2 audit artifact carries a per-asset completion ledger (inventory/coverage/freshness/margin-evidence/refresh-profile status, gaps, next action), reusing existing schema/registry fields where available, kept out of the register (no per-asset `WORKSTREAMS.yaml` entries). §16.3: every future WS-0005 session must inspect accepted decisions, the register, retained artifacts, Intelligence/freshness records, and open/merged PR state before working, and work only the next unfinished authorized unit — never repeating completed work without new evidence, recorded staleness, a verified error, an incomplete deliverable, or accepted reopening authority. §16.4: the register never originates authority; completing one milestone never authorizes the next; no completion claim may be inferred from a deleted branch, a satisfied blocker, a merged PR alone, or stale wording. No authority ceiling changes — Milestones 1-2 remain the only executable scope, Milestones 3-9 remain proposed. Explicitly prohibits changing tiers/targets/holdings, any order, allocator-formula changes, a computed conviction score, Intelligence-to-allocator coupling, margin-governance changes, `MARGIN-0005` S3, WS-0002 dashboard implementation, WS-0003/default-workflow changes, and generated reports replacing authoritative files. WS-0005 becomes the sole `priority: primary` workstream; WS-0001 moves to `priority: secondary` (sequencing only — its `MARGIN-0005` authority, milestones, evidence, and still-unauthorized S3 gate are completely unchanged); WS-0002 stays `priority: secondary`, `status: authorized`, with OPS-0005's Phase Two grant fully intact. Separately reconciles two non-blocking findings (F-2, F-3) that OPS-0005's independent Fable reviews (review `4779232640` and delta reviews) left open for "the ordinary first post-merge register sync," both located in `operations/WORKSTREAMS.yaml`'s WS-0002 entry: F-2 (`authorized_by` read "this session" with no session identifier) corrected to name the authoring session; F-3 (`active_branch`/`active_pr: 149` and milestone `status: drafted` self-stale at merge) corrected now that PR #149 is confirmed merged (merge commit `536e71f58857f2e55d98169b2e829e392c27a016`, matching `origin/main`'s tip) — `active_branch`/`active_pr` nulled, milestone flipped to `merged`. `OPS-0005`'s accepted text is not reopened or edited. No allocator, `targets.yaml`, `holdings.yaml`, margin, or Intelligence-record change.
- **July 2026** — **PI-0023 accepted: first bounded WS-0005 Milestone 3 batch authorized — ASML, AMAT, KLAC, LRCX** (`governance/decisions/PI-0023-ws0005-milestone3-batch1-semis-equipment.md`). Evidence development only — no research performed by this filing. Authorizes a later, separate implementation PR to create exactly one Company Intelligence record per company (existing schema), required source/evidence references, freshness metadata and a defensible refresh profile, relationship evidence among the four companies (structural overlap, not price correlation), margin-relevance evidence (factual/advisory only, no borrowing/leverage/deployment recommendation), an external opportunity/replacement-candidate scan (advisory leads only — no holding add, no `holdings.yaml` change, no tier/target assignment, no mechanical ranking, no batch expansion, no outside-candidate research without separate authorization), focused tests/validators only where convention requires them, and one new `intelligence/freshness_registry.yaml`/`freshness_checkpoints.yaml` row per company (`monitoring_enabled: false`). Batch selected as the `semis` cluster's semiconductor capital-equipment sub-segment (ASML lithography; AMAT/KLAC/LRCX deposition/etch/process-control — `targets.yaml`'s own comment already singles out "the equipment (KLAC/LRCX/AMAT)... names" as behaving distinctly within the cluster) — distinct from the cluster's chip designers, foundry, and memory makers, all still uncovered and unauthorized. Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use or margin-policy recommendation; any `MARGIN-0005` S3 or trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a second Milestone 3 batch (automatic or otherwise); or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`) — this is Company Intelligence research content (`category: portfolio_intelligence`), matching the `PI-0007`/`PI-0009` historical multi-company-batch precedent, not workstream-register mechanics. First-coverage discipline applies (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`), not `PI-0016`'s committee-review-of-existing-conviction framework — none of the four companies has an existing record to review. Zero-based discipline (`OPS-0006` §2/§3) applies throughout: current tier/target preserved as historical baseline only, never as research evidence. Opened as a draft PR; not merged, not researched, in this session.
- **July 2026** — **PI-0024 accepted: second bounded WS-0005 Milestone 3 batch authorized — MU, SKHY** (`governance/decisions/PI-0024-ws0005-milestone3-batch2-memory.md`). Evidence development only — no research performed by this filing. Authorizes a later, separate implementation PR to create exactly one Company Intelligence record per company plus one hand-authored batch comparison artifact, covering (per company) economic function/role, business model, DRAM/NAND/HBM/packaging product exposure, manufacturing footprint, customers/suppliers/competitors/substitutes, financial quality and cycle sensitivity, capex/supply discipline, management/capital allocation, hyperscaler concentration, China/Korea/Taiwan/export-control/geopolitical exposure, balance-sheet resilience, risks and disconfirming evidence, thesis-break conditions, what exposure is lost if absent, current tier/target labeled as historical policy only, factual margin-relevance evidence (no leverage recommendation), an evidence-driven freshness/refresh profile, and unauthorized external-opportunity leads only — plus a comparison covering HBM-vs-conventional exposure, product/technology/packaging differentiation, customer overlap, shared equipment dependencies (including NVDA/TSM/ASML/AMAT/KLAC/LRCX relationships), shared pricing/capex cycles, China/export-control exposure, correlated-loss mechanisms, and whether the pair adds distinct exposure or duplicates one memory-cycle bet — no score or ranking of any kind. Requires direct primary-source inspection attempts (SEC 10-K/10-Q/8-K, earnings releases, IR materials for MU; SEC registration statement/424B4, 20-F/6-K, SK hynix IR materials, Korean-market filings where usable for SKHY), with fact/inference/uncertainty/judgment/source-type/access-status separation throughout; earlier WebFetch failures on the Batch 1 research do not excuse skipping a fresh attempt. **WDC is explicitly excluded** — Western Digital separated its Flash business into Sandisk (SNDK) in February 2025, leaving WDC centered on HDD storage, not the DRAM/NAND/HBM memory-cycle function this batch examines; `targets.yaml`'s existing `semis` cluster comment grouping MU/WDC as "memory" names is identified as stale but is **not corrected by this filing** — recorded as a separate, unauthorized, future factual-reconciliation item. **The authorized batch is exactly MU + SKHY, with no license for a future implementation session to contract it to MU alone** for any reason, including inaccessible primary sources — insufficient evidence for either company requires stopping, disclosing the specific access problem, and returning for explicit principal amendment, not silent scope narrowing. Does not authorize: any tier/target/role/cluster/cap/holdings change (WDC's included); any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use or margin-policy recommendation; any `MARGIN-0005` S3 or trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a third Milestone 3 batch; the `targets.yaml` MU/WDC comment correction; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####`, first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`), not `PI-0016`'s committee-review framework — neither company has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session.
- **July 2026** — **PI-0025 accepted: third bounded WS-0005 Milestone 3 batch authorized — AVGO, AMD, MRVL, INTC** (`governance/decisions/PI-0025-ws0005-milestone3-batch3-compute-networking.md`). Evidence development only — no research performed by this filing, and **this filing itself authorizes only the governance-authorization package** (the decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any Company Intelligence record or the comparison artifact. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, and required to stay in draft state until that review lands — to create exactly one Company Intelligence record per company, one hand-authored four-company comparison artifact (`intelligence/BATCH3_SEMIS_COMPUTE_CONNECTIVITY_COMPARISON.md`), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization. Batch selected as the `semis` cluster's remaining compute/accelerated-computing/networking/custom-silicon/foundry sub-segment — distinct from Batch 1's equipment sub-segment and Batch 2's memory sub-segment — while preserving each company's materially different business model (AVGO: semiconductor plus infrastructure-software conglomerate; AMD: fabless CPU/GPU/accelerator designer; MRVL: fabless networking/custom-silicon specialist; INTC: integrated device manufacturer pursuing external foundry customers). Required per-company research standard covers, at minimum: AVGO's semiconductor-vs-software economics, custom AI accelerators, VMware integration/debt/deleveraging, and acquisition-integration risk; AMD's data-center CPU/accelerator positioning, NVIDIA/Intel competition, and TSMC dependence; MRVL's data-center networking/optical/interconnect/custom-silicon exposure and design-win timing; INTC's foundry strategy, manufacturing-roadmap execution, capital intensity/subsidies, and `PI-0014`'s prior conversational INTC overlap work treated as context only, never as a substitute for a filed record — plus margin-relevance evidence (factual/advisory only) and an evidence-driven, non-universal refresh cadence for all four. The comparison artifact must distinguish CPU/GPU/networking/custom-silicon/software/foundry exposure, fabless-vs-IDM economics, TSMC/packaging dependence, hyperscaler concentration, acquisition dependence, shared AI-capex-cycle risk, China/export-control exposure, and duplicated portfolio exposure — advisory only, no ranking or capital-deployment recommendation. Blocked primary sources must be labeled "attempted but not directly inspected," never merged with secondary evidence; **no silent scope contraction** — insufficient evidence for any one of the four requires stopping and returning for principal amendment, not narrowing the batch. Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a fourth Milestone 3 batch; the `targets.yaml` MU/WDC or BTC comment corrections; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`/`PI-0024`), not `PI-0016`'s committee-review framework — none of the four companies has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **OPS-0007 accepted: capability-based independent-review standard, and a bounded provisional allocation-readiness bridge for WS-0005** (`governance/decisions/OPS-0007-capability-based-review-provisional-allocation-bridge.md`). Governance-only — creates no Company Intelligence record, comparison artifact, target scenario, or allocation check, and does not itself review, correct, mark ready, or merge PR #161. Replaces, for WS-0005 review gates from this decision's own merge forward, a reviewer requirement based solely on a named model ("Fable") with a twelve-point capability-based standard (independence from authorship, exact-head anchoring, sufficient repository/controlling-authority access, sufficient web/research capability, primary-source-inspection attempts, fact/inference/evidence-type separation, validator/test verification, severity classification, retained GitHub-review-or-audit-artifact attribution, disclosure of model/session/access-limits/head/verdict, a bounded correction plus exact-head re-review for material findings, and explicit principal acceptance before merge). Fable remains eligible; an independent ChatGPT session (or any other reviewer meeting every point) is equally eligible; a Sonnet session may never review work it authored itself. **Prospectively supersedes only `PI-0025` §I's model-name-specific reviewer clause** — every other `PI-0025` §I requirement (independent review, exact-head anchoring, retained attribution, bounded correction, principal acceptance, post-merge verification, factual `operations/WORKSTREAMS.yaml` sync) is unchanged; `PI-0025`'s file is not edited and its `status: Accepted` is not changed, following this repository's own narrow-supersession convention (`OPS-0003`/`OPS-0005` precedent — the superseded decision keeps its accepted status). PR #161 (the PI-0025-gated AVGO/AMD/MRVL/INTC implementation) may therefore be independently reviewed by an eligible ChatGPT session once OPS-0007 merges — this filing leaves PR #161 exactly as found (open, draft, unmerged) and performs no review itself. Defines a **PROVISIONAL** Intelligence status requiring, at minimum, all five of: eligible-reviewed under §1; any required bounded correction and exact-head re-review completed; explicit principal acceptance at the exact final head; **merged to authoritative `main` at that head**; and post-merge ancestry/scope/validator/test re-verification with `operations/WORKSTREAMS.yaml` factually synchronized — **independent review and principal acceptance alone do not qualify a batch** (an independently reviewed but unmerged draft, including PR #161 as it stands, is not PROVISIONAL); once so qualified, content is not yet subjected to WS-0005's later, deeper Milestone-4-9/eligible-reviewer audit — without altering the frozen Company Intelligence schema (`docs/PORTFOLIO_INTELLIGENCE_SPEC.md` §9/§20/§24) — recorded through review metadata, `operations/WORKSTREAMS.yaml`, and scenario-artifact labeling, not a new schema field. Authorizes, effective only on its own merge and only as later, separate, bounded implementation PRs neither opened nor performed by this filing: **(a)** one preliminary portfolio-role/tier/target-range-scenario architecture package built only from Intelligence that has cleared the full PROVISIONAL definition above (merged and post-merge-verified, not merely reviewed), explicitly labeled provisional, preserving current `targets.yaml` policy as an unchanged comparison baseline only, and never claiming Milestones 3-8 complete; and **(b)** one scenario-only, cash-only, zero-margin ($0 margin requested) Monday allocation-check package comparing an official `targets.yaml`-based run against a separately named scenario configuration — **may display the allocator's own ordinary advisory buy/trim/underweight/blocked output for both runs (display is not execution; no output is automatically authoritative and none may be automatically executed)** — with a reconciliation table, confidence/evidence flags, no `targets.yaml` mutation, no `holdings.yaml` change beyond an independently authorized live sync, and no order placement — built by calling `allocate.py`'s existing pure functions (`plan()`, `build_roster()`, confirmed by direct code inspection to already accept `targets` as an explicit parameter rather than reading the file internally) with an in-memory scenario config or an isolated throwaway copy, not by modifying `allocate.py` itself. Requires every provisional artifact to record its creation date, evidence cutoff, source Intelligence heads, holdings-sync date, market-data run time, assumptions, unresolved evidence, and expiration/supersession rule, with mandatory re-review on Fable-access return, material underlying-record change, expanded coverage, a material thesis-affecting event, or expiration — superseding a provisional conclusion later does not make the provisional work wasted. Does not authorize: a fourth Milestone 3 batch or any Milestone 4-9 execution; any tier/target/holdings/cluster/cap/margin-parameter change; any `allocate.py`/`margin_state.py` modification; any margin deployment; execution, routing, or automatic placement of any trade or order (advisory display of what the allocator would recommend, per (b) above, is not execution and is exactly what this decision authorizes); or treating a provisional scenario as automatically authoritative. Opened as a draft PR; not merged, not independently reviewed, in this session — the governance PR itself must be independently reviewed (by an eligible reviewer under its own §1 standard) and principal-accepted before merge, and its own merge is the sole event that makes §1's capability standard, §2's `PI-0025` narrowing, §3's PROVISIONAL category, and the §4/§5 later-PR authorizations effective. **Bounded correction (same day, this PR):** an independent ChatGPT review returned CHANGES REQUIRED — two Major findings (§7's blanket "recommend or execute a trade" prohibition conflicted with §5's own advisory allocation-check authorization; §§3/4/8 did not require PR #161 to merge and pass post-merge verification before its records could be treated as PROVISIONAL or feed §4/§5 work) and one Minor finding (stale WS-0005 register wording on Batch 2/PI-0025/Batch 3/Milestone 9) — resolved by one bounded correction: §5 and §7 now distinguish advisory display from trade execution explicitly; §§3/4/8 now require the full chain (review, correction if needed, re-review, principal acceptance, merge, post-merge verification) before PROVISIONAL status or §4/§5 use; `operations/WORKSTREAMS.yaml`'s stale passages were corrected to reflect PI-0025's and PR #159's actual merged state. Requires its own exact-head independent re-review before this decision may be considered ready.
- **July 2026** — **PI-0026 accepted: fourth bounded WS-0005 Milestone 3 batch authorized — ETN, VRT, PWR** (`governance/decisions/PI-0026-ws0005-milestone3-batch4-power-infrastructure.md`). Evidence development only — no research performed by this filing, and this filing itself authorizes only the governance-authorization package (the decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any Company Intelligence record or the comparison artifact. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, and required to stay in draft state until that review lands — to create exactly one Company Intelligence record per company, one hand-authored three-company comparison artifact (`intelligence/BATCH4_POWER_INFRASTRUCTURE_COMPARISON.md`), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization. Batch selected as the three currently uncovered members of the governed `power_infra` correlated-cluster cap (`targets.yaml`: `GEV, ETN, VRT, PWR`, ≤20% of book) — GEV, the cluster's fourth member, already has an existing Company Intelligence record (`PI-0007`, refreshed under `PI-0019`/`PI-0020`) and may be used as existing comparison context in the batch's comparison artifact but its record must not be edited, refreshed, or reassessed by this batch under any circumstance. Required per-company research standard covers, at minimum: ETN's electrical-segment economics, data-center/utility end-market exposure, and acquisition/capital-allocation history; VRT's thermal/power-management-for-data-centers exposure, backlog, and customer concentration; PWR's electric-power/utility/communications-infrastructure segment mix, backlog and project-cancellation risk, and fixed-price-contract/labor/safety risk — plus margin-relevance evidence (factual/advisory only) and an evidence-driven, non-universal refresh cadence for all three. The comparison artifact must distinguish each company's distinct function in the power/electrification value chain, direct/indirect data-center exposure, backlog composition, recurring-vs-project economics, common macro/spending-cycle dependencies, and correlated-loss mechanisms — and must explicitly address whether the `power_infra` grouping describes one coherent risk cluster or several distinct economic functions, as an **advisory research finding only, with no automatic effect on the cluster or cap** (any actual `power_infra` cluster/cap change requires its own separate, later, explicit governance decision). Blocked primary sources must be labeled "attempted but not directly inspected," never merged with secondary evidence; no silent scope contraction — insufficient evidence for any one of the three requires stopping and returning for principal amendment, not narrowing the batch. Does not authorize: any tier/target/role/cluster/cap/holdings change (GEV's included); any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a fifth Milestone 3 batch; any advancement of `OPS-0007` §8 step I (the official-and-provisional Monday allocation-check package — eligible following step H's completion, but deferred, unbegun, and subject to its own separate implementation, review, and execution gates); or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`/`PI-0024`/`PI-0025`), not `PI-0016`'s committee-review framework — none of the three companies has an existing record (GEV's own separate `PI-0016`-methodology review under `PI-0019`/`PI-0020` is untouched and not reopened). Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **OPS-0008 accepted: Research Wave Protocol v1 for future WS-0005 Milestone 3 batches** (`governance/decisions/OPS-0008-research-wave-protocol-v1.md`). Follows a separate, explicitly bounded proposal-only session's audit finding that Batches 2-4 each consumed a governance PR, an implementation PR, and a dedicated post-merge reconciliation PR that changed nothing but recorded an already-complete merge — proportional overhead, not a batch-size effect — and that Batch 4's implementation required a mid-flight correction after research proceeded to completion under blocked primary-source access instead of stopping for principal direction (a `PI-0026` §D violation, corrected via an ad hoc, one-time principal-authorized GPT-5.6 Thinking evidence-recovery audit). Adopts, prospectively for future batches only (Batches 1-4 unaffected, unedited, and still valid under the discipline each was filed against): a default 5-6-company wave size with a documented common-economic-mechanism requirement (not merely shared sector/tier/cluster label); a mandatory **stop-before-drafting** primary-source-readiness gate, reversing the draft-then-correct sequence Batches 1/2/4 each actually followed, paired with a **standing, pre-authorized** evidence-recovery method (an eligible independent reviewer per `OPS-0007` §1 — no fresh one-off principal sign-off required per future instance, unlike Batch 4's ad hoc correction) bounded to sourcing/factual recovery only, no policy authority; a **default two-PR lifecycle** (one governance-authorization PR, one implementation PR carrying its own full review-correction-re-review-acceptance-merge cycle) followed by **mandatory but read-only post-merge verification** — folded into the next batch's own preflight section or a minimal register update, not a routine dedicated third reconciliation PR (one is filed only if verification finds a genuine material discrepancy); the unchanged `OPS-0007` §1 twelve-point review standard and the unchanged existing acceptance standard (primary-source discipline, claim-level provenance, thesis-break conditions, baseline-as-history-only, no ranking, full validators/tests, independent exact-head review, principal acceptance, post-merge verification); reference-not-restate retained-evidence discipline; and an explicit stopping condition against over-engineering the protocol further. **Bounded correction (same day, this PR):** an independent GPT-5.6 Thinking exact-head review found three boundaries — no automatic scoring/ranking/aggregation, no Intelligence-to-allocator/margin coupling, no automatic authorization of another wave or Milestone 4 — stated only by cross-reference rather than as this protocol's own operative text; resolved by adding new §12, "Hard boundaries," restating all three (plus that evidence-recovery sessions gain sourcing authority only, and that stale/inaccessible evidence requires disclosure or abstention, never an automatic policy change) as this protocol's own standalone, explicit limits, narrowing nothing already in force. A future narrow, read-only tool to automate mechanical PR-bookkeeping (SHAs, diff scope, checks, protected-path diffs, registry counts) is flagged as a candidate for its own future proposal — **not authorized, not built, not scoped by this decision.** Creates no Company Intelligence record, comparison artifact, or research finding of any kind. Filed in the same governance PR as `PI-0027` (Batch 5) but as its own separate decision file, per the principal's explicit amendment following GPT-5.6 Thinking's independent review of the originating proposal. Opened as a draft PR; not merged, not independently reviewed, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes this protocol effective for future batches.
- **July 2026** — **PI-0027 accepted: fifth bounded WS-0005 Milestone 3 batch authorized — MSFT, GOOGL, META, AMZN** (`governance/decisions/PI-0027-ws0005-milestone3-batch5-hyperscaler-ai-infrastructure.md`). Evidence development only — no research performed by this filing, which authorizes only the governance-authorization package (this decision file, `OPS-0008` filed alongside it, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, the applicable CLAUDE.md entries) — not drafting any MSFT, GOOGL, META, or AMZN Company Intelligence record or the comparison artifact. Closes the entire remaining Company Intelligence coverage gap in CLAUDE.md's own "T1 AI-infra cluster cap: scanned and declined" 7-name finding (ASML/TSM/NVDA/GEV already covered; MSFT/GOOGL/META were the only three left) and adds AMZN, the fourth major hyperscaler already cited, unverified, as a customer/counterparty across three completed batches' own records (Batch 1/3). The retained `WS0005_COVERAGE_GAP_REGISTER_20260726.md` independently flags MSFT/GOOGL/META with its own highest disclosed materiality rating. **EQIX was considered (as a data-center-REIT counterpart to this demand-side wave) and explicitly deferred, not included** — its REIT legal/disclosure structure is untested anywhere in this repository's Intelligence coverage; deferred by name to its own future batch, not silently dropped. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, required to stay in draft state until that review lands, and required to apply `OPS-0008`'s mandatory stop-before-drafting source-readiness gate before writing any company's substantive content — to create exactly one Company Intelligence record per company, one hand-authored four-company comparison artifact (`intelligence/BATCH5_HYPERSCALER_AI_INFRASTRUCTURE_COMPARISON.md`), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization following `OPS-0008`'s read-only-by-default post-merge convention. Required per-company research standard covers, at minimum: MSFT's Azure economics and OpenAI partnership/investment structure; GOOGL's Google Cloud/TPU strategy and ad-revenue concentration; META's AI-capex trajectory absent any public-cloud-resale business (explicitly distinguished from the other three); AMZN's AWS margin economics and Trainium/Inferentia strategy — plus antitrust/regulatory exposure for all four (each carries live or recent matters, to be researched as current fact, not assumed), margin-relevance evidence (factual/advisory only), and an evidence-driven, non-universal refresh cadence. The comparison artifact must distinguish public-cloud sellers (MSFT/GOOGL/AMZN) from the AI-capex-only buyer (META), custom-silicon dependency on already-covered semis-cluster names, and whether completing MSFT/GOOGL/META's coverage closes the documented 7-name AI-infra concentration finding — **an advisory research observation only, with no automatic effect on any tier, target, or cluster.** Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a sixth Milestone 3 batch; any advancement of `OPS-0007` §8 step I; any EQIX research or record; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`-`PI-0026`), not `PI-0016`'s committee-review framework — none of the four companies has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Filed in the same governance PR as `OPS-0008` (its own separate decision file), per the principal's explicit approval of a five-candidate Batch 5 comparison narrowed, after GPT-5.6 Thinking's independent review, to exactly these four names with EQIX deferred. **Bounded correction (same day, this PR):** the same independent GPT-5.6 Thinking exact-head review found the original text ambiguous on whether the future research must separate business quality from capital priority and compare each company against the next-best use of capital — resolved by adding explicit §B.23 (per-company) and §C.10 (batch-level) requirements stating that comparison, uncertainty-preserving and advisory only, producing no score or ranking, consistent with §G's existing prohibition. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **PI-0028 accepted: sixth bounded WS-0005 Milestone 3 batch authorized — V, MA, JPM ("Financial Infrastructure")** (`governance/decisions/PI-0028-ws0005-milestone3-batch6-financial-infrastructure.md`). Evidence development only — no research performed by this filing, which authorizes only the governance-authorization package (this decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any V, MA, or JPM Company Intelligence record or the comparison artifact. This session also independently re-verified, per `OPS-0008` §4(a)'s read-only post-merge convention, that Batch 5 (`PI-0027`, MSFT/GOOGL/META/AMZN) is fully merged (PR #169, merge commit `d16ec07f3c003f7e0ec218773388344a22a18977`) and that MSFT/GOOGL/META/AMZN now satisfy every element of `OPS-0007` §3's PROVISIONAL definition (eligible review with every material finding resolved across four rounds, principal acceptance and merge at the approved head, and this session's own post-merge ancestry/validator/test re-verification — 24 companies valid, freshness OK, 38 filed = 38 indexed, WS-0005 sole primary workstream, 1502/1502 tests passing, `git diff --check` clean). The first financial-sector batch: V (T1, 3.35%) and MA (T2, 1.65%) are grouped as a payment-network economics pair — the retained `operations/provisional/WS0005_COVERAGE_GAP_REGISTER_20260726.md` itself already flags both as sharing the same unverified interchange-fee regulatory exposure — while JPM (band, 0.75%) is included as a structurally distinct financial-intermediation role (deposit-taking, lending, credit, trading, investment banking), not a second network name; none of the three is a member of any `targets.yaml` correlated-cluster cap. **Three companies, smaller than `OPS-0008` §1's 5-6 default**, justified on coherence grounds — no further genuinely coherent uncovered financial-sector candidate remains in the roster, and forcing a larger wave (e.g. by adding BRK.B) would dilute rather than serve coherence. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, required to stay in draft state until that review lands, and required to apply `OPS-0008`'s mandatory stop-before-drafting source-readiness gate before writing any company's substantive content — to create exactly one Company Intelligence record per company, one hand-authored three-company comparison artifact (`intelligence/BATCH6_FINANCIAL_INFRASTRUCTURE_COMPARISON.md`), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization following `OPS-0008`'s read-only-by-default post-merge convention. Required per-company research standard covers, at minimum: V's payment-network economics, interchange-fee regulatory/litigation exposure, and issuing/acquiring bank-partner dependency; MA's directly-compared payment-network economics, scale, and value-added-services emphasis relative to V; JPM's deposit/lending/credit-cycle economics, investment-banking/markets/custody revenue, regulatory capital and G-SIB status, and its distinction from V/MA's fee-based network model — plus margin-relevance evidence (factual/advisory only) and an evidence-driven, non-universal refresh cadence for all three. The comparison artifact must distinguish V-versus-MA payment-network overlap/redundancy from JPM's balance-sheet-intermediation role, and must explicitly address the limits preventing a mechanical capital-priority ordering across a fee-based network model and a regulated depository institution — **an advisory research finding only, with no automatic effect on any tier, target, or cluster.** Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a seventh Milestone 3 batch; any advancement of `OPS-0007` §8 step I; any EQIX research or record (EQIX remains deferred per `PI-0027`); or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`-`PI-0027`), not `PI-0016`'s committee-review framework — none of the three companies has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **PI-0029 accepted: seventh bounded WS-0005 Milestone 3 batch authorized — LLY, ABBV, MRK, JNJ, GILD ("Biopharmaceuticals")** (`governance/decisions/PI-0029-ws0005-milestone3-batch7-biopharmaceuticals.md`). Evidence development only — no research performed by this filing, which authorizes only the governance-authorization package (this decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any LLY, ABBV, MRK, JNJ, or GILD Company Intelligence record or the comparison artifact. No prior batch has covered any pharmaceutical/biopharmaceutical holding; this is the roster's first coverage of that economic mechanism. Authorized under `OPS-0008`'s Research Wave Protocol v1 at its default 5-6-company wave size, common economic mechanism stated as biopharmaceutical franchise and pipeline economics: patented therapeutic franchises, clinical development, regulatory approval, commercialization, exclusivity loss, pipeline replenishment, acquisition dependence, and capital allocation — explicitly distinct from `life_sciences_tools_medtech`'s device/tools/diagnostics economics (ISRG, TMO; SYK, DHR deferred) and from UNH's managed-care/insurance economics. **UNH was considered and explicitly excluded from this batch, not covered** — a structurally different economic mechanism (payer economics, not branded-franchise/pipeline economics); UNH remains uncovered and unauthorized, alongside EQIX (`PI-0027`). Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, required to stay in draft state until that review lands, and required to apply `OPS-0008`'s mandatory stop-before-drafting source-readiness gate before writing any company's substantive content — to create exactly one Company Intelligence record per company, one hand-authored five-company comparison artifact (`intelligence/BATCH7_BIOPHARMACEUTICALS_COMPARISON.md`), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization following `OPS-0008`'s read-only-by-default post-merge convention. Required per-company research standard covers, at minimum: LLY's GLP-1/incretin franchise concentration and manufacturing buildout; ABBV's post-Humira biosimilar-erosion trajectory and replacement-franchise strategy; MRK's key oncology franchise concentration and patent-cliff/diversification strategy; JNJ's post-consumer-health-separation pharmaceutical/MedTech segment structure (explicitly distinguished from `life_sciences_tools_medtech`'s existing membership rather than assumed identical) and multi-franchise patent-cliff exposure; GILD's antiviral franchise concentration and oncology/cell-therapy pipeline diversification — plus margin-relevance evidence (factual/advisory only, including patent-cliff-driven revenue-cliff risk as a distinct drawdown driver) and an evidence-driven, non-universal refresh cadence for all five. The comparison artifact must distinguish franchise-concentration and patent-cliff-timing profiles across the five, JNJ's distinct multi-segment structure, and whether the group represents genuine diversification or duplicated regulatory/pricing/biosimilar-competition exposure — advisory only, no ranking or capital-deployment recommendation. Blocked primary sources must be labeled "attempted but not directly inspected," never merged with secondary evidence; no silent scope contraction — insufficient evidence for any one of the five requires stopping and returning for principal amendment, not narrowing the batch. Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; an eighth Milestone 3 batch; any advancement of `OPS-0007` §8 step I; any EQIX or UNH research or record; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`-`PI-0028`), not `PI-0016`'s committee-review framework — none of the five companies has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **PI-0030 accepted: eighth bounded WS-0005 Milestone 3 batch authorized — IBM, NOW, CRM, ORCL, CRWD, PANW ("Enterprise Software and Cybersecurity")** (`governance/decisions/PI-0030-ws0005-milestone3-batch8-enterprise-software-cybersecurity.md`). Evidence development only — no research performed by this filing, which authorizes only the governance-authorization package (this decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any IBM, NOW, CRM, ORCL, CRWD, or PANW Company Intelligence record or the comparison artifact. No prior batch has covered any enterprise-software or cybersecurity-platform holding. Authorized under `OPS-0008`'s Research Wave Protocol v1 at the top of its default 5-6-company wave-size range, common economic mechanism stated as enterprise software and cybersecurity platform economics: recurring subscription/support revenue, switching costs, embedded enterprise workflows, cloud and hybrid-cloud delivery, platform consolidation, AI monetization and infrastructure cost, enterprise IT/security budgets, renewal durability, sales efficiency, acquisition integration, competitive bundling, and cyber/data/regulatory risk. All six are `band` tier (0.75% target, 1.25x cap); none is a member of any `caps.clusters` correlated-cluster cap. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, required to stay in draft state until that review lands, and required to apply `OPS-0008`'s mandatory stop-before-drafting source-readiness gate before writing any company's substantive content — to create exactly one Company Intelligence record per company, one hand-authored six-company comparison artifact (`intelligence/BATCH8_ENTERPRISE_SOFTWARE_CYBERSECURITY_COMPARISON.md`, required to address CRWD/PANW's direct competitive overlap explicitly), one freshness-registry row and one freshness-checkpoint row per company, and minimum factual `operations/WORKSTREAMS.yaml` synchronization following `OPS-0008`'s read-only-by-default post-merge convention. Required per-company research standard covers, at minimum: IBM's hybrid-cloud/Red Hat and consulting-segment economics and watsonx AI-monetization strategy; NOW's workflow-automation subscription/renewal economics and Now Assist AI-monetization traction; CRM's CRM/Data Cloud/Agentforce platform economics and acquisition-assembled product-stack integration risk; ORCL's database-to-cloud-infrastructure (OCI) transition, AI/cloud-capex capital intensity, and debt load; CRWD's Falcon platform ARR economics and the July 2024 outage's disclosed financial/contractual/reputational impact; PANW's network-security-to-cloud/SASE/XSIAM platformization strategy and acquisition-driven expansion — plus margin-relevance evidence (factual/advisory only, including cybersecurity-incident tail risk as a distinct drawdown driver for CRWD/PANW) and an evidence-driven, non-universal refresh cadence for all six. **This filing also incorporates the deferred Batch 7 post-merge factual synchronization** left incomplete when PR #175 (a dedicated reconciliation PR correcting four stale `operations/WORKSTREAMS.yaml` fields after PR #174's merge) was closed without merging — per the principal's explicit instruction, this filing does not reopen or reuse PR #175's closed branch, but independently re-derives the same small, accurate delta directly from current `main` (confirmed matching PR #175's intended corrections exactly) and records it here, per `OPS-0008` §4(a)'s convention of folding prior-batch post-merge verification into the next batch's own governance filing rather than a dedicated third PR — the smallest accurate delta, not a restatement of PR #175's full narrative body; PI-0029/Batch 7 (LLY, ABBV, MRK, JNJ, GILD) itself remains unedited and fully complete. Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a ninth Milestone 3 batch; any advancement of `OPS-0007` §8 step I; any EQIX or UNH research or record; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`-`PI-0029`), not `PI-0016`'s committee-review framework — none of the six companies has an existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **PI-0031 accepted: ninth bounded WS-0005 Milestone 3 batch authorized — CVX only, and Milestone 3 completion standard defined** (`governance/decisions/PI-0031-ws0005-milestone3-batch9-cvx-completion-standard.md`). Evidence development only — no research performed by this filing, which authorizes only the governance-authorization package (this decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`, this CLAUDE.md entry) — not drafting any CVX Company Intelligence record or the XOM/CVX comparison artifact. **CVX is the sole remaining uncovered member of the governed `oil` correlated-cluster cap** (`targets.yaml`: `XOM, CVX`, ≤20% of book); XOM is already covered (`PI-0005`), unmodified and not reopened here. A deliberate single-company wave — smaller than `OPS-0008` §1's 5-6-company default, justified as an intentional use of §1's smaller-wave exception because no larger coherent economic-mechanism group currently exists for this specific coverage gap. **WDC is explicitly not bundled with CVX** — WDC remains the sole uncovered `semis`-cluster member, a wholly different cluster and mechanism, untouched by this filing. Authorizes a later, separate implementation PR — gated on this governance decision's own independent review, principal acceptance, and merge, required to stay in draft state until that review lands, and required to apply `OPS-0008`'s mandatory stop-before-drafting source-readiness gate before writing CVX's substantive content — to create exactly one Company Intelligence record (CVX), one hand-authored, bounded XOM/CVX comparison artifact (XOM's own existing record read only as context, never modified), one freshness-registry row and one freshness-checkpoint row for CVX, and minimum factual `operations/WORKSTREAMS.yaml` synchronization following `OPS-0008`'s read-only-by-default post-merge convention. Required research standard covers, at minimum: CVX's upstream/downstream/midstream segment mix; its shared commodity-price mechanism with XOM specifically (the driver the `oil` cluster cap is built on); financial quality, dividend history, and cyclical behavior; capital allocation; regulatory/environmental/geopolitical/energy-transition risk; thesis-break conditions; margin-relevance evidence (factual/advisory only); and a capital-priority comparison against XOM and other governed holdings, advisory and score-free. **Also defines the Milestone 3 completion standard**, a future qualifying test (not itself satisfied by this filing or by Batch 9 alone): (1) every T1 company covered — already true, all ten T1 holdings carry a record; (2) every non-deferred T2 company covered — not yet true, **CEG, BRK.B, WMT, MLM, and AAPL remain uncovered with no deferral rationale on record**, while DHR, SYK (`PI-0014`), and EQIX (`PI-0027`) remain uncovered but explicitly deferred; (3) every active correlated-cluster-cap member covered — `power_infra` fully covered, `oil` closes once this batch's implementation merges, **`semis` still missing WDC**; (4) every remaining uncovered company covered, deferred, or reassigned; (5) no unresolved MATERIAL research-coverage finding (CRM's and IBM's two residual findings stay MINOR, the universal 90-day cadence observation stays a NOTE — neither is MATERIAL, and this criterion doesn't require resolving either); (6) coverage indexes, freshness records, and `operations/WORKSTREAMS.yaml` synchronized; (7) every completed record has passed the full independent-review/merge/post-merge-verification lifecycle per `OPS-0007` §3. **States explicitly**: these seven criteria authorize no research for any other ticker; satisfying Batch 9 does not itself complete Milestone 3; WDC and the remaining non-deferred T2 gaps stay open; Milestone 4 remains unauthorized until a later decision determines the criteria are met. Does not authorize: any tier/target/role/cluster/cap/holdings change; any trade or investment recommendation of any kind; any ranking/conviction/composite score; any allocator or production-code change; any margin use, margin-policy recommendation, or `MARGIN-0005` S3/trial consumption; any automated scanner/scheduler/external-data integration; Milestone 4 execution beyond the batch's own required overlap evidence; a tenth Milestone 3 batch; any advancement of `OPS-0007` §8 step I; any research on AAPL, CEG, DHR, SYK, BRK.B, WMT, MLM, EQIX, UNH, or WDC; reopening CRM's/IBM's residual findings or the 90-day NOTE; or any generated report replacing an authoritative Intelligence record. Filed as `PI-####` (not `OPS-####`), first-coverage discipline (like `PI-0003`/`PI-0005`/`PI-0007`/`PI-0009`/`PI-0023`-`PI-0030`), not `PI-0016`'s committee-review framework — CVX has no existing record. Zero-based discipline (`OPS-0006` §2/§3) applies throughout. Opened as a draft PR; not merged, not researched, in this session — the governance PR itself must be independently reviewed under `OPS-0007` §1's capability-based standard and principal-accepted before merge, and its own merge is the sole event that makes the later research implementation PR authorized to begin.
- **July 2026** — **OPS-0009 accepted: Lean Delivery and Review Lifecycle v1** (`governance/decisions/OPS-0009-lean-delivery-review-lifecycle-v1.md`, merged via PR #182, merge commit `8768bfd`). A cross-cutting process decision — lane-based lifecycle (governance authorization / research content / mechanical sync / bounded correction) scaling preflight and review weight to change type, cross-referencing rather than duplicating `OPS-0007`/`OPS-0008`, plus two reusable templates (`governance/templates/lean_task_packet.md`, `governance/templates/lean_final_report.md`). Creates no investment, allocation, tier, target, margin, or trading authority. **Accepted and merged — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current status.**
- **July 2026** — **PR #181 CVX retrospective review retained; CVX Company Intelligence now PROVISIONAL** (`governance/audits/PR181_CVX_RETROSPECTIVE_INDEPENDENT_REVIEW_20260728.md`). A separate, independent Claude Code session that did not author PR #181 retrospectively reviewed the merged CVX implementation (`PI-0031`, merge commit `173fef53e73f0e9df86df4a81c38464167d2bbf4`, reviewed head `a79d237cc53f7b623814e3177dfc124d36c6377a`) and returned verdict **PR #181 CVX EXACT-HEAD RETROSPECTIVELY APPROVED** — no BLOCKER or MATERIAL findings, three provenance/retention NOTEs only. The principal explicitly accepted that retrospective verdict. Per `OPS-0007` §3, CVX Company Intelligence now satisfies all five PROVISIONAL elements and is **PROVISIONAL**. This entry is a narrow `OPS-0009` Lane M factual-retention/synchronization unit only: it authorizes no CVX content edit, no new research, no `PI-0032`/`PI-0033`, no tenth Milestone 3 batch, no Milestone 4, and no tier/target/holdings/cluster/cap/allocation/margin/allocator change — Milestone 3 remains incomplete (WDC and the non-deferred T2 gaps named in `PI-0031` remain open).
- **July 2026** — **PI-0032 accepted: remaining governed-holding research units plus bounded Sandisk candidate addition** (`governance/decisions/PI-0032-ws0005-milestone3-remaining-governed-holdings-and-sandisk-candidate.md`, merged via PR #185, merge commit `25af2afb`). Authorizes six independent governed-holding Company Intelligence research units — WDC, CEG, BRK.B, WMT, MLM, AAPL — closing every remaining Milestone 3 governed-holding gap `PI-0031` §K named, each treated as its own independent unit rather than a fabricated shared-mechanism batch (no genuine common economic driver exists among a utility, an insurance/holding conglomerate, a retailer, a materials producer, and a consumer-tech company). WDC's research is structurally paired with one bounded Sandisk candidate comparison — the only genuine structural relationship in this filing, reflecting WDC's own February 2025 separation of the Sandisk flash business. **Sandisk is explicitly and repeatedly classified as candidate research only**: outside the governed 62-company coverage denominator, not a Milestone 3 completion prerequisite, not a holding, not assigned any tier/target/weight/capital priority, and not authorized for purchase under any circumstance — confirmed absent from `targets.yaml`, `holdings.yaml`, and `intelligence/companies/` this session; its identity (Sandisk Corporation, ticker SNDK, Nasdaq) was verified only via corroborating secondary sources after this session's own SEC/Nasdaq primary-source fetches were blocked by network policy, disclosed as attempted-but-not-directly-inspected. This filing authorizes no research itself, no Company Intelligence record, no comparison artifact, no freshness row, and no tier/target/cluster/cap/holdings/margin/allocator change — evidence development only, gated on a later, separate implementation PR's own independent review, correction if needed, principal acceptance, merge, and post-merge verification. Filed alongside `PI-0033` in the same PR, as its own separate file per the `OPS-0008`/`PI-0027` two-decisions-one-PR precedent. `OPS-0009` Lane G (governance authorization) throughout — full weight, no reduction. **Accepted and merged — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current implementation status.** Milestone 3 remains in progress; completing this authorization does not itself complete Milestone 3 or authorize Milestone 4.
- **July 2026** — **PI-0033 accepted: residual Company Intelligence dispositions for the remaining uncovered roster** (`governance/decisions/PI-0033-ws0005-milestone3-residual-deferrals.md`, merged via PR #185, merge commit `25af2afb`). Records fourteen individually-reasoned new dispositions — CAT, GNRC, NFLX, SHOP, UBER, HOOD, RTX, DELL, PLTR, SPCX, RKLB, TSLA, BABA, and UNH — each with its own company-specific materiality assessment, deferral reason, and reopening trigger (no generic trigger substituted for company-specific reasoning anywhere), and restates, without editing or reopening, the three already-accepted deferrals (DHR and SYK per `PI-0014`; EQIX per `PI-0027`) — **seventeen names total represented; UNH is one of the fourteen newly dispositioned names, not a fifteenth name added to them.** BABA and UNH receive materially longer, particular-care treatment: BABA as the roster's only foreign private issuer, carrying VIE-structure/PCAOB-audit-history/PRC-geopolitical evidence-regime questions this repository has never addressed, with its reopening trigger naming a material change to its audit-inspection or delisting-risk posture specifically; UNH's disposition formalizes the payer/managed-care mechanism rationale `PI-0029` already recorded (structurally distinct from `life_sciences_tools_medtech`'s device/tools/diagnostics economics and from `PI-0029`'s own branded-franchise/pipeline economics) and adds the explicit reopening trigger that entry never stated. Authorizes no research of any kind — a disposition here is a recorded reason research does not proceed now, not a research authorization. Filed alongside `PI-0032` in the same PR, as its own separate file. `OPS-0009` Lane G throughout. **Accepted and merged — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current details.** Milestone 3 remains in progress; this filing materially advances `PI-0031` §K criterion 4's completeness accounting but does not itself evaluate or satisfy the seven-criterion completion standard, and does not authorize Milestone 4.
- **July 2026** — **PHQ-2026-01 accepted: canonical v1.30 destination architecture and actionable-core transition architecture approved as policy** (`governance/decisions/PHQ-2026-01-canonical-architecture-and-transition-policy-approval.md`). Records the principal's explicit approval, in a separate out-of-repository "Portfolio-HQ committee" research process (standalone backtest application, not this repository's own `allocate.py`/`margin_target_study` engines — an independent track from, and not a component of, `WS-0005`'s in-repository zero-based tier review), of: canonical v1.30 (37-row destination architecture, `Portfolio_HQ_Grand_Master_Architecture_v1_30.csv`) as the long-term destination; the "actionable core" (canonical v1.30 with seven gated names' target capital held as cash) as the current transition architecture; SNPS/ICE/SPGI/WM/RKLB/TSLA/SPCX remaining individually gated (cash) until separately cleared per name; quarterly review with tolerance bands (monthly forced rebalancing rejected); no new margin; deposits/sale proceeds repay inherited debit first; an 8% effective single-issuer ceiling and a 40% AI/platform common-driver ceiling (measured at signing ≈40.03% — at, marginally over, the ceiling, recorded as measured, not rounded into compliance); manual Robinhood execution; and that Intelligence/research/backtests never automatically change targets or policy. **Architecture policy only — this decision does not touch `targets.yaml` or `holdings.yaml`, does not synchronize current holdings (some Robinhood orders were entered after the frozen v1.31 snapshot; some filled, some queued — the frozen snapshot is historical evidence only), does not activate any gated name, and authorizes no trade or order.** SPCX's disposition is `HOLD TARGET IN CASH` (gated), not sell-all; SKHY is untouched and remains unresolved. Target/allocator implementation requires a separate future design/review/implementation workstream and its own principal approval — see the companion design note `docs/PHQ-2026-01_TARGET_ALLOCATOR_IMPLEMENTATION_DESIGN_NOTE.md` (design only, no implementation authorized) and `operations/WORKSTREAMS.yaml`'s `WS-0006` (evidence-synchronization scope only, `priority: secondary`). Evidence retained verbatim at `governance/evidence/PHQ-2026-01/` (verified SHA-256 throughout; see that directory's own `README.md`). Opened as a draft PR; not merged in this session.
- **July 2026** — **OPS-0011 accepted: repository-native, read-only Portfolio-HQ dashboard capability authorized, bounded** (`governance/decisions/OPS-0011-repository-native-dashboard-capability-authorization.md`). Closes an authority gap an independent review of draft PR #199 identified: the dashboard implemented there had **no prior repository authority**. Independently re-derived this session — `WS-0003` is `status: proposed` with `authorized_scope: none` and a `prohibited_scope` barring presentation/UX/reporting implementation within itself; `OPS-0005` §3 expressly declined a generated/tracked report artifact for WS-0002 Phase Two (stdout-only) while leaving a future decision free to authorize one; `OPS-0001` rejected a generated dashboard for the register on the same terms; `OPS-0006`/WS-0005 prohibit dashboard/status-layer work; repository precedent (`PI-0002`, `PI-0011`, `AUTO-0002`/`AUTO-0003`, `OPS-0005` §3) authorizes a named module **before** implementation; and a `docs/` design note cannot self-authorize its own implementation. **Authorizes a capability class and bounded scope, never an exact diff**: one repository-native dashboard capability; one canonical generator and one supported master HTML interface; static standalone HTML from structured repository sources plus local git metadata; an optional local server bound **only** to the loopback interface `127.0.0.1` — binding to `0.0.0.0`, a LAN address, or any externally reachable interface is not authorized and must not be configurable; read-only presentation of repository truth reusing existing public APIs (no duplicated governed calculation, no second allocator); commit/branch/dirty-worktree/effective-date/input-file provenance; prominent stale/missing/incomplete-state warnings; **disclosure or abstention when current allocation truth is unavailable**; historical HTML as archive evidence only, never operational input; reproducible, non-authoritative generated output (never committed); focused tests for safety, provenance, stale-state behavior, accessibility, offline operation, and historical-HTML isolation — delivered through **exactly one** bounded implementation PR with an enumerated file list. **Prohibits**: any brokerage connection, Robinhood/Alpaca execution, order creation/submission or order path; mutation of `holdings.yaml`/`targets.yaml`/gates/decisions/workstreams from the UI; browser-side portfolio authority; a second allocator; duplicated governed calculations; external analytics or any required CDN/network dependency; treating generated HTML as repository authority; parsing historical HTML as current state; fabricating a missing measurement; silently hiding stale or incomplete truth; automatically changing investment policy; and **merging PR #199 solely because this authorization is filed**. **`OPS-0005` is not superseded, narrowed, or reinterpreted** (its stdout-only Phase Two grant and Fable Audit Gate #2 stand exactly as filed); **`WS-0003` is not activated, edited, or rescoped** — its broader daily allocation-check UX planning intent stays deferred and unauthorized, and a new `WS-0007` register entry (`status: proposed`, `priority: secondary` — WS-0005 keeps the sole primary slot) hosts the dashboard instead, filed at `proposed` because `OPS-0001` forbids a session from self-authorizing a workstream while its governing decision is unmerged. **Explicitly does not**: approve PR #199's exact code; resolve, evaluate, or restate the four implementation findings reported against it (not seen or verified by the filing session — PR #199 carries zero GitHub reviews and no retained `governance/audits/` artifact); manufacture any review provenance for this decision; authorize `PHQ-2026-02` implementation, any holdings reconciliation, any trade or order, any gated-name activation, or any target/tier/cluster/cap/allocator/margin/order-behavior change. PR #199 remains open, draft, unmerged, untouched (head `4f9f6d0`), gated on its own independent exact-head review under `OPS-0007` §1, bounded correction, exact-head re-review, explicit principal acceptance, and immediate post-merge verification (`OPS-0009` §9, Lane G throughout). Opened as a draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **July 2026** — **PHQ-2026-02 accepted: holdings reconciled from post-execution evidence; canonical v1.30 destination architecture, actionable gates, and 8%/40% no-add controls implemented** (`governance/decisions/PHQ-2026-02-holdings-reconciliation-and-actionable-allocation-policy.md`). Resolves `PHQ-2026-01`'s deferred implementation questions (design note §10) per explicit principal approval. **Holdings**: `holdings.yaml` reconciled from the verified v1.35 evidence package (SHA-256 `fb8eb811...4cddd889e`) — the later screenshot is the complete post-transition equity/fund list (24 total, SPCX included within that count) and the earlier the complete crypto list (7 entries), confirmed both by internal arithmetic cross-checks and by explicit principal confirmation before any file changed; the 41 previously-tracked tickers absent from the evidence were removed (not zeroed), matching the existing zero-position convention. Verified post-execution cash `$2,579.84` is retained as evidence (`governance/evidence/PHQ-2026-02/`) and supplied to the allocator through its existing `--cash` runtime input — `holdings.yaml` has no persisted `cash` field in its current schema. Margin debt `$0.00`. SPCX (0.502727 sh) held, gated no-add. SKHY (0.278473 sh) held, unresolved — no canonical target, no gate, no buy/trim/exit instruction. **Targets**: `targets.yaml` migrated from the prior five-tier structure to canonical v1.30's flat 37-row `destination:` list (weights transcribed verbatim from the retained `PHQ-2026-01` evidence, sums to exactly 100%). **Gates**: new `gates.yaml`, separate from `targets.yaml` per the principal's explicit approval text — the seven gated names (SNPS, ICE, SPGI, WM, RKLB, TSLA, SPCX) transcribed verbatim from the retained gated-name-disposition evidence; a gated name's destination weight is never bought and never redistributed to any other name (regression-tested). **8%/40% no-add controls**: new `issuer_lookthrough.yaml` (point-in-time ETF look-through constituent weights, transcribed verbatim from retained evidence) backs a mechanical clip-or-block in `allocate.py`'s `plan()` — never a trim/sell rule. The retained `40.0284%` AI/platform common-driver measurement is recorded exactly as measured, labeled above-ceiling, alongside a live current calculation from reconciled holdings. **Retired by this migration** (see the Allocation-logic and cluster-cap notes above): the T1/T2/ETF/band/spec tier structure, the band/spec RSI-gated opportunistic trim, and the T1/T2 1.5x concentration-ceiling trim rule — none had a canonical-architecture equivalent, and none was replaced with an invented substitute. **Test suite** (bounded author correction, same PR, same day): the three pytest files GitHub CI found actually failing after the migration (`test_allocate_integration.py`, `test_margin.py`, `test_plan_gates.py`, 51 failures) were migrated from the retired tiered/cluster/crypto-sleeve/T1T2 schema to the canonical `destination:` schema — still-governed behavior (gap ordering, trend/earnings gates, cluster-cap clip/trim) migrated in place, tests of retired policy (aggregate crypto sleeve, T1/T2 proximity, T1/T2 ceiling trim, band/spec RSI trim) replaced with explicit tests proving that policy no longer fires. Full pytest suite passes with zero failures at this PR's corrected head. The standalone backtest scripts (`backtest_*.py`) are not part of the pytest suite or CI and remain out of scope — one-time closed historical studies, not routinely re-run. No live Alpaca credentials were available in the implementing session, so the authoritative allocation check could not be run against live current prices — verified instead via focused tests against synthetic prices and the full suite. No trade or order, no margin change, no gated-name activation. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **July 2026** — **PHQ-2026-03 accepted: SKHY and SPCX zero-based disposition — no canonical target, manual exit authorized** (`governance/decisions/PHQ-2026-03-skhy-spcx-zero-based-disposition-manual-exit-authorization.md`). Records the principal's explicit, verbatim-quoted policy approval: SKHY and SPCX receive no canonical `targets.yaml` target and are authorized for manual exit by the principal; sale proceeds remain cash pending a live, credentialed allocation check; SPCX remains a research candidate only (its existing `gates.yaml` `hold_no_add` gate stays fully binding); SKHY remains a watchlist name only; no automated order, margin use, or unrelated target change is authorized. **Policy only — no execution has occurred and none is claimed.** Both names remain held exactly as currently recorded (SKHY 0.278473 sh, SPCX 0.502727 sh); `holdings.yaml`, `targets.yaml`, and `gates.yaml` are unchanged by this decision. A separate future factual-synchronization decision and PR, gated on principal-supplied verified post-trade evidence (execution date, final quantities, transaction confirmation, resulting cash, margin-not-used confirmation), is required before any repository mutation reflecting an actual exit. `operations/WORKSTREAMS.yaml`'s `WS-0009` tracks that pending future unit. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **July 2026** — **PHQ-2026-04 accepted: SKHY and SPCX post-execution factual synchronization** (`governance/decisions/PHQ-2026-04-skhy-spcx-post-execution-factual-synchronization.md`). The `PHQ-2026-03`-required factual-sync unit: the principal manually executed both exits in Robinhood and supplied post-trade screenshots directly in-session (reviewed, facts extracted and reported, **not retained in the repository** — a deliberate narrower evidence-retention choice than `PHQ-2026-02`'s retained v1.35 package). Both orders Filled/Sell/Market, full fill (Quantity == Qty filled): SKHY 0.278473 sh @ $147.02 avg ($40.94 notional, submitted 15:11:27); SPCX 0.502727 sh @ $107.94 avg ($54.27 notional, submitted 15:10:57); both confirmed absent from the post-trade Positions list. Post-trade cash $2,675.05, margin used $0.00. `holdings.yaml` had SKHY/SPCX removed (not zeroed, existing convention); SPCX's `targets.yaml` row (0.75%) and `gates.yaml` `hold_no_add` entry retired (position confirmed gone); no target added for SKHY; **no renormalization** — the removed 0.75% is unallocated cash, not redistributed (`destination:` now sums to 99.25%). Sale proceeds remain cash, supplied only through `allocate.py`'s existing `--cash` runtime input at a future live, credentialed run — none was run here (no Alpaca credentials present in this environment, the same disclosed limitation as `PHQ-2026-02`/`PHQ-2026-03`). SPCX remains a research candidate only; SKHY remains a watchlist name only; re-entry for either requires separate research and principal approval per `PHQ-2026-03` §7, unchanged. Classified Lane G (`OPS-0009`) since it narrows authority (retiring a gate) — full independent review and principal acceptance required before merge. **Accepted and merged via PR #205 (merge commit `f700fca0abeb321196e015550d1439dfedb9d7b0`, stacked under PR #206) — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current details.**
- **July 2026** — **PHQ-2026-05 accepted: post-buy quantity reconciliation from the v1.37 evidence package** (`governance/decisions/PHQ-2026-05-post-buy-quantity-reconciliation-v1-37.md`). The principal supplied a further "Final Confirmed Account State v1.37" package (SHA-256 independently verified against its own manifest) describing quantities after seven buy fills, effective ~16:13 ET 2026-07-31. Compared field-by-field against `holdings.yaml` as left by `PHQ-2026-04`: exactly seven changes — AMZN 0.423377→1.146551, NVDA 0.986492→1.858992, TMO 0.2038→0.325606, SPY 0.25105→1.250856 (all increased), plus three new share-tracked positions, PWR 0.116807, VEA 6.186296, VWO 0.476555 (all three already carried existing canonical `targets.yaml` destination rows at zero prior shares — no new ticker/target/cap/gate). Every other tracked equity/fund and all crypto (BTC/ETH/SOL) unchanged. **A material conflict was found and escalated before any edit**: v1.37's own `positions` list still showed SKHY (0.278473 sh) and SPCX (0.502727 sh) at their exact pre-exit quantities, contradicting `PHQ-2026-04`'s independently-evidenced 15:10–15:11 ET sale fills, and v1.37's reported cash ($845.84) does not reconcile against `PHQ-2026-04`'s post-sale cash ($2,675.05) even accounting for the seven buy fills. Principal directed: `PHQ-2026-04`'s exit facts control; v1.37's SKHY/SPCX rows and cash figure are stale for those two data points — **not restored, not reopened**. `PHQ-2026-04`'s exact commits were adopted onto this filing's branch via a clean fast-forward from the same base (no conflicting branch created, no duplicated re-derivation of its SKHY/SPCX work). No `targets.yaml`/`gates.yaml`/`issuer_lookthrough.yaml`/`allocate.py` change. Classified Lane M (`OPS-0009`) — mechanical/factual synchronization, no new tier/target/cluster/cap/gate/allocator authority; principal acceptance of the underlying facts is still required before merge. **Bounded correction (same day, this PR):** the principal supplied a further "Final Account Summary Correction v1.38" package (the original transfer ZIP was verified before extraction with SHA-256 `d423827f...c57a60` — recorded provenance, not a repository-recomputable aggregate, since the ZIP itself is not retained; every retained internal file's own SHA-256 was independently recomputed and matched against the manifest; 3 Robinhood screenshots retained under `governance/evidence/PHQ-2026-05/v1_38/`, matching the `PHQ-2026-02`/v1_35 screenshot-retention precedent) that **resolves** the previously-disclosed cash discrepancy exactly: post-sale cash $2,675.05 (`PHQ-2026-04`) minus the seven confirmed buy fills' $1,734.00 = $941.05, matching Robinhood's own displayed final cash of $941.05 ($0.00 difference). v1.37's $845.84 is superseded for final-account-state purposes and is no longer described as unresolved. v1.38 supersedes v1.37 only for account-summary fields (cash, buying power $7,243.93, margin used $0.00, margin available $6,302.88, maintenance requirement $1,558.09) — it does not touch position quantities and does not reopen SKHY/SPCX, which remain governed entirely by `PHQ-2026-04`. The $0.21 portfolio-value difference between v1.38's two screenshots (taken moments apart) is treated as ordinary live-market timing, not a reconciliation gap. `holdings.yaml` still has no persisted cash field — this correction updates its comment block and this decision's own text only. The authoritative allocation check was re-run (not assumed unchanged) against the corrected state. **Accepted and merged via PR #206 (merge commit `f700fca0abeb321196e015550d1439dfedb9d7b0`) — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current details.**
- **July 2026** — **PI-0035 accepted: WS-0005 Milestone 3 roster reconciliation and gated-name disposition** (`governance/decisions/PI-0035-ws0005-milestone3-roster-reconciliation-and-gated-name-disposition.md`). `PHQ-2026-02` (merged 2026-07-31, three days after `PI-0031`/`PI-0032`/`PI-0033`) retired the T1/T2/ETF/band/spec tier structure those three decisions' Milestone 3 completion accounting was written against, replacing it with `targets.yaml`'s flat 27-name canonical `destination:` equity list. `PI-0035` narrowly supersedes only `PI-0031` §K's completion criteria 1 and 2 (merged into one restated criterion, since the T1/T2 distinction no longer exists) and the roster-membership clause of criterion 3, restating them against the current canonical roster: of 27 canonical equity names, 19 already carry a Company Intelligence record; 4 are actionable-gated with no prior disposition — **SNPS, ICE, SPGI, WM**, each newly dispositioned by this decision, individually deferred to its own `gates.yaml` `next_gate` text as the reopening condition (no gate itself is changed); 4 are non-gated and already dispositioned by `PI-0033` — **GNRC, RTX, RKLB, TSLA** — reaffirmed unedited, not reopened. `caps.clusters` reconfirmed: `semis` (ASML/TSM/NVDA/AVGO/KLAC) and `power_infra` (ETN/GEV/PWR) fully covered under current membership; `oil` now carries no members. The 26 Company Intelligence records for tickers no longer in the canonical roster (AAPL, ABBV, AMAT, AMD, BRK.B, CRM, CRWD, CVX, GILD, IBM, INTC, JNJ, JPM, LRCX, MA, MLM, MRK, MRVL, MU, NOW, ORCL, SKHY, VRT, WDC, WMT, XOM) are classified retained/historical-advisory/non-current for Milestone 3 accounting — none edited, deleted, or relabeled. **Milestone 3 remains `in_progress` — this decision does not declare it complete**; criteria 5-7 of `PI-0031` §K are restated but not independently re-verified by this filing, and a future, separate completion-determination decision must evaluate all seven together. No research performed; no gate/tier/target/cluster/cap/holdings/margin/allocator change; no eleventh Milestone 3 batch or Milestone 4 work authorized. **Accepted and merged via PR #208 (squash merge commit `b91a17efb87d3a5a511aacaf6cc7ffc7b6025a4d`) — controlling authority; see the decision file and `operations/WORKSTREAMS.yaml` for current details.** Milestone 3 remains in progress; this decision does not itself complete it or authorize Milestone 4.
- **August 2026** — **OPS-0012 accepted: Dashboard 2.0 — Modern Responsive Experience Authorization** (`governance/decisions/OPS-0012-dashboard-2-modern-responsive-experience-authorization.md`). `WS-0007`'s single `OPS-0011`-authorized implementation PR (`PR #199`) merged 2026-07-31 and its completion criteria are satisfied; the principal separately directed a comprehensive visual redesign of that same read-only dashboard capability through rapid iteration, with reasonable visual liberties authorized within governance bounds. `OPS-0012` authorizes exactly one future, separate, bounded implementation PR — "Dashboard 2.0" — for a restrained, modern, dark-first, calm visual and navigational redesign (system-safe fonts only, subtle animation, reduced-motion support, responsive desktop/tablet/mobile layouts) organized into five areas (Overview, Portfolio, Intelligence, Governance, System/Provenance), built only from information the existing dashboard model already exposes — no new investment calculation, score, or ranking. Every `OPS-0011` mandatory boundary and prohibition (read-only, recommendation-only, loopback-only optional server, no brokerage/order path, no repository mutation, no second allocator, no duplicated governed calculation, no external asset/CDN/font/script) is restated in full and unweakened. Adds a new milestone to the existing `WS-0007` register entry (Phase 2, conditional on this filing's own merge) rather than creating a new workstream; `WS-0007`'s completed Phase 1 milestones (`PR #200`, `PR #199`) are unedited. The implementation lifecycle requires, beyond `OPS-0011` §8's existing gates, rendered desktop/mobile preview artifacts and explicit principal **visual** acceptance before the future implementation PR may merge. This filing authorizes no dashboard code, no investment/allocation/tier/target/margin/policy change, and no `WS-0005` Milestone 4 work — implementation does not begin in this session. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **OPS-0013 accepted: Portfolio-HQ Governance Decision Explorer Authorization** (`governance/decisions/OPS-0013-governance-decision-explorer-authorization.md`). `OPS-0011`/`OPS-0012` authorize the read-only dashboard and its visual redesign, but neither authorizes any decision drill-down — `model.py`'s `DecisionRow` carries only `decision_id`/`date`/`status`/`category`/`file`, and `render.py`'s Governance section is a flat 25-row index table with no per-decision detail view. `OPS-0013` authorizes exactly one future, separate, bounded implementation PR to add a **Governance Decision Explorer** inside the same single generated dashboard HTML: click a decision ID, open an in-document detail view with the decision's safely rendered Markdown content, metadata, related-decision cross-links, authority/lifecycle labeling, and a return path (breadcrumb/back link/hash history) — hash-based routes (`#/decision/OPS-0011`), no client-side framework/database/network fetch/second document. Every committed decision `.md` file is treated as **untrusted display input**: HTML escaping or a tightly constrained safe renderer, no raw HTML/script execution, no event-handler attributes, no `javascript:` links, no `iframe`, no remote resources. **Supersession is never inferred from dates or IDs** — this repository's own narrow-supersession convention (`OPS-0007` §2, `OPS-0009` Rationale) leaves a narrowed decision's `status` field unchanged, so the explorer displays literal content and related-decision links without asserting a structured relationship the text doesn't state. Four-level progressive disclosure (index row → concise detail → full rendered body → original source/evidence links); local substring search/filter only, no semantic search or AI summaries. Limited to the Governance Decision Explorer for this first slice — Company Intelligence, Theme, backtest, workstream-detail, and evidence-package explorers are named as separate, unauthorized future phases. Restates the `research/margin_target_study/data/untouched_sealed/` exclusion and every `OPS-0011` mandatory boundary (read-only, recommendation-only, loopback-only, GET-only, no brokerage/order path, no repository mutation, no second allocator, one generator, one master file), unweakened. Adds one new milestone to the existing `WS-0007` register entry (no existing field/milestone edited). **Disclosed explicitly**: this filing found no retained "Repository Explorer and Drill-Down Architecture Audit" artifact anywhere in the repository despite the task describing one as completed — the capability grant is grounded in this decision's own stated Rationale, not on that unverified audit. **Filed and reviewed independently of PR #212** (the in-flight `OPS-0012` Dashboard 2.0 visual-redesign implementation) — this session did not touch PR #212, its branch, or any dashboard production code. This filing authorizes no dashboard code, no investment/allocation/tier/target/margin/policy change. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **LADDER-0001 accepted: canonical buy-ladder backtest research charter, new decision family** (`governance/decisions/LADDER-0001-canonical-buy-ladder-backtest-authorization.md`). A read-only audit found `reports/rung_backtest.md` (2026-07-12) tested the current ATR-based buy-ladder methodology (`levels.py`) only against the retired 65-ticker T1/T2/ETF/band/spec tier universe — `backtest_rungs.py`'s `roster_tickers()` reads `targets.yaml["tiers"]`, a key `PHQ-2026-02` removed, confirming it cannot run against the current canonical roster and never has. `LADDER-0001` is a new, first-of-its-kind decision prefix (`PHQ-####` was considered and rejected — that series names the separate out-of-repository Portfolio-HQ committee process specifically, not an in-repository backtest; `MARGIN-####` was considered and rejected — domain-specific to margin/target-sizing research), structurally modeled on `MARGIN-0005`'s bounded/pre-registered/hash-pinned research-charter shape, category `research_charter`. Authorizes exactly the protocol frozen in `research/buy_ladder_backtest/PROTOCOL_V1.md` (SHA-256 `4568fbfcff21b353f61e8f60cf34f3c0834d4717846bba65ca36e8348831ea52`, pinned in the decision's §3 — corrected same-PR, pre-merge, after an independent review found the protocol's descriptive universe totals arithmetically wrong (31 total named rows, 25 simulated — not 30/27) and Arm B's fixed pullback percentages missing a NUM-0001 classification (now recorded as a §2 **research assumption**, not empirically calibrated, not an evidence-bounded selection); neither correction touched the frozen universe, arms, threshold, or adoption rules): three arms — (A) current production ATR ladder (50SMA − {1,2,3}×ATR14, L3 floored at 200SMA), (B) a fixed −5%/−10%/−15% pullback ladder off 50SMA, (C) immediate/scheduled deployment — run against the current canonical **non-gated** equity/fund/GLD roster (21 non-gated equities + SPY/VEA/VWO + GLD, reported as separate segments, never pooled), 2021-06-01 through the last complete session at data-freeze, price-return only, against a pre-committed 1.0pp TWR / 1.0pp MaxDD materiality threshold matching every other closed backtest in this log. SNPS, ICE, SPGI, WM, RKLB, and TSLA (all six currently gated per `gates.yaml`) are excluded from the simulated universe entirely — no counterfactual arm, no implied buy eligibility. BTC/ETH/SOL are excluded from this study's scope entirely (protocol §19) — no equity finding may be extrapolated to crypto. The previously-rejected support/resistance pivot arm (`rung_backtest.md`'s own Arm B, already REJECTED) was explicitly considered and declined for reopening — no new hypothesis or evidence regime, and TradingView screenshots are not a permitted study input under this charter (protocol §8). This filing itself performs no data acquisition, writes no simulation code, and runs no backtest — it authorizes a later, separate implementation PR (limited to `research/buy_ladder_backtest/**` plus one output report, zero import coupling with `allocate.py`/`levels.py`) under its own full `OPS-0007` §1 review cycle, and establishes `WS-0010` (`status: proposed`, `priority: secondary`, `WS-0005` unaffected as sole `priority: primary`). No result under this charter, however strong, automatically changes `levels.py` or any production file — adoption requires its own separate future governance decision, exactly `MARGIN-0005` §13's discipline. This filing does not touch `allocate.py`, `levels.py`, `targets.yaml`, `holdings.yaml`, `gates.yaml`, `issuer_lookthrough.yaml`, Intelligence, dashboard code, the Constitution, or PR `#212`/`#213`. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **PHQ-2026-06 accepted: cash-only factual synchronization for a $100 deposit** (`governance/decisions/PHQ-2026-06-cash-synchronization-100-deposit.md`). Records a new, independently-hashed and retained Robinhood screenshot's displayed cash of **$1,041.23**, following a reported $100 deposit. Reconciles exactly from an earlier connected-account snapshot cash of $941.23 ($941.23 + $100.00 = $1,041.23), **not** from this repository's own `PHQ-2026-05`-resolved $941.05 — a $0.18 difference between those two prior figures is disclosed as unresolved, not evidence of a trade, and not explained. No `shares:`, `crypto_shares:`, or `margin:` value in `holdings.yaml` is changed — the underlying screenshot shows only category-level Equities/Crypto market values and account-level figures, not a per-ticker list, so no full holdings reconciliation is attempted or claimed. Buying power ($7,444.11) and margin available ($6,403.06) are recorded as context only, explicitly not cash and not deployment authority; margin used is confirmed $0.00. Notable evidence-handling episode, disclosed in full in the decision record: this session was first given figures ($6,315.86/$315.73 total/crypto, $7,441.11 buying power) that did not match any file it could actually open; then a claimed upload path (`/mnt/data/image(256).png`) that does not exist in this execution container; the session declined both rather than fabricate a hash, and instead extracted the actual attached image bytes directly from its own conversation transcript, independently hashed and re-verified them, and used only that retained capture's real figures. `operations/WORKSTREAMS.yaml`'s `WS-0009` entry has its one stale $941.05 reference updated to point to this decision — no other field of that entry, and no unrelated workstream, is touched. No `allocate.py` run of any kind occurred; no buy, trim, or margin recommendation is produced or authorized. Per `OPS-0009`, classified **Lane M**. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **CHART-0001 proposed (not accepted): chart-evidence pilot governance proposal** (`governance/decisions/CHART-0001-chart-evidence-pilot-governance-proposal.md`). The principal authorized *preparation* of a narrowly bounded Lane G proposal for one advisory chart-evidence pilot — not acceptance of its content. `CHART-0001` is a new decision-domain prefix (no `CHART-####` series existed; genuinely distinct from `PI-####`'s frozen Company Intelligence schema, which it does not touch or extend, and from `LADDER-0001`'s numerical backtest charter, whose own chart-pattern/screenshot exclusion is unaffected) and the first filing in this governance layer to carry `status: Proposed` rather than `Accepted`, since the principal's own authorization stops short of approving its substance. Defines, for a possible future pilot only: an advisory Chart Evidence Record type (logical schema only, no code) separating visible fact from observation/inference/uncertainty, with no numeric score or ranking; a storage model reusing the existing `governance/evidence/<decision-id>/` screenshot-plus-manifest pattern rather than a new subsystem; a privacy standard **stricter** than `PHQ-2026-06`'s own retained account-summary screenshot (balances/account numbers/brokerage identifiers barred outright, not merely disclosed), with one named exception — the principal confirmed that a visible platform username, approved for the specific retained image and recorded only in that image's own pilot package manifest rather than repeated in general governance prose, needs no redaction, a narrow fact that does not waive any other privacy requirement; and one bounded one-asset/one-screenshot pilot (default: NVDA daily, which already carries a current `intelligence/companies/NVDA.yaml` record, `conviction.rating: High`, for a fundamental cross-check) — itself unauthorized until CHART-0001 is independently reviewed and separately accepted, and even then gated behind its own future implementation PR. Explicitly authorizes no bulk ingestion, no recurring/automated capture, no dashboard integration, no scoring/ranking/trading signal, no tier/target/margin/allocator/holdings change, and no brokerage action. This governance PR itself contains no chart screenshot, no extracted chart fact, and no implementation code. Establishes `WS-0011` (`status: proposed`, `priority: secondary`; `WS-0005` unaffected as sole `priority: primary`). Draft PR; not merged, not independently reviewed, **and not yet principal-accepted even as to content** — effective only on a future merge that itself requires that separate acceptance step first.
- **August 2026** — **CHART-0001 accepted in principle; §8 pilot-preparation authorization activated (implementation not begun)** (dated note appended to `governance/decisions/CHART-0001-chart-evidence-pilot-governance-proposal.md`). Records the principal's explicit acceptance — distinct from, and later than, both the original preparation authorization and PR #218's merge (`9b85ee163ae978d2d8f4aa68c9b9b23dc66892cd`, independently confirmed live and already carrying an independent delta review verdict of "DELTA APPROVED — APPROVED FOR PRINCIPAL ACCEPTANCE" at head `4f7c1f60e46c6b4f49a65dbb86be245250202b9c`, zero surviving MATERIAL/MINOR findings). Frontmatter `status` flipped `Proposed` → `Accepted` (mirroring `PI-0034`'s own status-transition precedent — a dated note appended, no edit to §§1-14/Rationale/Alternatives/Consequences). Effect, bounded to exactly what §§1-13 already, conditionally, defined: the advisory record schema, storage model, privacy standard (including its named username exception), provenance/freshness rules, and — the operative change — §8's bounded one-asset/one-screenshot pilot description now authorizes **preparation only** of a future, separate implementation PR (default asset unchanged: NVDA, daily). That future PR is unweakened by this note: must stay draft, receive independent exact-head review under `OPS-0007` §1 (including disclosure of author/reviewer/session/model overlap), any required bounded correction and re-review, explicit principal approval before merge, and post-merge verification per `OPS-0009` §4(a) once merged. Restates, as cross-reference only (no new authority, nothing loosened): the future pilot may test ingestion of one image, provenance, deterministic metadata, structured visual observation separated from interpretation, uncertainty/unreadability disclosure, report schema/formatting, and abstention when evidence is unclear — and may not create a technical signal, score or rank a security, recommend a transaction, change any tier/target/holdings/allocation/margin, couple to allocator output, perform bulk ingestion, or access/act through a brokerage. This note does not open the §8 implementation branch or PR, does not commit any chart image or chart-derived fact, and touches no file beyond the same four this repository's convention already bounded CHART-0001 to (the decision file, `governance/decisions.yaml`, `operations/WORKSTREAMS.yaml`'s `WS-0011` entry, one `CLAUDE.md` pointer). `WS-0011` stays `status: proposed`/`priority: secondary` (matching `WS-0010`/`LADDER-0001`'s identical accepted-but-unimplemented shape) with its `authorized_scope`/`blocker`/`next_action` updated to reflect this acceptance-recording PR's own pending independent review and merge. Draft PR; not merged, not independently reviewed in this session at the time this bullet was first written — this note describes what acceptance would mean, and does not itself make it effective. **Bounded correction (same day, this PR, review 4835983890):** an independent exact-head review of PR #219 found the PR's first commit had set `status: Accepted` in the decision file and `governance/decisions.yaml` prematurely — before any independent review and without a separately retained, PR-specific `Principal acceptance:` comment distinct from the review verdict and the merge action — contradicting the note's own closing paragraph and inconsistent with `WS-0011`'s already-contingent `blocker`/`completion_criteria` language in the same diff. Resolved by correction commit `0db5bc80cd75ce08bca3b160c2e8a747e07a9bfc`, reverting `status` to `Proposed` in both files (the acceptance-note prose itself was left unedited, per the review's own finding that it is "otherwise sound") and appending a bounded-correction section to the decision file. A separately retained `Principal acceptance:` PR comment (`issuecomment-5153866676`, 2026-08-01) naming that exact head was then posted, and `status: Accepted` was re-set in this further, later, distinct commit — mirroring `PI-0034`'s actual (not merely labeled) commit sequence. This bullet, `WS-0011`, `governance/decisions.yaml`, and the decision file's frontmatter still take effect, per §14, only when PR #219 is independently re-reviewed at this final head and merged — neither has occurred yet.
- **August 2026** — **CHART-0002 proposed (not accepted): bounded multi-chart evidence framework governance proposal** (`governance/decisions/CHART-0002-bounded-multi-chart-evidence-framework-proposal.md`). The principal authorized *preparation* only of a Lane G proposal scaling CHART-0001's closed one-image pilot (`WS-0011`, `status: complete`, untouched by this filing) into a governed multi-chart framework — not acceptance of its content, and `status: Proposed`, matching CHART-0001's own original filing convention. Defines a two-stage advisory-record architecture (Stage 1 image-level Chart Evidence Records, reusing CHART-0001 §3's fact/observation/inference/uncertainty schema unmodified; Stage 2 ticker-level cross-timeframe synthesis records that only reference Stage 1 records and must disclose agreement/conflict/uncertainty/missing-evidence rather than silently resolving disagreement), CHART-0001's privacy/provenance/freshness standards reused by reference, and a pixel-retention (not reference-only) storage model reusing the existing `governance/evidence/<decision-id>/` pattern under a new `governance/evidence/CHART-0002/` sibling directory. Filed as a **new workstream, `WS-0012`** (`status: proposed`), not a `WS-0011` phase — `WS-0011`'s own closure fields (`status: complete`, `blocker: None`, `next_action` disclaiming any authorized scaling) foreclose reopening it, directly precedented instead by `WS-0010` (`LADDER-0001`)'s own new-workstream-for-a-new-charter pattern. **Deterministic first batch, computed live this session against `targets.yaml`, `intelligence/companies/`, and `gates.yaml` (not assumed from any prior report): alphabetical order among tickers carrying both a canonical destination-target entry and an existing Company Intelligence record — `AMZN, ASML, AVGO, CEG, COST`** (first five of a 19-name eligible set), all independently confirmed to have complete, unflagged chart coverage in the read-only external `~/Projects/Chart-Automation` governed library (220 PNGs, 55 tickers, 4 timeframes, verified this session). **Timeframe scope: daily (`1D`) only, five images, Stage 1 only** — a four-timeframe/20-image first batch was evaluated and rejected as conflating two new architectural axes (multi-ticker, multi-timeframe/Stage-2) in one implementation PR; Stage 2 cross-timeframe synthesis is architecturally defined but explicitly deferred to a future, separately authorized batch. **This filing performed no chart analysis and retained no image** — every inventory fact came from Chart-Automation's own existing filesystem/manifest, read-only. Authorizes no chart evidence, no Chart Evidence Record, no Stage 2 record, no processing of the other 14 eligible tickers or any gap-category ticker, no dashboard/Intelligence/allocator/margin/tier/target/holdings/brokerage change, and no `LADDER-0001` use — accepting this proposal would authorize only its own separate, future, five-image implementation PR (its own independent review, principal acceptance, merge, and post-merge verification required), never the remaining library. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **CHART-0002 scale-and-throughput amendment proposed (not accepted)** (`governance/decisions/CHART-0002-bounded-multi-chart-evidence-framework-proposal.md`, appended §§A-Q). CHART-0002's original governance PR (#222) merged the five-image proposal to `main` at `status: Proposed`; a subsequent acceptance-recording PR (#223) was closed **unmerged** before any independent review, readiness marking, or principal acceptance (superseded per PR #223 issue comment `5158074244`, retained) — the earlier PR #222 principal acceptance of the five-image content does **not** carry forward to this amendment. The principal clarified the original five-image ceiling reflected an obsolete web-upload constraint, not a desired limit, now that Claude Code can inspect the governed chart library directly through Terminal, and authorized this bounded amendment while `CHART-0002` stays `Proposed`. **Read-only capacity testing performed live this session, not assumed**: an all-220-image mechanical preflight (hash/decode/filename/manifest reconciliation) passed 220/220 in 2.18 seconds with zero failures, confirming one deterministic mechanical pass over the full governed library is safe and cheap; a 12-image deterministic privacy-process dry run (first/middle/last ticker alphabetically × all 4 timeframes) found a consistent watermark pattern and zero brokerage-sensitive content, with **no image privacy-approved by the dry run**. **Two-layer architecture recommended**: Layer A (mechanical preflight) may run against all 220 images, produces no retained artifact; Layer B (interpretive Stage 1) gets a general **25-image single-timeframe ceiling** per implementation PR (evaluated against 5/20/25/55 — 55 rejected as unmatched to the current eligible universe and unsupported by evidence; 20 rejected for lacking near-term headroom); Layer C (Stage 2) remains entirely unauthorized. **First-batch recommendation, if later accepted: the full current 19-name eligible universe** under CHART-0002 §13's *unchanged* selection rule (`AMZN, ASML, AVGO, CEG, COST, ETN, GEV, GOOGL, ISRG, KLAC, LLY, META, MSFT, NVDA, PANW, PWR, TMO, TSM, V`, reconfirmed live with zero drift) — one governance/implementation cycle instead of four, with a review-shard architecture (5-ticker shards, one final exact-head integration review) preserving per-image attributable review. **Storage**: primary model unchanged (repository-native, LFS-free evidence packages, matching CHART-0001) — Git LFS confirmed **not installed** in this environment and not adopted; fallback is GitHub release assets with in-repo manifests/hashes, reconsidered only past a disclosed ~250MB repository-size stopping condition; no second repository. This amendment's own scope is three files (this decision file, `WS-0012`, this pointer) — `governance/decisions.yaml` and every test file are unaffected, since no new decision entry is added. **Performed no chart interpretation, retained no image, created no Stage 1/Stage 2 record, and began no implementation.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — `CHART-0002` remains `Proposed`; effective only on this amendment's own future merge, and even then only as governing architecture for a still-separately-gated future implementation PR.
- **August 2026** — **CHART-0002 accepted (as amended); acceptance-recording PR pending its own review and merge — 19-chart implementation batch not yet authorized** (dated note appended to `governance/decisions/CHART-0002-bounded-multi-chart-evidence-framework-proposal.md`). The scale-and-throughput amendment's own governance PR (#224) was independently reviewed (review `4838752317`, **APPROVED FOR PRINCIPAL ACCEPTANCE**, zero BLOCKING/MAJOR/MINOR findings), explicitly principal-accepted at its exact head `ab9fb6b49b55f8cf575c2e8d4267e8e15bce52d3` (retained comment `issuecomment-5158527253`, bounding acceptance to exactly: all-220 Layer A mechanical-preflight capacity; the 25-image single-timeframe Layer B ceiling; the recommended 19-name first cohort; five-ticker review shards with one final integration review; repository-native LFS-free storage with its disclosed ~250MB stopping condition and fallback — explicitly **not** authorizing a live preflight, image retention, chart interpretation, Stage 1, Stage 2, processing of the cohort, automatic continuation, or any policy/trading/execution change), and merged to `main` at merge commit `3d8ad7896236148e57e9bef3dd8fc003ef55b07a`. A new, separate acceptance-recording PR then sets frontmatter `status: Proposed` → `Accepted` in the decision file and `governance/decisions.yaml`, citing that already-closed PR #224 chain. **This differs from `CHART-0001`'s own premature-status finding (review `4835983890`)**: there, no principal-acceptance comment existed anywhere for that content when status was first flipped, inside the very PR making the change; here, the accepted content (the amendment) was independently reviewed, principal-accepted, and merged entirely within a prior, separate, already-closed PR (#224) before this recording PR was even branched — the status change transcribes an already-closed acceptance rather than asserting a new one. This recording PR's own lifecycle gate is unweakened: it must remain draft, receive its own independent exact-head review, receive explicit principal mark-ready approval at its own exact final head before merge, and undergo complete post-merge verification — `CHART-0002`'s `Accepted` status is not EFFECTIVE in this repository until that merge occurs. **Even once effective, this authorizes no batch of any kind** — the 19-chart implementation batch (five-ticker shards, `1D` only, Stage 1 only, `governance/evidence/CHART-0002/<batch-slug>/`) requires its own further, separate, explicit principal authorization and implementation PR, under its own full `OPS-0007` §1 review cycle, before it may even be drafted. No chart image, chart interpretation, Chart Evidence Record, live mechanical preflight, or repository file beyond the decision file, `governance/decisions.yaml`, `WS-0012`, and this pointer is touched by this filing. Draft PR; not merged, not independently reviewed, not principal-approved at its own exact head in this session.
- **August 2026** — **CHART-0002 first implementation batch merged prematurely (PR #226), without required independent review or valid principal acceptance; corrected by a bounded post-merge correction PR** (`operations/WORKSTREAMS.yaml` `WS-0012`, no decision-file or `governance/decisions.yaml` change). Following PR #225's merge (making `CHART-0002` Accepted status EFFECTIVE), a separate implementation session received an explicit, bounded controlling principal authorization for the recommended 19-name first cohort — AMZN, ASML, AVGO, CEG, COST, ETN, GEV, GOOGL, ISRG, KLAC, LLY, META, MSFT, NVDA, PANW, PWR, TMO, TSM, V — daily (`1D`) only, four review shards (5/5/5/4), Stage 1 only. Live preflight reconciled a discrepancy the principal flagged (261 PNGs total under the external governed library's root vs. 220 expected): 220 live under the authoritative `library/governed/2026-08-01/` namespace (matching that project's own governed-copy manifest/validation, ALL CHECKS PASSED); the other 41 live entirely under a structurally separate, unrelated `~/Downloads` cleanup-quarantine tree, zero SHA-256 overlap with the 220. A Layer A mechanical preflight against the authoritative 220 passed clean (0 errors, 55/55 complete tickers, 0 duplicate hashes). All 19 images share an identical visible TradingView username watermark; since `CHART-0002` §6 does not extend `CHART-0001`'s NVDA-specific exception grant to other images, a fresh per-image approval was required — after an informal remark the principal explicitly declined to treat as a governance waiver, the principal supplied a formal, itemized, per-image written approval individually invoking the `CHART-0001` §5 / `CHART-0002` §6 exception for each of the 19 images, recorded verbatim in every package's own `MANIFEST.json`/`record.yaml`. All 19 images are retained byte-for-byte identical to their governed source (no redaction; `governed_source_sha256 == retained_sha256` for all 19). 19 packages (image + `MANIFEST.json` + `record.yaml` + `README.md`) plus one immutable cohort manifest were written under `governance/evidence/CHART-0002/`, authored from four parallel, bounded, read-only shard-review subagents' direct image inspections, independently integrated and hash-verified by the primary session. **PR #226 was merged to `main` at merge commit `bc3dc540a23aac6939ccc5bc37c73508e4a411f9` (2026-08-02T16:21:47Z), approximately eight minutes after opening, carrying zero GitHub reviews and no valid final-head principal acceptance** — the only PR comment was the per-image privacy-approval comment (`issuecomment-5159177312`), which explicitly states it "does not constitute final approval of PR #226, does not authorize readiness or merge" — **a direct violation of `CHART-0002` §20 / `OPS-0007` §1's required pre-merge gate.** A separate, later Claude Code (Sonnet 5) session with no authorship role in PR #226 retrospectively audited the merged state at the exact merge commit/tree (`issuecomment-5159313447`, retained, beginning "RETROSPECTIVE POST-MERGE AUDIT — PRE-MERGE GATE WAS NOT SATISFIED") — independently re-confirmed hash/pixel integrity, image legibility, fact/observation/inference/uncertainty separation, cohort/shard completeness, protected-path isolation, and full test results (357/357 batch, 2110/2110 suite) as clean, but found one systemic MAJOR content defect (all 19 evidence packages presented a paraphrase of the privacy-approval comment as a verbatim quotation, omitting the exact-head anchor and two full disclaimer sentences, with no source-comment ID/URL cited anywhere) and one MINOR lifecycle-desynchronization defect (this entry and `WS-0012` describing PR #226 as an unreviewed, unmerged draft after it had already merged). That audit performed no repository mutation and does not itself retroactively satisfy the missed pre-merge gate. A bounded corrective PR was then prepared, under its own separate, explicit controlling principal authorization, correcting exactly: the privacy-approval-citation defect across all 19 `MANIFEST.json`/`record.yaml` packages (adding a structured, mechanically verifiable `privacy_approval_source` citation — PR number, comment ID, canonical URL, exact implementation head, per-ticker approval scope, explicit non-final-approval disclosure — and replacing the paraphrased quoted block with the retained comment's exact verbatim text, independently re-verified byte-for-byte against the live GitHub comment); and this pointer's and `WS-0012`'s lifecycle text. Zero PNGs, zero Stage 1 facts/observations/inferences/uncertainties/thesis_relationship fields, and zero lifecycle/reviewer/merge-status fields were touched — every package's `lifecycle_status: draft`/`reviewer: null`/`principal_acceptance: null` remains accurate, per the audit's own explicit finding, since no independent review or principal acceptance of the batch's *content* has in fact occurred. **This corrective PR does not retroactively satisfy the missed pre-merge gate on PR #226** — that gate was skipped and stays skipped as permanent historical fact. **PR #227's own required lifecycle is now complete**: independently reviewed at exact head `ecdee26b8ca5a8c309db45475149f0bfea37bbcc` (review `4839328758`, verdict APPROVED FOR PRINCIPAL EXACT-HEAD APPROVAL, zero BLOCKING/MAJOR/MINOR findings), explicitly principal-approved at that exact head (issue comment `5159540001`), merged via merge commit `423c99a1e52af8e3062f6bd5f08d1bcc0ff8dc8c` (parents `bc3dc540a23aac6939ccc5bc37c73508e4a411f9` and `ecdee26b8ca5a8c309db45475149f0bfea37bbcc`; merge-commit tree confirmed byte-identical to the reviewed head's tree — zero drift at merge), and post-merge verified (issue comment `5159572306`: exact 41-file scope, all 19 PNGs and Stage 1 content confirmed byte-identical/unchanged, full suite 2421/2421 passing, post-merge CI run `30759211611` successful). No image, `MANIFEST.json`, `record.yaml`, `README.md`, cohort manifest, governance decision file, or `governance/decisions.yaml` was touched by PR #227 or this merge. No Stage 2 record, second batch, second timeframe, or portfolio-policy/tier/target/holdings/margin/allocator change of any kind is authorized or made by this filing, its corrective PR, or this synchronization — any such change requires its own separate, future, explicit principal authorization.
- **August 2026** — **PI-0036 accepted: bounded GNRC/RTX Company Intelligence wave authorized** (`governance/decisions/PI-0036-gnrc-rtx-company-intelligence-wave.md`). The principal determined GNRC and RTX are the only two canonical equity destinations simultaneously held, targeted, ungated, and missing Company Intelligence coverage — independently re-verified against live `holdings.yaml`/`targets.yaml`/`gates.yaml` (WDC already covered; SNPS/ICE/SPGI/WM/RKLB/TSLA gated) — acting on the `OPS-0014` audit's own recorded sequencing recommendation ("GNRC and RTX first, as the smallest coherent batch"). **Narrowly supersedes `PI-0033` solely for research-authorization purposes as to GNRC and RTX specifically** — neither ticker's literal `PI-0033` reopening trigger has occurred; `PI-0033`'s text is otherwise unedited and remains fully controlling for the other sixteen names it addresses. **Explicit administrative wave, not an `OPS-0008` §1 economic-mechanism batch** — GNRC (backup power/energy technology) and RTX (aerospace/defense) share no genuine business mechanism; bundled in one filing following `PI-0032`'s own six-independent-unit precedent (identical portfolio-governance status and execution efficiency only). Authorizes exactly: `intelligence/companies/GNRC.yaml`/`GNRC.md` and `RTX.yaml`/`RTX.md` (first coverage for both), one freshness-registry/checkpoint row each (`monitoring_enabled: false`, `enrollment_authority: PI-0036`), this decision file, `governance/decisions.yaml`, and an additive `WS-0005` synchronization entry. Does not authorize: any tier/target/gate/cluster/cap/holdings/margin/allocator change; any trade; any third ticker; any research on a gated or otherwise-excluded name; or Milestone 4 relationship mapping. Milestone 3 remains `in_progress`. Opened as a draft PR; not merged, not independently reviewed, not principal-accepted in this session — the governance PR itself must be independently reviewed and principal-accepted before merge.
- **August 2026** — **OPS-0015 accepted: Personal-First Unified Application Mission, Parallel-Work Protocol, and Discovered-Work Reconciliation** (`governance/decisions/OPS-0015-personal-first-unified-app-roadmap.md`). Records that Portfolio-HQ is first being built as a personal, repository-native, recommendation-only system for the principal (not constrained by App Store/multi-user/subscription/customer-support concerns), and that its future user-facing application is one visual projection of repository truth — never a second ledger, allocator, or policy store, always calling rather than duplicating `allocate.py`. Records a seven-phase future application roadmap (A: provenance/origin-sync visibility; B: unified read-only command center; C: research/evidence/governance/roadmap navigation; D: relationship and correlated-risk visualization; E: guarded advisory allocation-check launcher; F: governed `OPS-0014` factual-sync interface; G: conversational operator), each phase dependency-gated and requiring its own separate future authorization — Phase A/C already have bounded slices delivered by `OPS-0011`/`OPS-0013`, Phase E overlaps `OPS-0007` §5's existing scenario-display bridge without narrowing or widening it, Phase G is `WS-0003`'s existing unauthorized scope, not newly activated. Restates (does not edit) `OPS-0014` §D's one-active-mutation-lane rule as part of a named parallel-work protocol (one mutation lane plus up to three read-only lanes, research parallelized only after scope/schema/evidence-standard freeze, one branch/PR per authorized unit) and records a discovered-work reconciliation reporting discipline (report gap/evidence/owning-workstream/authorization/dependency/next-unit; never silently implement discovered scope). Records commercialization as a deferred, unauthorized, non-blocking future possibility only, explicitly not shaping current personal-system completion. Discloses explicitly that no app-architecture or roadmap-reconciliation audit artifact is retained anywhere in this repository — the mission/roadmap wording is recorded solely on the principal's explicit authorization, not on any inspected audit. Adds one additive, non-authority-bearing cross-reference milestone each to `WS-0002`, `WS-0003`, `WS-0007`, and `WS-0013` (`operations/WORKSTREAMS.yaml`) — no status, priority, `authorized_scope`, `prohibited_scope`, or `completion_criteria` changed on any of the four. **Authorizes no application code, no research, no relationship mapping, no chart interpretation, no holdings/allocation/margin/brokerage/order change, and no new workstream.** Also discloses, as separately discovered work not folded into this filing's scope: `WS-0007`'s and `WS-0013`'s `active_pr` fields (230 and 231 respectively) are stale post-merge references, the same pattern already disclosed for `WS-0005`'s `active_pr: 233` — all three PRs are independently confirmed merged via live GitHub state. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **PI-0037 accepted: WS-0005 Milestone 3 completion determination** (`governance/decisions/PI-0037-ws0005-milestone-3-completion-determination.md`). A bounded Lane G (`OPS-0009` §1) completion-determination unit, independently re-verifying all seven of `PI-0031` §K's completion criteria (as narrowly restated by `PI-0035` §C) against live repository/GitHub state, updated for `PI-0036`'s subsequent completion of GNRC/RTX Company Intelligence coverage. Confirms the canonical 27-name equity roster as 21 covered + 4 gated-and-`PI-0035`-dispositioned (SNPS, ICE, SPGI, WM) + 2 gated-and-`PI-0033`-dispositioned (RKLB, TSLA); confirms all 47 currently-existing Company Intelligence records — including all 21 covering a current roster name — independently PROVISIONAL under `OPS-0007` §3, per record, relying on the retained `WS0005_M3_CRITERION7_RETROSPECTIVE_LIFECYCLE_AUDIT_20260729.md`/`WS0005_M3_SIX_RECORD_FRESH_RETROSPECTIVE_REVIEW_RETENTION_20260729.md` audits plus this session's own fresh GNRC/RTX verification (PR #232/#233). **Verdict: MILESTONE 3 COMPLETE**, effective on this decision's own merge — sets the `milestone-3-intelligence-completion` gate's `status` to `complete` in `operations/WORKSTREAMS.yaml`, WS-0005's own top-level `status` remaining `in_progress` since Milestones 4-9 remain unauthorized. Discloses, without correcting (outside this unit's authorized scope): WS-0005's stale `active_branch`/`active_pr`/`last_verified_main_sha` self-reference fields; `PI-0035`'s own governance PR #208 carrying no GitHub-API-retrievable independent review (a governance-decision-level provenance gap, distinct from any Company-Intelligence-record criterion evaluated here); and that any gated name's real-world reopening-trigger event, if it has occurred, is unverified external state outside this unit's scope. **Authorizes no research, no Company Intelligence edit, no gate reopening, no tier/target/role/cluster/cap/holdings/margin/allocator change, and explicitly, repeatedly does not authorize, begin, schedule, or imply Milestone 4 or any later WS-0005 milestone.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **OPS-0016 proposed (not yet accepted): Eureka identity, mobile/cloud operating architecture, and governed decision-evidence roadmap** (`governance/decisions/OPS-0016-eureka-identity-mobile-cloud-architecture-roadmap.md`). Follows from `OPS-0015`, which named no future application and left mobile/cloud execution, screenshot-evidence boundaries, the already-accepted chart-evidence (`CHART-0001`/`CHART-0002`) and buy-ladder (`LADDER-0001`) charters' relationship to the broader roadmap, a fuller future chart package, a trim/sell-review research dimension, Portfolio Intelligence's eventual opportunity-cost function, further governance-mechanism candidates, and a connecting future sequencing order all unrecorded. Names the future unified user-facing application **Eureka**, one word — the GitHub repository stays `Mast3rkey/Portfolio-HQ`, no rename authorized. Records: an authoritative-repository, cloud-executable, mobile-operable execution architecture and four conceptual operating lanes (read; advisory allocation; evidence-sync; engineering); screenshot/evidence-ingestion boundaries (evidence not policy, no silent modification of governed rules); the chart-evidence framework's relationship to already-accepted `CHART-0001`/`CHART-0002` authority (cross-referenced, not re-authorized or duplicated) plus a future, broader chart-package design candidate (fundamentals/valuation/price-risk/portfolio-fit); a buy-ladder/trim/sell-review research program restating `LADDER-0001`'s existing charter and adding trim/sell-review candidates it does not cover; four governed visual zones (buy/hold/trim-review/exit-review) as a future presentation concept that may only ever visualize approved policy; Portfolio Intelligence's eventual next-dollar/opportunity-cost comparison function and its advisory-only limit; ten future governance-mechanism candidates (too-hard register, pre-registered falsifiers, base-rate evidence, roads-not-taken ledger, friction ledger, evidentiary provenance types, red-team requirement, doctrine-amendment cooling-off, dashboard cadence governance, conviction-versus-confidence) — none adopted, each requiring its own future evaluation; the policy/analysis/software/visualization separation restated as one explicit statement; and a seventeen-step future sequencing order converging on `OPS-0015` §G's and `WS-0013`'s existing final-allocation-readiness destination, not a second or competing sequence. Adds one additive, non-authority-bearing cross-reference milestone each to `WS-0002`, `WS-0003`, `WS-0005`, `WS-0007`, `WS-0012`, and `WS-0013` (`operations/WORKSTREAMS.yaml`) — no status, priority, `authorized_scope`, `prohibited_scope`, or `completion_criteria` changed on any of the six. Does not edit, narrow, widen, or supersede `CHART-0001`, `CHART-0002`, `LADDER-0001`, `OPS-0006`, `OPS-0007`, `OPS-0009`, `OPS-0011`, `OPS-0012`, `OPS-0013`, `OPS-0014`, or `OPS-0015` — each is cross-referenced by name only. **Authorizes no application, mobile, cloud, screenshot-ingestion, or chart code; no research or backtesting; no `WS-0005` Milestone 4 or relationship mapping; no target/tier/cap/cluster/gate/ladder/trim/sell/margin/holdings/allocator change; no brokerage integration; no order; no commercialization step; no repository rename; and no new workstream.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0001 accepted: WS-0005 Milestone 4 relationship-mapping schema, taxonomy, evidence standard, and bounded inventory authorization** (`governance/decisions/REL-0001-ws0005-milestone4-relationship-schema-taxonomy-evidence-standard-and-inventory-authorization.md`). A bounded Lane G (`OPS-0009` §1) unit, on the human principal's explicit authorization, freezing (not executing) Milestone 4 doctrine: a future pairwise relationship-record convention (`intelligence/relationships/<A>_<B>.yaml`/`.md`, filesystem-is-the-index, no graph database); a closed twelve-item primitive relationship taxonomy (`supplier_dependency`, `customer_dependency`, `manufacturing_dependency`, `technology_platform_dependency`, `capital_spending_dependency`, `regulatory_or_reimbursement_dependency`, `commodity_or_energy_dependency`, `financing_or_interest_rate_dependency`, `geographic_or_geopolitical_dependency`, `competitor`, `substitute`, `complement`) with `duplicate_economic_exposure`/`correlated_loss_mechanism`/`missing_exposure` explicitly excluded as primitives (derived conclusions only, never independently asserted as observed fact); directionality rules; a claim-level evidence/abstention standard (source, evidence classification, relationship status, confidence, materiality, `decision_served`, uncertainty, disconfirming evidence); a closed `decision_served` vocabulary (duplicate-exposure detection, thesis monitoring, stress testing, next-dollar/opportunity-cost, missing-exposure review, zero-based portfolio review); and the structural-versus-measured-price-correlation separation (no correlation study authorized here — any future one requires its own separate `MARGIN-0005`/`LADDER-0001`-style charter). **New decision prefix `REL-####`** — independently re-derived, not assumed: `PI-####` is frozen one-way, non-relational Company/Theme Intelligence (`PI-0006`); Milestone 4's subject is an inherently two-endpoint, directional/symmetric record type that does not fit that frozen shape; `LADDER-0001`/`CHART-0001` already establish, and this filing re-applies, the test for minting a genuinely new prefix, confirmed to require no schema/catalog/validator/test change (`portfolio_hq/dashboard/decisions.py`'s decision-ID pattern is fully generic). Authorizes exactly one future, separate, bounded inventory-only implementation unit — classify already-existing Company/Theme Intelligence and comparison-artifact evidence against the frozen taxonomy for the current 27-name canonical roster only (21 covered + 4 gated/`PI-0035`-dispositioned + 2 gated/`PI-0033`-dispositioned), reusing all ten existing comparison artifacts, both Theme Intelligence records, and `issuer_lookthrough.yaml` before any new research — effective only after this decision's own independent review, principal acceptance, merge, and post-merge verification. That future unit may create `relationship_validator.py`, its test file, and one retained `governance/audits/` artifact; it may not create any `intelligence/relationships/` record, conduct new external research, alter any Company/Theme Intelligence record, compute correlations, score or rank anything, touch any tier/target/holdings/cluster/cap/margin/allocator/brokerage/order, implement "Eureka" (`OPS-0016`), or begin Milestone 5 or a new workstream. Adds one additive, non-authority-bearing milestone-adjacent gate to the existing `WS-0005` register entry — no status, priority, `authorized_scope`, `prohibited_scope`, or `completion_criteria` changed; the `milestone-4-portfolio-relationship-mapping` gate's own `status: proposed` is unchanged, and Milestone 3 (`PI-0037`) is unedited and not reopened. **Authorizes no relationship content, no external relationship research, no price-correlation study, no graph or Eureka implementation, no automatic scoring/ranking, no policy change, and no target/tier/cap/cluster/gate/ladder/trim/sell/holdings/margin/allocator/brokerage/order change.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0002 proposed (not yet accepted): first WS-0005 Milestone 4 relationship-content batch — CEG_MSFT** (`governance/decisions/REL-0002-ws0005-milestone4-first-relationship-content-batch-ceg-msft.md`). On explicit principal authorization, authorizes and — in the same combined governance-and-implementation PR, per explicit packaging instruction — implements exactly one `intelligence/relationships/` record: `CEG_MSFT.yaml`/`.md`, a `customer_dependency` primitive (CEG depends on MSFT via the disclosed 20-year Microsoft PPA supporting the Crane Clean Energy Center restart), naming `thesis_monitoring`/`stress_testing` from `REL-0001` §F's closed vocabulary — both taken directly from the `REL-0001` §I inventory audit's own §9 first-candidate recommendation. Uses only evidence already identified in that audit and in `intelligence/companies/CEG.yaml`; no external research performed. **One-sided sourcing disclosed explicitly**: this session directly grepped `MSFT.yaml`/`MSFT.md` and confirmed zero independent corroboration of the PPA, so per the inventory's own stopping condition for this pair, the evidence entry carries `evidence_classification: inferred`, not `observed` — a deliberate epistemic-humility choice, not a claim CEG's own disclosure is unreliable. Neither `CEG.yaml`/`CEG.md` nor `MSFT.yaml`/`MSFT.md` was modified. No correlation computed; no `duplicate_economic_exposure`/`correlated_loss_mechanism`/`missing_exposure` conclusion asserted (excluded as primitives by `REL-0001` §C); no tier/target/holdings/cluster/cap/gate/ladder/trim/sell/margin/allocator/brokerage/order/chart-evidence/Eureka file touched. Corrects, in the same filing, two pre-existing tests whose literal assertions the new real record made false (`test_relationship_validator.py`'s directory-must-not-exist and loaders-never-touch-it checks) and two hardcoded decision-count assertions (`test_portfolio_hq_dashboard_decisions.py`, 66→67) — disclosed as necessary test-suite hygiene for the domain this batch's own content belongs to, not scope creep. Adds one additive `WS-0005` gate entry (`operations/WORKSTREAMS.yaml`) — the `milestone-4-portfolio-relationship-mapping` gate's own `status: proposed` is unchanged; one additive record is a first step, not Milestone 4 completion. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0003 proposed (not yet accepted): second WS-0005 Milestone 4 relationship-content batch — eight remaining evidence-ready pairs** (`governance/decisions/REL-0003-ws0005-milestone4-second-relationship-content-batch-remaining-eight-pairs.md`). Also records, as a Lane M factual synchronization folded into this same filing: `REL-0002` (`CEG_MSFT`, PR #240) independently re-confirmed merged (`7305dc21bf6e7f30e62d75e70b82275ef3530d09`), independently reviewed (`4856012228`, APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE), and principal-accepted at its exact head — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `CHART-0001`/`CHART-0002` two-step acceptance-recording pattern, not corrected here. On explicit principal authorization, `REL-0003` authorizes and — in the same combined governance-and-implementation PR, following `REL-0002`'s own packaging precedent — implements eight `intelligence/relationships/` records: `AVGO_GOOGL`, `AVGO_META` (`customer_dependency`, AVGO's own disclosed AI-segment customer roster naming Google/Meta among a small named group); `ETN_GNRC`, `GEV_GNRC`, `GNRC_PWR` (`complement`, GNRC's own disclosed dollar-quantified data-center-backup-power pivot, framed in GNRC's own record as complementary to `power_infra`'s switchgear/turbine/EPC roles); and `GOOGL_MSFT`, `AMZN_MSFT`, `AMZN_GOOGL` (`competitor`, the MSFT/GOOGL/AMZN public-cloud-seller overlap `BATCH5` §7 identifies as "the batch's clearest overlap," one pairwise record per pair per `REL-0001` §D). All eight pairs and their exact evidence citations were independently re-inspected this session (not merely cited from the `REL-0001` §I inventory audit's own §9 recommendation), and every counterparty's own Company Intelligence record was freshly grepped and found to carry zero independent corroboration in every case — every evidence entry therefore carries `evidence_classification: inferred`, matching `REL-0002`'s own conservative discipline; the three `complement` records also carry GNRC's own explicit materiality caveat (data-center exposure remains a minority of GNRC's total revenue; no correlation scan against `power_infra` has ever been performed). **Mechanical alphabetical-filename correction**: three of the eight pairs required correcting from the non-alphabetical reference labels supplied in the authorizing task to `REL-0001` §B's mandatory ordering (`GNRC_GEV`→`GEV_GNRC`; `GNRC_ETN`→`ETN_GNRC`; `MSFT_GOOGL`/`MSFT_AMZN`/`GOOGL_AMZN`→`GOOGL_MSFT`/`AMZN_MSFT`/`AMZN_GOOGL`) — no pair's identity or evidence changed. No pair was abstained. No `duplicate_economic_exposure`/`correlated_loss_mechanism`/`missing_exposure` conclusion asserted; no correlation computed; no Company or Theme Intelligence record modified (including `CEG_MSFT`, which needed no compatibility correction); no tier/target/holdings/cluster/cap/gate/ladder/trim/sell/margin/allocator/brokerage/order/chart-evidence/Eureka file touched. Corrects, in the same filing, one pre-existing test whose literal `record_count == 1` assertion the new records made false (`test_relationship_validator.py`) and two hardcoded decision-count assertions (`test_portfolio_hq_dashboard_decisions.py`, 67→68). Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) — the `milestone-4-portfolio-relationship-mapping` gate's own `status: proposed` is unchanged; nine total additive records (`CEG_MSFT` plus this batch's eight) are a second step, not Milestone 4 completion, and this filing creates no standing authorization for a third batch. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0004 proposed (not yet accepted): WS-0005 Milestone 4 completion standard** (`governance/decisions/REL-0004-ws0005-milestone4-completion-standard.md`). Also records, as a Lane M factual synchronization folded into this same filing: `REL-0003` (PR #241) independently re-confirmed merged (`607832d07e2849051c74f4f83cf05627fce2fc8c`), independently reviewed (`4856585060`, CHANGES REQUIRED — one MINOR finding, resolved by correction commit `fe81561`), exact-head delta-reviewed (`4856745242`, APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE), and principal-accepted at its exact head (`fe815617488c6223a472920c57cfe759095c1507`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `REL-0002`/`CHART-0001`/`CHART-0002` two-step acceptance-recording pattern, not corrected here. On explicit principal authorization, `REL-0004` defines — for the first time in this repository — the WS-0005 Milestone 4 ("Portfolio Relationship Mapping") completion standard: eight numbered, evidence-based, finite, auditable criteria (full-roster inventory-pass currency; candidate-set exhaustion; explicit per-name disposition, including that zero evidenced relationships is a valid terminal state; per-record `OPS-0007`-§3-equivalent PROVISIONAL status; freedom from unresolved MATERIAL findings; new-external-research gaps disclosed but explicitly **not required**, since `REL-0001` authorized no Milestone-4 research-wave protocol analogous to `OPS-0008`; register/validator synchronization; and a separate, later, independently reviewed completion-determination filing) plus a bounded reopening-trigger model (new canonical roster names, cleared gates, refreshed Company Intelligence evidence, or a post-completion MATERIAL finding — each a disclosed, bounded, separately authorized future filing, never an automatic reopening, explicitly disclosed as a forward design with no existing repository precedent for reopening an already-complete milestone) and an explicit non-authority restatement against target/tier/allocator policy, measured price correlation, chart evidence, Milestone 5, and "Eureka." Filed at the exact moment the `REL-0001` §I inventory audit's own §9 advisory candidate table became fully actioned (all nine recommended records filed via `REL-0002`/`REL-0003`) — the filing states explicitly that this exhaustion is a **necessary but not sufficient** completion signal, since the governing inventory's own currency, per-record review-lifecycle status, and MATERIAL-finding freedom were not freshly re-verified as part of a completion evaluation by this filing. **Makes no Milestone 4 completion determination of any kind** — the `milestone-4-portfolio-relationship-mapping` gate's own `status: proposed` is unchanged. Creates no relationship-content authorization, no external research, no correlation study, no Company/Theme Intelligence edit, and no tier/target/holdings/cluster/cap/gate/ladder/trim/sell/margin/allocator/brokerage/order/chart-evidence/Eureka change. Corrects, in the same filing, two hardcoded decision-count assertions (`test_portfolio_hq_dashboard_decisions.py`, 68→69) made false by this filing's own new `governance/decisions.yaml` row. Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) recording `REL-0003`'s post-merge state and this filing's own live work, and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0005 proposed (not yet accepted): third WS-0005 Milestone 4 relationship-content batch — four TSM-centered semiconductor pairs** (`governance/decisions/REL-0005-ws0005-milestone4-third-relationship-content-batch-semis-tsm.md`). Also records, as a Lane M factual synchronization folded into this same filing: `REL-0004` (PR #242) independently re-confirmed merged (`29761b6a12bb81e3fff7c199f5ea4de57f3d32a5`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `REL-0002`/`REL-0003`/`CHART-0001`/`CHART-0002` two-step acceptance-recording pattern, not corrected here. Filed from an isolated git worktree/branch deliberately kept separate from PR #242's own concurrent, active mutation lane throughout — this session paused all mutation work while PR #242 remained open and resumed only after independently confirming its merge via the GitHub API. On explicit principal authorization — originally three pairs, later explicitly expanded mid-session to a fourth — `REL-0005` authorizes and implements four `intelligence/relationships/` records sharing TSM as the common counterparty, drawn from the `REL-0001` §I inventory audit's own §4.1/§4.2 evidence-classification tables (not its §9 batch-recommendation table, which `REL-0002`/`REL-0003` already fully actioned): `NVDA_TSM` and `AVGO_TSM` (`manufacturing_dependency`, both fabless/hybrid designers' own foundry-dependency risk-factor language, AVGO's own dedicated TSMC-naming entry being if anything more directly on-point than NVDA's more generic language); `ASML_TSM` and `KLAC_TSM` (`capital_spending_dependency`, both equipment vendors' own disclosed foundry-customer-concentration figures naming TSMC, ASML's own ~24%-of-net-sales figure materially stronger than KLAC's own single, untracked FY2023 data point — the two records deliberately carry different `confidence` ratings, `moderate` and `low` respectively, rather than a uniform one). `AVGO_TSM` was added on the principal's own explicit, separately-conditioned authorization, independently re-verified this session against `REL-0001` §E before acceptance. Every evidence entry carries `evidence_classification: inferred` — this session directly grepped TSM's own record for each counterparty's name and found zero independent corroboration in every case. **No pair was abstained.** The inventory's own §5.1 hyperscaler-to-semis capital/customer chain (MSFT/GOOGL/AMZN/META ↔ NVDA/AVGO) was independently re-inspected and confirmed to remain an explicit, disclosed, unresolved evidence gap on both sides, per Batch5's own text — not converted into a fifth record; no fifth pair of any kind was added. **Identifier-collision correction**: this batch's three original draft records were first drafted under a working self-reference to `REL-0004`, the correct next-available identifier at that time — a concurrent session's own separately authorized "Milestone 4 completion standard" filing then claimed `REL-0004` and merged first; all six pre-existing files' internal self-references were mechanically corrected to `REL-0005` with no change to any pair's identity, evidence, or scope. No `duplicate_economic_exposure`/`correlated_loss_mechanism`/`missing_exposure` conclusion asserted; no correlation computed; no Company or Theme Intelligence record modified; no tier/target/holdings/cluster/cap/gate/ladder/trim/sell/margin/allocator/brokerage/order/chart-evidence/Eureka file touched. Corrects, in the same filing, one pre-existing test whose literal `record_count == 9` assertion the new records made false (`test_relationship_validator.py`) and two hardcoded decision-count assertions (`test_portfolio_hq_dashboard_decisions.py`, 69→70). Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) recording `REL-0004`'s post-merge state and this filing's own live work, one `evidence_refs` block, and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields. Thirteen total additive `intelligence/relationships/` records (the nine from `REL-0002`/`REL-0003` plus this batch's four) are a third step, not Milestone 4 completion, and this filing creates no standing authorization for a fourth batch or a fifth pair. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0006 proposed (not yet accepted): WS-0005 Milestone 4 completion determination — MILESTONE 4 COMPLETE** (`governance/decisions/REL-0006-ws0005-milestone4-completion-determination.md`). A bounded Lane G (`OPS-0009` §1) unit, on explicit principal authorization, freshly re-verifying all eight of `REL-0004` §C's completion criteria against live repository and GitHub state rather than inferring from any prior filing's own summary — the same discipline `PI-0037` applied to Milestone 3. Also records, as a Lane M factual synchronization folded into this same filing: `REL-0005` (PR #243) independently re-confirmed merged (`7562c49fe0a2a52db42c7f40e5609c452d78af0e`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `REL-0002`/`REL-0003`/`REL-0004`/`CHART-0001`/`CHART-0002` two-step acceptance-recording pattern, not corrected here. **Criterion-by-criterion determination**: full-roster inventory currency (zero drift confirmed via `git log` since the governing inventory's own `2026-08-04` authoring commit); the governing inventory's own §9 recommendation table fully actioned (nine records), no further §9-comparable table produced since; explicit disposition confirmed for all 21 non-gated canonical names (14 covered by a filed record, 7 — COST, ISRG, LLY, PANW, RTX, TMO, V — explicitly disclosed no-canonical-pair-evidence in the governing inventory's own §6/§7); all 13 `intelligence/relationships/` records independently re-confirmed PROVISIONAL per record (not per batch) by directly re-reading all three originating PRs' (#240, #241, #243) own independent-review, principal-acceptance, merge, and post-merge-verification evidence via the GitHub API, rather than trusting any batch's own narrative; zero unresolved BLOCKING/MAJOR findings across all three PRs, independently re-tallied; the §5.1 hyperscaler-to-semis evidence gap (MSFT/GOOGL/AMZN/META ↔ NVDA/AVGO) independently re-confirmed still unresolved on both sides via a fresh grep of each hyperscaler's own Company Intelligence record, disclosed per Criterion 6 as not required, not converted into new research; register/validator synchronization performed by this same filing. **One factual correction disclosed, not silently inherited**: the governing inventory's own §6 table listed GNRC as carrying no canonical-pair evidence — now stale, superseded by `REL-0003`'s later GNRC-anchored `complement` records (a positive drift, not a gap) — recorded in `REL-0006`'s own text only, per this repository's never-silently-rewrite convention for retained artifacts. **Two residual, disclosed, non-blocking items newly surfaced by this filing**: the governing inventory's own §4 tables record a `GEV`/`ETN`/`PWR` `capital_spending_dependency` and an `MSFT`/`AMZN` `regulatory_or_reimbursement_dependency` that were never elevated to the §9 recommendation table and so are not required by Criterion 2's own text — flagged as optional, unauthorized future work, not acted on. **Verdict: MILESTONE 4 COMPLETE**, and this filing accordingly sets the `milestone-4-portfolio-relationship-mapping` gate's `status` to `complete` in `operations/WORKSTREAMS.yaml`, effective only on this decision's own merge — `WS-0005`'s top-level `status` remains `in_progress` since Milestones 5-9 remain unauthorized. Corrects, in the same filing, two hardcoded decision-catalog-count assertions (`test_portfolio_hq_dashboard_decisions.py`, 70→71) made false by this filing's own new `governance/decisions.yaml` row. Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) recording `REL-0005`'s post-merge state and this filing's own live work, and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields. **Authorizes no relationship research, no new `intelligence/relationships/` record, no price-correlation study, no Company/Theme Intelligence edit, no gate reopening, no tier/target/role/cluster/cap/holdings/margin/allocator change, and explicitly, repeatedly does not authorize, begin, or imply Milestone 5 or any later WS-0005 milestone, or "Eureka" (`OPS-0016`).** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **TIER-0001 proposed (not yet accepted): WS-0005 Milestone 5 first bounded unit — classification-question inventory** (`governance/decisions/TIER-0001-ws0005-milestone5-classification-question-inventory.md`). A new decision prefix — independently re-verified unused this session (zero matches in `governance/decisions.yaml`, zero matches via GitHub code search) — minted following the `LADDER-0001`/`REL-0001` precedent of naming a domain-specific prefix rather than filing milestone content under `OPS-####`. The first explicit, separate principal authorization to begin `OPS-0006` §4 Milestone 5 ("zero-based classification and tier-architecture review"), matching the `PI-0023`/`REL-0001` per-unit-authorization pattern already used for Milestones 3 and 4. Combines governance authorization and delivery in one PR, per the principal's explicit packaging instruction (the `REL-0002`/`REL-0003` combined-filing precedent, not `REL-0001`'s split-into-two-PRs precedent, since no taxonomy-freeze prerequisite exists here). Delivers one retained bounded inventory artifact, `governance/audits/WS0005_M5_CLASSIFICATION_QUESTION_INVENTORY_20260804.md`, mapping nine candidate classification questions (economic/portfolio role, research conviction, capital-allocation priority, risk concentration and overlap, dependency/relationship exposure, monitoring intensity, review cadence, implementation readiness, uncertainty/evidence quality) against already-existing repository evidence only — no new company, theme, or relationship research performed. **Key findings**: `target_pct` in `targets.yaml` remains the sole allocator-visible field, by deliberate design (`PI-0003`); `intelligence/freshness_registry.yaml`'s `monitoring_enabled` and Company Intelligence `risks[].status` are both 100% uniform across all 47 records (`false` / `monitoring` respectively, independently re-confirmed live this session) and therefore decorative rather than load-bearing today; `portfolio_role_ref` still carries the pre-`PHQ-2026-02` tier vocabulary (`T1`/`T2`/`band`/`spec`) that `targets.yaml` itself no longer defines anywhere, a genuine vocabulary drift rather than a live conflict; `risks[].severity` now shows a third value (`high`, 6 of 253 entries) that had never appeared at `PI-0015`'s original 7-company writing, naturally following the corpus's growth to 47 companies; and `docs/INVESTMENT_ONTOLOGY.md` (`ONTO-0001`) already freezes a conceptual Economic-Systems → Company-Roles → Company-Quality → Capital-Priority → Tier → Target-Allocation hierarchy and a "preserved distinctions" list covering most of the nine axes, with zero applied instances found anywhere in the 47-record corpus — an existing, unused scaffold to reconcile with, not a blank slate. The artifact recommends, as advisory text only (not an authorization of any kind), that at most four axes — economic role, capital priority (already the load-bearing anchor, unchanged), risk concentration, and a merged uncertainty/evidence-quality axis — warrant a future Milestone-5 framework-design pass, and names six unresolved questions (portfolio_role_ref/ONTO-0001 reconciliation; whether conviction should ever inform target_pct through a governed rule; extending risk-concentration coverage beyond the 19 canonical names outside any cluster cap; whether review cadence should actually vary per `OPS-0006` §12's own already-accepted but unexecuted proportionality requirement; whether the 6 gated names need any classification while gated; and where a future framework's output would live — a new Intelligence schema field, a `targets.yaml` schema change, or a purely descriptive artifact) requiring their own future principal judgment. **Classifies no ticker, proposes no candidate final framework, performs no blind classification, reconciles no baseline, and computes no mechanical score.** Adds one additive `WS-0005` gate entry (`operations/WORKSTREAMS.yaml`) recording this bounded unit and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields — the `milestone-5-zero-based-classification-and-tier-architecture-review` gate's own `status: proposed` is unchanged, since this bounded unit is the milestone's first step, not its completion. **Does not authorize Milestone 6 (blind classification), Milestone 7 (baseline reconciliation), Milestone 8 (policy recommendation package), or Milestone 9 (independent review and adoption), and does not change any tier, target, cap, cluster, gate, holding, allocator, or margin behavior.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **TIER-0002 proposed (not yet accepted): WS-0005 Milestone 5 candidate classification framework design, structural only** (`governance/decisions/TIER-0002-ws0005-milestone5-candidate-classification-framework-design.md`). Also records, as a Lane M factual synchronization folded into this same filing: `TIER-0001` (PR #245) independently re-confirmed merged (merge commit `96020e55b5317aa6191733e22d2df84bea4a6574`), independently reviewed (`4859945925`, CHANGES REQUIRED — 2 MAJOR, resolved by correction commit `eed05c07`), delta-reviewed (`4860022747`, "DELTA APPROVED — APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE"), and principal-accepted at its exact head (`issuecomment-5186141437`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `CHART-0001`/`CHART-0002`/`REL-0002`-`REL-0005` two-step acceptance-recording pattern, not corrected here. On the principal's explicit authorization, `TIER-0002` designs — as text only, not an authorization, not an adoption, not applied to any ticker — a candidate classification framework using exactly the four axes `TIER-0001` retained: economic role, capital priority, risk concentration, and uncertainty/evidence quality. Full field-by-field design retained at `governance/audits/WS0005_M5_CANDIDATE_CLASSIFICATION_FRAMEWORK_DESIGN_20260805.md`. **Recommended structure**: a new, optional, non-coupled record namespace, `intelligence/classification/<TICKER>.yaml` (not created by this filing) — reusing `docs/INVESTMENT_ONTOLOGY.md`'s already-frozen, currently-unapplied vocabulary for `economic_role` (distinct from `portfolio_role_ref`'s stale tier label and from conviction); a bounded, non-numeric, `PI-0016`-comparator-shaped `capital_priority` assessment (`maintain_current_weight` / `case_for_review` / `no_assessment`) that never becomes a `target_pct` input by formula, matching `docs/PORTFOLIO_INTELLIGENCE_SPEC.md` §19's own "suggested target, human-applied" integration tier; a purely computed cross-reference rollup (`risk_concentration`) of existing `caps.clusters`/`issuer_lookthrough.yaml`/`intelligence/relationships/` coverage, adding no new ceiling and operationalizing `TIER-0001`'s corrected 13/27-uncovered finding as a standing per-ticker flag; and an `evidence_quality` axis reusing the already-differentiated `risks[].severity` vocabulary plus one new required per-ticker uncertainty statement, while explicitly declining to reuse the two fields `TIER-0001` found decorative (`monitoring_enabled`, `risks[].status`). **One rejected alternative recorded**: extending the frozen Company Intelligence YAML schema (§9) directly with a `classification:` block — rejected for requiring its own separate governance decision, creating backfill pressure across 47 existing records regardless of eventual adoption, and mismatching `risk_concentration`'s inherently portfolio-level (not single-company-level) content, the same reasoning `REL-0001` already applied to justify its own new namespace. A future Milestone 6 blind-classification method is described (per-ticker blind draft, sealed before comparison, unblinded only at a future Milestone 7 reconciliation step, per `OPS-0006` §2's already-frozen zero-based discipline) but not begun, authorized, or applied to any ticker. **Classifies no ticker, creates no `intelligence/classification/` file, computes no mechanical score, and does not authorize Milestone 6 (blind classification), Milestone 7 (baseline reconciliation), Milestone 8 (policy recommendation package), or Milestone 9 (independent review and adoption).** Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) — `tier0001-post-merge-verification` (recording `TIER-0001`'s confirmed merge/review/acceptance state without editing its own prior gate text) and `tier0002-candidate-framework-design` — and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields; the `milestone-5-zero-based-classification-and-tier-architecture-review` gate's own `status: proposed` is unchanged, since this is Milestone 5's second bounded step, not its completion. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **PI-0038 proposed (not yet accepted): WS-0005 gated-six Company Intelligence completion, plus a durably recorded pre-Milestone-6 roadmap** (`governance/decisions/PI-0038-ws0005-gated-six-company-intelligence-completion.md`). On explicit principal authorization, authorizes and — in the same combined governance-and-implementation PR — implements first Company Intelligence coverage for the six currently gated canonical equities (SNPS, ICE, SPGI, WM, RKLB, TSLA; `gates.yaml`, authority `PHQ-2026-01`) — the only six canonical equity destinations that still lacked any Company Intelligence record. Narrowly supersedes `PI-0033` solely for RKLB's and TSLA's research-authorization purposes (both also carry a legacy pre-`PHQ-2026-02` `PI-0033` spec-tier disposition), matching the `PI-0036` precedent exactly; `PI-0033`'s text is otherwise unedited and remains fully controlling for its other fifteen names. SNPS/ICE/SPGI/WM carried no `PI-0033` disposition — `PI-0035` had already newly dispositioned all four with no prior research authorization; this filing is that first authorization. **This is not a reopening of Milestone 3** (`PI-0031` §K's own completion criteria treated the six gated names as adequately dispositioned via their own `next_gate` conditions, without requiring coverage; Milestone 3's determination, `PI-0037`, is genuinely `status: Accepted`/merged and is unedited) — it is a separate, forward-looking preparation step for a future Milestone 6, going beyond what Milestone 3 required, on the principal's own explicit instruction. Precisely disclosed, not assumed: `operations/WORKSTREAMS.yaml`'s `milestone-4-portfolio-relationship-mapping` gate already shows `status: complete`, but the underlying `REL-0006` decision remains `status: Proposed`/unmerged in `governance/decisions.yaml` as of this filing's base — the register's forward-written status is not treated as settled fact by this filing. Each of the six new records (`SNPS.yaml`/`.md`, `ICE.yaml`/`.md`, `SPGI.yaml`/`.md`, `WM.yaml`/`.md`, `RKLB.yaml`/`.md`, `TSLA.yaml`/`.md`, `conviction.rating: Medium` on all six, PROPOSED pending independent review) discloses its own research session's source-access failure in full — every session found `WebFetch` either completely non-functional (HTTP 403 on every domain attempted, including a neutral control domain/page) or blocked at the network-policy layer, so no primary-source document was directly opened for any of the six; every fact traces to WebSearch-returned snippets, with cross-source numeric inconsistencies disclosed rather than silently resolved in each record's own Uncertainty section. **Also durably records, per separate explicit principal direction, an ordered six-step pre-Milestone-6 roadmap** in `WS-0005`'s register entry (new gates `milestone6-prereq1` through `milestone6-prereq6`): (1) this filing's own gated-six completion, BLOCKING, `in_progress`; (2) a future focused current-roster Intelligence freshness verification, required after (1), `proposed`, not authorized to execute; (3) a future classification-material relationship-gap check (distinct from, not reopening, Milestone 4/`REL-0006`), required after (2), `proposed`, not authorized to execute; (4) an explicit principal decision on whether blind classification is fundamentals-only or requires governed chart evidence (`CHART-0001`/`CHART-0002`) as an input, required before Milestone 6 authorization, `pending_principal_decision`, no default assumed; (5) a future Milestone 6 population reconciliation from live repository truth once (1)-(4) resolve, not predeclared to any particular count, `proposed`, not authorized to execute; (6) a future, separate, fresh Milestone 6 authorization required after (1)-(5) — completing the prerequisites does not itself authorize Milestone 6, and the existing `milestone-6-blind-classification` gate remains untouched, `status: proposed`, "Not authorized to execute." **Does not use, adapt, move, or reuse** the two preserved, untracked draft files (`classification_validator.py`, `test_classification_validator.py`) left idle in a prior, separate, unpushed Milestone 6 session's own local working tree — that session's branch was independently confirmed absent from `origin` before this filing began, and this filing's own implementation worktree cannot reach those files. Authorizes no tier/target/gate/cluster/cap/holdings/margin/allocator/trade change, activates no gate, and performs no Milestone 6 classification of any kind. **Bounded correction (same day, this PR):** an independent exact-head review (review `4860559002`, verdict CHANGES REQUIRED, 0 BLOCKING / 5 MAJOR / 7 MINOR / 3 NOTE) confirmed every structural/authority/mechanical check but found the evidence layer not yet ready for unqualified reliance in three records' central claims and one process gap. Resolved by one bounded correction pass, performing narrow, targeted WebSearch-only re-verification of each disputed claim (WebFetch remained blocked on every domain attempted on a fresh attempt, including sec.gov/ftc.gov/iridium.com): **SPGI** — the Mobility-spinoff-scope claim is corrected/clarified rather than simply reversed, since this session's own re-verification found evidence supporting the record's original Mobility-only framing (Engineering Solutions confirmed sold to KKR in an unrelated 2023 transaction) rather than the reviewer's broader claim, presented transparently as this session's own finding, not a blind correction; the ~$2.1B segment-revenue reconciliation gap is closed by adding the previously-omitted Commodity Insights figure (~$1.95B). **SNPS** — the Ansys deal-terms figure is corrected to distinguish originally-announced (0.3450 shares + $197.00 cash) from final-at-closing (0.3399 shares + $199.91 cash) consideration, both apparently accurate for their respective points in the deal timeline; Nvidia-investment mechanics hedged to match the record's own treatment of comparably-sourced items. **ICE** — a new subsection adds Black Knight's own 2023 FTC antitrust settlement and mandated Empower/Optimal Blue divestiture to Constellation Software (completed deal value cut from ~$13.1B to ~$11.9B), kept separate from the MarketAxess discussion, cross-referenced from the antitrust-risk item. **WM** — the FY2025 revenue-mix breakdown is disclosed as incomplete (~90% of stated total; additional figures found do not cleanly close the gap either) and a numbering typo fixed. **RKLB** — the Iridium deal's existence is re-sourced to both parties' own official investor-relations releases and an SEC EDGAR exhibit URL (still not directly opened), and the source-access risk item's severity raised low→moderate. **TSLA** — the 41%→59% US EV market-share claim is flagged `[UNVERIFIED-CONFLICT]` against additional, unreconciled full-year (~46%) and Q1 2026 (~54.2%) figures found. **Process**: this filing's own text is amended to explicitly address `OPS-0008` §2's stop-before-drafting/evidence-recovery gate — disclosing that none of the six original research sessions invoked it, reasoning why `OPS-0008` textually scopes to Milestone 3 batches (this is a Milestone-6-preparation unit) and citing the `PI-0036` precedent, and stating plainly that all six records remain PROVISIONAL with specific, named direct-primary-source gaps still outstanding. A `portfolio_role_ref: gated` remark was also added, explaining (not changing) this new corpus value. No record's `conviction.rating`, no gate, no tier/target/cluster/cap/holdings/margin/allocator value changed by this correction. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **PI-0039 proposed (not yet accepted): WS-0005 Milestone-6-prerequisite-2, current-roster Company Intelligence freshness verification** (`governance/decisions/PI-0039-ws0005-milestone6-prereq2-current-roster-freshness-verification.md`). Also records, as a Lane M factual synchronization folded into this same filing: `PI-0038` (PR #247) independently re-confirmed merged (merge commit `37a7c92273ae66c74f84c20654975368e12cfff6`, parents `24be14552b0e5caed8052e07497855cde6a37085` and `43d6c5343b99c3a896e2274d16585ca6ffdc1a28`), independently reviewed (`4860559002`, CHANGES REQUIRED, resolved by correction commit `43d6c534`; delta `4860700027`, DELTA APPROVED — APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE), and principal-accepted at its exact head (`issuecomment-5187185273`), with post-merge verification (`issuecomment-5187207280`) confirming merge-tree identity, 53/53 valid records, decision-catalog reconciliation, 27/27 canonical-equity coverage, and merge-commit CI success — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the `TIER-0001`/`TIER-0002`, `REL-0002`-`REL-0006`, `CHART-0001`/`CHART-0002` two-step acceptance-recording pattern, not corrected here. Pre-Milestone-6 roadmap step 1 (`milestone6-prereq1`) is therefore complete. On explicit principal authorization ("I authorize one coherent freshness-verification PR covering every current canonical equity with a Company Intelligence record"), `PI-0039` executes pre-Milestone-6 roadmap step 2 (`milestone6-prereq2-current-roster-freshness-verification`): independently re-derived the 27-name canonical equity population from `targets.yaml` (zero drift), confirmed all 27 already have Company Intelligence coverage, and ran five parallel WebSearch-based research passes checking each record's `review.last_reviewed` date against material events through 2026-08-05 (earnings/guidance, transactions, restructuring, regulatory/litigation, major contracts, capital-allocation changes, resolution of previously-unresolved items). **Result** (full detail retained at `governance/audits/WS0005_M6PREREQ2_CURRENT_ROSTER_FRESHNESS_VERIFICATION_20260805.md`): **12 CURRENT** (TSM, AVGO, SNPS, ICE, SPGI, PANW, COST, WM, CEG, GEV, GNRC, RTX — no edit); **13 UPDATE REQUIRED**, each given a clearly labeled SECONDARY addition (never presented as primary-verified — WebFetch remained blocked on every finance/IR/SEC domain attempted this session, consistent with this repository's own extensively disclosed history) to the specific bearing risk/catalyst/competitive-advantage entry, a new `review.log` entry, and `review.last_reviewed`/`next_due` advanced to today, with `conviction.rating` explicitly preserved on every one: **NVDA** (disclosed-but-unfinalized ~$250B OpenAI financing-guarantee talks), **ASML** (China domestic-DUV mass-production escalation, DUV-only, EUV monopoly unchanged), **KLAC** (resolved its own flagged Q4 FY2026 earnings catalyst, updated backlog, Hua Hong "fairly immaterial" characterization), **MSFT** (resolved its own flagged FY2026 Q4 catalyst — Azure $100B+/43% growth, new Anthropic-investment-gain disclosure), **GOOGL** (found a genuine evidence-currency gap — Q2 2026 results actually predated the record's own evidence-recovery audit date), **AMZN** (Q2 2026 results added; a single-source, uncorroborated ~$53B Anthropic-gain claim explicitly NOT adopted as fact), **META** (the most significant gap in this batch — net income down 14% YoY, EPS miss, two wholly new risk entries: a $2.4B legal-proceedings charge and an ~8,000-employee layoff), **ISRG** (OTTAVA's reported actual FDA De Novo market authorization, both `.yaml` and `.md` updated), **TMO** (Q2 2026 results entirely missing from prior evidence base; added, plus two new risk entries — a microbiology-business divestiture and a disclosed Clario-acquisition evidence gap), **V** (resolved its own flagged fiscal Q3 2026 catalyst; added a new risk entry for a same-day 7% workforce reduction/$563M restructuring charge — corrected a self-inflicted YAML structural bug from this filing's own first edit attempt before it reached validation), **ETN** (resolved the Boyd Thermal first-full-quarter catalyst; Q2 2026 margin recovery to 23.1% added as relevant evidence in the existing UBS margin-quality dispute, not treated as resolving it), **PWR** (resolved its own flagged Q2 2026 catalyst — record backlog/RPO, four undisclosed completed acquisitions; the pre-existing FY2025 adjusted-EBITDA ambiguity was checked and remains explicitly unresolved, not silently dropped), **TSLA** (corrected a robotaxi-geography claim in `TSLA.md` that was already stale at the record's own same-day authoring time — Orlando/Tampa launches; added a corroborating, non-resolving data point to the record's own disclosed `[UNVERIFIED-CONFLICT]`; corrected a pre-existing `next_due` arithmetic error). **2 UNRESOLVED, explicitly NOT edited** per this filing's own "update only UPDATE REQUIRED records" scope discipline: **LLY** (the record's own catalyst date is independently confirmed wrong — actual confirmed report date is today, 2026-08-05 — but whether results have actually been released could not be confirmed from available search results); **RKLB** (all three of the record's own already-disclosed gaps — WebFetch block, Neutron milestone ambiguity, share-count spread — were re-tested, not merely re-flagged, and confirmed still open). **Disclosed, not actioned**: four other same-day-filed (`PI-0038`) records plus `SNPS`/`GNRC` (ICE, SPGI, WM, RKLB) carry the same class of `next_due` arithmetic inconsistency TSLA's was corrected for — left untouched since none of the five is itself classified UPDATE REQUIRED, flagged in the retained artifact for a future session. `operations/WORKSTREAMS.yaml`'s `milestone6-prereq2` gate transitions `proposed` → `in_progress` (reaching `complete` only on this filing's own merge, per `OPS-0006` §16.1); `WS-0005`'s `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields updated to this session's own branch and verified SHA. **Authorizes no tier/target/gate/cluster/cap/holdings/margin/allocator/trade change, no `conviction.rating` change on any record, no blind classification, no relationship-gap analysis, and no chart-evidence decision.** The two preserved, untracked Milestone 6 validator drafts (`classification_validator.py`, `test_classification_validator.py`) were not read, moved, staged, or reused. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **REL-0007 proposed (not yet accepted): WS-0005 Milestone-6-prerequisite-3, classification-material relationship gap check — no new relationship record justified** (`governance/decisions/REL-0007-ws0005-milestone6-prereq3-relationship-gap-check.md`). Also records, as a Lane M factual synchronization folded into this same filing: `PI-0039` (PR #248) independently re-confirmed merged (merge commit `1a91d8986652461584b4562bb4cd31b3c1b58bbd`), independently reviewed (`4863450699`, APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE, 2 MINOR/1 NOTE), and principal-accepted at its exact head (`issuecomment-5191738468`), with post-merge verification (`issuecomment-5191791015`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. Pre-Milestone-6 roadmap steps 1 and 2 are therefore complete, synchronized by this filing (the `pi0038-gated-six-company-intelligence-completion`, `milestone6-prereq1-gated-six-intelligence-completion`, and `milestone6-prereq2-current-roster-freshness-verification` gates all transition `in_progress` → `complete`), resolving both MINOR findings `PI-0039`'s review explicitly deferred to "the next WS-0005 filing." On explicit principal authorization ("I authorize one coherent Step 3 governance-and-gap-check PR"), `REL-0007` executes pre-Milestone-6 roadmap step 3: determine whether any missing `intelligence/relationships/` record would materially affect at least one `TIER-0002` classification axis (`economic_role`, `capital_priority`, `risk_concentration`, `evidence_quality`) — explicitly not an exhaustive relationship graph. **Recomputed `TIER-0002` §3.5's own three-mechanism `risk_concentration.unmeasured_flag` test live against all 27 canonical names** (cluster-cap membership, issuer-look-through membership, relationship-record coverage), disclosing that `TIER-0001`/`TIER-0002`'s own stated "13 of 27 unmeasured" figure is stale by two names — `LLY` and `TSLA` are both `issuer_lookthrough.yaml` members and were never actually unmeasured under the field's own three-mechanism definition; the corrected count is **11**: `SNPS, PANW, ISRG, TMO, ICE, SPGI, V, COST, WM, RTX, RKLB`. Read all six `PI-0038` gated-name Company Intelligence records in full for newly-available relationship evidence (unavailable to any prior Milestone-4 filing) and re-evaluated the two candidates `REL-0006` Criterion 6 disclosed but never actioned. **Dispositions**: zero candidates reached MATERIAL AND EVIDENCE-READY; `SNPS↔NVDA` (`technology_platform_dependency`, SNPS's own record citing a $2B Nvidia equity stake and a CUDA-X/AI-physics/Omniverse platform pivot) and `RTX↔RKLB` (a joint Golden Dome government-program selection, no clean primitive-type fit) both classified MATERIAL BUT REQUIRES NEW RESEARCH — both rest on a single, self-disclosed-as-weak, one-sided source, wholly uncorroborated by the counterparty's own comparatively well-sourced, primary-source-heavy record (NVDA's and RTX's records, respectively, make zero mention of either claim despite each record's demonstrated depth on comparable topics); `GEV`/`ETN`/`PWR` `capital_spending_dependency` and `MSFT`/`AMZN` `regulatory_or_reimbursement_dependency` both classified DUPLICATIVE OF EXISTING STRUCTURE (every named ticker already fully measured via cluster cap and/or issuer look-through and/or existing relationship records — implementing either would not change any `TIER-0002` axis for any ticker involved); `PANW, ISRG, TMO, V, COST` each freshly re-checked and reconfirmed to carry zero canonical-pair relationship evidence of any kind. **No new `intelligence/relationships/*.yaml` or `.md` record was created** — the negative finding is preserved explicitly, per the authorizing instruction's own efficiency standard ("prefer a complete, well-supported negative finding over unnecessary relationship files"), and does not block Milestone 6 (matching `REL-0004`/`REL-0006`'s own precedent that new external relationship research is not a completion prerequisite). Full methodology, the recomputed 27-name coverage table, and every candidate's reasoning are retained at `governance/audits/WS0005_M6PREREQ3_CLASSIFICATION_MATERIAL_RELATIONSHIP_GAP_CHECK_20260805.md`. Also performs, per explicit principal authorization, one bounded `intelligence/companies/AMZN.yaml` wording correction (the ~$53B Anthropic-gain claim now states it was reported across multiple outlets but not primary-source-verified, preserving its exclusion as a one-time non-operating item rather than because it was uncorroborated) — the exact correction `PI-0039`'s own independent review recommended. **The `milestone6-prereq3-relationship-gap-check` gate itself is left at `status: in_progress`, `pr: null`** (not marked complete) — consistent with this repository's own established discipline (`TIER-0001`'s independent review previously found a premature `status: complete` on a still-open, self-referencing PR to be a MAJOR defect) — its completion is deferred to a later filing's own Lane M synchronization once independent review, correction if needed, principal acceptance, merge, and post-merge verification have actually occurred. Authorizes no new `intelligence/relationships/` record, no external relationship research, no price-correlation study, no Milestone 6 population, authorization, or classification of any kind, no `milestone6-prereq4` chart-evidence scope decision, and no tier/target/role/cluster/cap/gate/holdings/margin/allocator change. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **TIER-0003 proposed (not yet accepted): WS-0005 Milestone-6-prerequisite-4, chart-evidence scope decision — Option A (fundamentals-only) recorded** (`governance/decisions/TIER-0003-ws0005-milestone6-chart-evidence-scope-decision.md`). Also records, as a Lane M factual synchronization folded into this same filing: `REL-0007` (PR #249) independently re-confirmed merged (merge commit `71dab2d218de5c4184d5f62bc29f0bc7b409c64f`, head `1662cb038006bc677ec8a741606e7b47f966c894`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. `milestone6-prereq3-relationship-gap-check` therefore transitions `in_progress` → `complete`. On the human principal's explicit decision, `TIER-0003` executes pre-Milestone-6 roadmap step 4 (`milestone6-prereq4-chart-evidence-scope-decision`, a principal decision, not a research unit): **Option A** — WS-0005 Milestone 6 blind classification will be fundamentals/business-evidence only. Permitted evidence: governed Company Intelligence; business and financial evidence; disclosed risks and catalysts; governed `intelligence/relationships/` records; cluster-cap and issuer-look-through dependency/concentration evidence; each record's `TIER-0002` evidence-quality signal. **Chart evidence under `CHART-0001`, `CHART-0002`, or any later chart decision is excluded from every `TIER-0002` axis during blind drafting** — stated as binding, not discretionary, with an explicit excluded-evidence list (raw chart images, filenames, manifests, coverage/inventory status, technical indicators, support/resistance levels, momentum/trend descriptions, technical interpretations, price-action conclusions, CHART-0001/CHART-0002 evidence packages) and a per-axis statement (`economic_role`/`capital_priority`/`risk_concentration`/`evidence_quality`: all four "no chart input") so chart evidence cannot create an implicit fifth classification axis. Chart evidence remains a separate, downstream, advisory layer after sealing — monitoring, technical-risk overlays, implementation sequencing, buy-ladder (`LADDER-0001`)/sell-discipline analysis, entry/exit timing, later policy-recommendation context — explicitly labeled chart-derived and kept apart from the classification record; it may never automatically change targets/tiers/holdings/gates/caps/clusters/allocator/margin/orders/trades. **This filing authorizes no Milestone 6, no ticker classification, no chart ingestion or interpretation, no Step 5 (population reconciliation) or Step 6 (fresh authorization) work, and no edit to `CHART-0001`/`CHART-0002`/`TIER-0001`/`TIER-0002`'s own text.** `milestone6-prereq4-chart-evidence-scope-decision` is left at `status: in_progress`, `pr: null` — not marked complete, consistent with this repository's own established discipline (a filing does not mark its own still-unmerged, unreviewed work complete); completion is deferred to a later filing's own Lane M synchronization once independent review, correction if needed, principal acceptance, merge, and post-merge verification have actually occurred. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **TIER-0004 proposed (not yet accepted): WS-0005 Milestone-6-prerequisite-5, population reconciliation and blindness-process preflight** (`governance/decisions/TIER-0004-ws0005-milestone6-population-and-blindness-preflight.md`, supporting artifact `governance/audits/WS0005_M6_POPULATION_AND_BLINDNESS_PREFLIGHT_20260805.md`). Also records, as a Lane M factual synchronization folded into this same filing: `TIER-0003` (PR #250) independently re-confirmed merged (merge commit `ecd1e89d1278432874dab0d5440a9a8eecbc57d1`), independently reviewed (`4865299395`, 0 BLOCKING/0 MAJOR/0 MINOR/2 NOTE, "APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE"), and principal-accepted at its exact head (`issuecomment-5193111879`), with post-merge verification (`issuecomment-5193183437`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. `milestone6-prereq4-chart-evidence-scope-decision` therefore transitions `in_progress` → `complete`. On explicit principal authorization ("I authorize exactly one Step 5 filing"), `TIER-0004` executes pre-Milestone-6 roadmap step 5 (`milestone6-prereq5-population-reconciliation`): independently re-derives the 27-name canonical equity population from live `targets.yaml` (zero drift), confirms all 27 carry governed Company Intelligence with **zero exclusions** — a canonical equity's `PROVISIONAL` status, partial/blocked primary-source access, a bounded unresolved evidence item, or gate status is never grounds for population exclusion; LLY, RKLB, and the six formerly-gated names (SNPS, ICE, SPGI, WM, RKLB, TSLA) are explicitly confirmed included on identical terms, directly resolving `TIER-0001`'s own open question #5 ("does the 6-name gated set need any classification at all while gated"). Specifies, design only, no implementation: the permitted/forbidden Milestone 6 evidence boundary (restating and extending `TIER-0003` §A/§B); a deterministic per-ticker redaction procedure stripping `portfolio_role_ref`, `conviction`, and `review.log` narrative from Company Intelligence records before any drafting session sees them, retaining `sector`/`industry`/`themes`/`competitive_advantages`/`risks[]`/`sources[]`; a nine-step judgment-before-risk-concentration drafting sequence (`economic_role` and `capital_priority` sealed before `caps.clusters`/`issuer_lookthrough.yaml`/relationship-record cross-references ever enter the record); per-ticker sealing metadata (`lifecycle_status`/`sealed_at`/`governing_decision`/`drafting_session_or_shard_id`/`schema_version`/`content_sha256`/cohort-manifest entry), explicitly framed as a hash-and-timestamp audit trail, not cryptographic immutability (matching `CHART-0002`'s own `verifiability_boundary` framing); and future `classification_validator.py` requirements. **Closes the one identified schema gap**: `TIER-0002`'s `capital_priority`/`evidence_quality` axes already had `no_assessment`/`blocked` abstention paths, but `economic_role` had none — adds exactly one new `economic_system_ref` enum value, `unable_to_determine`, requiring (only when selected) two new conditional sub-fields, `abstention_reason` and `evidence_gap_statement`, both non-empty and specific. **Abstention does not cascade** — a bounded unresolved evidence item on one axis is never automatic grounds for `no_assessment` or abstention on another; each axis is sealed on its own evidence sufficiency. No fifth axis, no score, no weighting formula. Independently confirms the two untracked local `classification_validator.py`/`test_classification_validator.py` drafts reported from a prior, separate, stopped session are absent from this session's own working tree (`git status --porcelain` clean, zero filesystem matches) — no contamination risk, no cleanup PR warranted; the future validator must be authored fresh from current `main` and independently reviewed. Recommends, not authorizes: all 27 tickers in one future implementation PR, ~5 blind-drafting shards of 5-6 names each (matching `CHART-0002`'s own shard-review precedent), one integration and sealing pass, one independent exact-head review of the complete batch, no one-PR-per-ticker design. **Discloses, does not silently correct, one discrepancy against the authorizing task's own assumed live state**: the task described `milestone6-prereq6-fresh-authorization-required` as `status: proposed`; live `operations/WORKSTREAMS.yaml` (set by `PI-0038`, unedited since) instead reads `status: blocked`, a materially equivalent, already-correct non-authorization state — left exactly as merged, not rewritten to match the task's copied assumption, per this repository's "never trust copied state over live repository truth" discipline. **This filing authorizes no Milestone 6, no ticker classification, no sanitized evidence package, no validator implementation, and no Step 6 (fresh authorization) work, and does not edit `CHART-0001`/`CHART-0002`/`TIER-0001`/`TIER-0002`/`TIER-0003`'s own text.** `milestone6-prereq5-population-reconciliation` is left at `status: in_progress`, `pr: null` — not marked complete, consistent with this repository's own established discipline; completion is deferred to a later filing's own Lane M synchronization once independent review, correction if needed, principal acceptance, merge, and post-merge verification have actually occurred. `milestone6-prereq6-fresh-authorization-required` and `milestone-6-blind-classification` are both left exactly as merged (`status: blocked` and `status: proposed` respectively) — neither is edited by this filing. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **TIER-0005 proposed (not yet accepted): WS-0005 Milestone-6-prerequisite-6, fresh Milestone 6 authorization** (`governance/decisions/TIER-0005-ws0005-milestone6-fresh-authorization.md`, no supporting artifact — `TIER-0004` already carries the complete accepted specification). Also records, as a Lane M factual synchronization folded into this same filing: `TIER-0004` (PR #251) independently re-confirmed merged (merge commit `d42988f2dae167c484d0b8a07bdb3ac2676a975c`), independently reviewed across three rounds (`4865967653` CHANGES REQUIRED — 1 BLOCKING/1 MINOR, resolved by correction commit `a9bef37`; `4866171285` delta CHANGES REQUIRED — the MINOR only half-fixed, resolved by delta-correction commit `eb3e281`; `4866305616` delta **DELTA APPROVED — APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE**, 0/0/0/0 at exact head `87891c9b720c14d7a37521136fe4d4248a155445`), and principal-accepted at that exact head (`issuecomment-5194141626`), with post-merge verification (`issuecomment-5194217647`) and merge-commit CI (`31023085469`, `completed`/`success`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. `milestone6-prereq5-population-reconciliation` therefore transitions `in_progress` → `complete` (`pr: 251`). On explicit principal authorization ("I authorize one Step 6 filing"), `TIER-0005` executes pre-Milestone-6 roadmap step 6 (`milestone6-prereq6-fresh-authorization-required`): **authorizes exactly one future, separate, bounded Milestone 6 implementation PR** covering all 27 canonical equities, binding it **by reference, not restatement** to `TIER-0004`'s already-accepted specification — population (27/27, zero exclusions), the four-axis framework (`economic_role`/`capital_priority`/`risk_concentration`/`evidence_quality`, no fifth axis, no score, no target), permitted/forbidden evidence boundary, `.yaml`+`.md` redaction mechanics with mandatory fail-closed re-scan, judgment-before-risk-concentration sequencing, sealing/contamination controls, the non-cascading `economic_role` abstention amendment, and future `classification_validator.py` requirements — none of it redesigned, loosened, or restated in this filing's own words beyond a citation index, on the reasoning that restatement itself is exactly the risk `TIER-0004`'s own BLOCKING finding (an `.md` redaction gap affecting 8 of 27 tickers) demonstrated is live, not hypothetical. Makes `TIER-0004` §I's recommended batch/shard shape binding: ~5 blind-drafting shards of 5-6 names, one integration and sealing pass, one `classification_validator.py`, one independent exact-head review of the complete batch — no one-PR-per-ticker design, no separate pilot unless a genuine architectural defect surfaces before drafting completes. **This filing classifies no ticker and implements no Milestone 6 output itself** — it authorizes a future implementation PR, gated on its own full independent-review/correction/re-review/principal-acceptance/merge/post-merge-verification lifecycle. Independently reconfirmed the two untracked local `classification_validator.py`/`test_classification_validator.py` drafts from the prior, separate, stopped Milestone 6 session remain absent from this session's own working tree, and that `intelligence/classification/` does not exist anywhere in the repository. `milestone6-prereq6-fresh-authorization-required` transitions `status: blocked` → `status: in_progress` (recording this filing's own branch, not yet `complete`, since this filing's own governance PR is itself unmerged). **`milestone-6-blind-classification` is deliberately left exactly as it stands, `status: proposed`, "Not authorized to execute"** — per the authorizing instruction, this filing's own authorization does not make that gate executable while its own PR remains unmerged; a later, separate, post-merge synchronization filing performs that transition once this PR merges, matching the identical discipline `TIER-0004` applied to `milestone6-prereq5` itself. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **WS-0005 Milestone 6 (blind classification) implementation, draft, not yet accepted** (no new governance decision — `TIER-0005`, merged via PR #252, is the sole authorization; this filing is the implementation it authorized). Independently re-confirmed `TIER-0005`'s merge before proceeding: accepted head `350faaf39afd6bf43348baa8444e6a7e3bb605b9`, merge commit `228b2dd0c6e68b205d99f1e6a4a0fc483df64a0e`, independent review `4866670109` (APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE), principal acceptance `issuecomment-5194672412`, post-merge verification `issuecomment-5194740939`, merge-commit CI `31027034116` `success` — zero open competing PRs, working tree clean, the two prior stopped-session `classification_validator.py`/`test_classification_validator.py` drafts confirmed absent. Delivers sealed blind-classification records for all 27 canonical equities under the accepted `TIER-0002`/`TIER-0003`/`TIER-0004`/`TIER-0005` controls: a fresh `intelligence_classification_sanitizer.py` (deterministic, retained as a reusable capability) redacting each ticker's Company Intelligence `.yaml` (whole-key strip of `portfolio_role_ref`/`conviction`/`review.log` narrative, plus item-level redaction of retained fields — `risks[].risk`, `competitive_advantages[]`, `sources[].note` — found live to leak policy/chart-domain language in a handful of records) and `.md` (whole-section strip for headers discovered to leak a forbidden conclusion with **no marker word in the section's own body** — `## Conviction` sections reading only `**Rating: High**`, `## Relationship to the gate` quoting `gates.yaml` directly, `## Capital-priority discipline`, `## Current governed tier and target`, `## Eleven-question committee review`, and similar — plus the existing marker-based paragraph strip), followed by a mandatory post-redaction re-scan and a fail-closed complete-package scan; all 27 tickers passed clean and byte-identically reproducible across repeated runs (two, SPGI and GEV, had their `.md` narrative excluded outright after a surviving marker, per the fail-closed design — sector/industry/risks/competitive_advantages remained available from the `.yaml`). Five ~5-6-ticker blind-drafting shards (read-only Explore subagents, each instructed to read exactly one sanitized scratch file outside the repository and nothing else — an **instructional, not filesystem-sandboxed, isolation boundary**, disclosed as such, matching `TIER-0004` §9.2's own admission that a hash-audit trail "cannot... prove a drafting session did not consult forbidden evidence before sealing") drafted `economic_role` and `capital_priority` only, from sanitized evidence alone, with zero access to `targets.yaml`/`holdings.yaml`/`gates.yaml`/`portfolio_role_ref`/`conviction`/cluster or issuer-look-through membership. Only after both judgment axes were returned did this session mechanically compute `risk_concentration` from live `targets.yaml caps.clusters`/`issuer_lookthrough.yaml`/`intelligence/relationships/*.yaml` — 11 of 27 independently confirmed `unmeasured_flag: true` (`SNPS, PANW, ISRG, TMO, ICE, SPGI, V, COST, WM, RTX, RKLB`), matching `REL-0007`'s own corrected recomputation exactly — and complete `evidence_quality` (`primary_source_coverage` taken from `TIER-0004` §2.3's own per-ticker evidence-posture table — `limited` for the six formerly-gated names, `partial` for LLY, `comprehensive` for the remaining 20; `highest_disclosed_risk_severity` a mechanical rollup of each ticker's own `risks[].severity`; one `thesis_uncertainty_statement` authored per ticker citing its single most severe disclosed risk). A post-hoc adversarial scan of all 27 shard outputs and all 27 sealed records for every forbidden marker (`portfolio_role_ref`, `conviction`, `target_pct`, `targets.yaml`/`holdings.yaml`/`gates.yaml`, tier labels, chart-domain terms) found zero leakage. **Result: 25 of 27 sealed `case_for_review`, 2 (COST, WM) `maintain_current_weight`, 0 `no_assessment`, 0 `economic_role` abstentions** — disclosed as a genuine, evidence-driven skew (every Company Intelligence record in this corpus is itself risk/catalyst-rich by construction, and the `case_for_review` bar as instructed is deliberately low — "worth a reviewer's specific attention," not a buy/sell signal) rather than templating, supported by materially distinct per-ticker reasoning, comparator sets, and the two genuinely differentiated exceptions. Fresh `classification_validator.py` (four-axis structural/vocabulary/abstention/hash/manifest validation, zero import coupling with `allocate.py`/`margin_state.py`, reuses `relationship_validator.load_canonical_universe()` rather than re-deriving the population check) plus `test_classification_validator.py`/`test_intelligence_classification_sanitizer.py` (86 tests, including 27 parametrized real-corpus fail-closed-scan tests) all pass; full suite 2661/2661; `intelligence_validator.py` (53 companies, 2 themes), `relationship_validator.py` (13 records), and the decision catalog (79, `issues == ()`) all unaffected; zero diff on every protected path (`allocate.py`, `margin_state.py`, `levels.py`, `holdings.yaml`, `targets.yaml`, `gates.yaml`, `issuer_lookthrough.yaml`, every existing `intelligence/companies|themes|relationships` record, `governance/evidence/`, `governance/decisions.yaml`). One retained `intelligence/classification/COHORT_MANIFEST.yaml` records all 27 tickers' `content_sha256`, reconciled bidirectionally by the validator. **No Milestone 7 baseline reconciliation, no comparison against current tiers/targets/holdings/gates/roles, no policy recommendation, and no tier/target/holdings/gate/cap/cluster/allocator/margin/ladder/chart/order/trade change of any kind occurs in this filing** — sealed records only, per `TIER-0004` §8 step 9's own hard stop; unblinding and comparison remains its own separately authorized future Milestone 7 unit. This session does not review its own PR, mark it ready, merge it, or post principal acceptance. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.

  **Bounded correction (same day, this PR, review `4867365726`).** An independent exact-head review of PR #253 (anchored to head `3876c21e08c5a4793bbce59b0ea976b1d260d844`) returned CHANGES REQUIRED — 1 BLOCKING, 1 MAJOR, 1 MINOR, 2 NOTE (not actionable). **BLOCKING, resolved:** the sanitizer's marker list caught the adjective "gated" but never the bare policy noun "gate" — every one of the six formerly-gated records (SNPS, ICE, SPGI, WM, RKLB, TSLA) discusses its own `gates.yaml` entry using constructions like "this record's gate"/"the gate's own X"/"the gate names Y", none of which contained "gated", so that language reached all five drafting shards unredacted; a related dangling-cross-reference defect let ETN.md's own "see 'Governed policy' below" paragraph survive even though the section it named was correctly stripped; the "fail-closed complete-package scan" was also found tautological — it reran the exact same pattern list the strip pass had already applied to the already-redacted text, so nothing the strip pass missed could ever be caught. Root-cause-fixed, not patched per-ticker: `intelligence_classification_sanitizer.py` v1.1 adds a gate-policy detector (bare "gate"/"gates" with a legitimate-use whitelist for real non-policy usage independently confirmed in this corpus — ASML's "technological gate," KLAC's "gate-all-around" transistor structures, PWR's "customer-qualification gate," and OPS-0008 process/methodology gates like "stop-before-drafting gate"/"CI gate"/"source-readiness gate"); merges the whole-section-header title list into the paragraph-level strip pass so any dangling cross-reference to a stripped section is caught generically, not as a one-off; extends item-level YAML redaction to `catalysts[].catalyst` (previously entirely unscanned — SNPS's own catalyst-field leak reached its shard through exactly this gap); adds a genuinely independent second-stage verifier (`independent_policy_scan()`, built from materially different, broader concept checks — bare `conviction`, the gate-policy family, literal `gates.yaml`/`next_gate`/`allow_add`/`cash_pending_clearance` config-key names, a target/percent-of-book numeric pattern, chart-domain — used by both the post-strip re-scan and the final complete-package scan, replacing the tautological reuse); and fixes a discovered whitespace-normalization gap (`yaml.safe_dump` line-wraps long strings, splitting a multi-word whitelist phrase like "customer-qualification gate" across a newline and defeating the literal-space match — every check now runs against a whitespace-collapsed working copy). Independently re-derived, not assumed: exactly 8 of 27 sanitized packages were provably contaminated (byte-hash comparison between the original and corrected sanitizer) — the six formerly-gated names plus **ETN** (the dangling reference) and **PWR** (a genuine bare-`conviction` leak in `risks[2].risk`, unrelated to gate policy) — MSFT's own apparent change on first pass turned out to be a false positive (a legitimate "source-readiness gate" phrase not yet whitelisted), confirmed by re-running the diff after adding it. 27 new regression tests added (`test_intelligence_classification_sanitizer.py`), including per-ticker fail-closed checks for all six formerly-gated names, the ETN dangling-reference regression, a policy-synonym-absent-from-the-strip-list case, and explicit tests proving the strip and verify layers use different mechanisms. **MAJOR, resolved:** the original shard instruction's "worth a reviewer's specific attention" threshold for `capital_priority.status: case_for_review` was this implementation's own gloss, not grounded in `TIER-0002`'s controlling text, and — because virtually every record in this risk/catalyst-rich corpus can satisfy "worth attention" under a permissive reading — produced a 25-of-27 near-uniform result. Corrected instruction re-grounded directly in `TIER-0002` §3.4's own quoted language ("does additional-capital-deployment consideration exist... relative to current policy and available alternatives?") and `docs/INVESTMENT_ONTOLOGY.md` §E's exact definition, explicitly naming `maintain_current_weight` as the expected default and restricting `case_for_review` to a genuinely distinctive, specific development materially different in kind or degree from ordinary disclosed risk — with an explicit instruction not to target any particular distribution and no exposure to the prior 25/2 result or to this repository's historical committee conclusions (which would themselves have been an improper answer key). All 27 tickers' `capital_priority` were redrafted from fresh, isolated shard sessions with zero access to the prior result, using the corrected instruction and (for the 8 contaminated tickers) the corrected sanitized evidence; economic_role was redrafted only for those same 8 contaminated tickers, since the sanitized evidence the other 19 saw is provably byte-identical to what an already-accepted-shape blind session already judged. **Corrected result: 17 of 27 `case_for_review`, 9 `maintain_current_weight` (COST, WM, KLAC, PANW, TSM, MSFT, V, AMZN, PWR), 1 `no_assessment` (SPGI, whose own evidence base is now majority-redacted after the sanitizer fix — 4 of 7 risks, 2 catalysts, 2 sources, and the full thesis narrative all excluded — correctly reflected as insufficient evidence rather than forced into either determination)** — a textual-similarity scan of all 26 non-`no_assessment` rationales found a maximum pairwise similarity of 13%, confirming genuine per-ticker differentiation, not templating. **MINOR, resolved:** the PR body's "33 files" corrected to the actual, independently reconfirmed **34** (unchanged by this correction, which modifies only already-tracked files — no new file was added). Full validation re-run clean at the corrected head: `classification_validator.py` OK (28 results); sanitizer + validator test suites 109/109 (up from 86, the 23 new regression tests); full `pytest` suite 2684/2684; `intelligence_validator.py`/`relationship_validator.py`/decision catalog/exactly-one-primary-workstream all unaffected; zero diff on every protected path. **No Milestone 7 comparison, no policy mutation, no tier/target/holdings/gate/cap/cluster/allocator/margin change of any kind in this correction either.** Requires its own fresh exact-head independent review before this PR may be considered ready.

  **Second bounded correction (same day, this PR, review `4868016198`).** A fresh full independent exact-head review of the first correction's own head (`025271629dbf2a03e15ab39ef225105bb24ab0f3`) confirmed the prior BLOCKING/MAJOR/MINOR findings genuinely fixed (bare-"gate" leakage reproduced-clean corpus-wide across all 53 companies, not just the 8 previously affected; the corrected 17/9/1 `capital_priority` distribution independently reproduced and read as well-justified; the 34-file count reconfirmed) but found three new MAJOR findings and two new MINOR findings, no BLOCKING, and confirmed **zero findings against the 27 sealed classification records themselves** — none of this round's corrections touch drafting shards, sanitized evidence packages, economic_role/capital_priority judgments, risk_concentration, evidence_quality, sealed content, hashes, or the cohort manifest. **MAJOR (classification_validator.py axis schemas were a denylist, not closed), resolved:** the reviewer's own direct execution showed `capital_priority.score`/`weight`/`recommendation`-shaped keys, and forbidden chart/policy/conviction content inside `risk_concentration.notes`/`economic_role.role_basis`/`evidence_quality.thesis_uncertainty_statement`, all validated cleanly — a fixed 8-name `_FORBIDDEN_ANSWER_KEY_FIELDS` denylist checked only `capital_priority.rationale`'s content, and `risk_concentration`/`evidence_quality` ran no content check at all. `classification_validator.py` v1.1 makes every axis genuinely closed-schema (explicit permitted-key sets derived from `TIER-0002`/`TIER-0004`'s controlling text plus a live scan of all 27 real records' actually-used keys, never invented) and extends the rationale-only content scan into one shared `_scan_free_text()` check applied to every free-text field on every axis, plus closes the cohort-manifest's own row and top-level shape (same gap, same fix). **MAJOR (no test covered the gap above), resolved:** direct regression tests reproducing all four of the reviewer's own live bypass examples, now confirmed rejected, plus explicit unknown-key tests for each axis, the top level, seal metadata, and both manifest shapes, plus positive tests proving optional fields (present or absent) and the abstention path still validate correctly. **MAJOR (the "genuinely independent second-stage verifier" claim was overstated for paragraph/item content), resolved:** traced directly and confirmed live — `_paragraph_is_forbidden()`/`_item_text_is_forbidden()` (the strip-decision functions) already called `independent_policy_scan()` as their own final check, so `verify_markdown_redaction()`/`complete_package_scan()` calling that same function again on text that had, by construction, already passed it once was a confirmatory re-check, not independent, for that content class (real independence held only for content the item pass never scans, e.g. bare `sector`/`industry`, and cross-paragraph-boundary composition). `intelligence_classification_sanitizer.py` v1.2 removes the `independent_policy_scan()` call from both strip-decision functions entirely — they now decide what to strip using only the phrase-pattern list (broadened with a bare `\bconviction\b` pattern, needed at strip time for real corpus correctness now that the fallback is gone) plus the shared, narrowly-scoped `_contains_gate_policy_leak()` checker (an explicit, disclosed exception: one leaf-level concept checker legitimately shared by both layers, not the verify-designated top-level function). `independent_policy_scan()` is now called *exclusively* by the two verify-designated functions, proven via AST-level structural tests (not a docstring/comment substring search, which would false-positive on this correction's own prose) that neither direction delegates to the other, plus a concrete test showing a phrase deliberately absent from the strip list (the percent-of-book numeric pattern) is still caught by the verifier. Re-run against the real 27-ticker corpus: zero regressions, all 27 pass, deterministic. **MINOR (stale full-suite count), resolved:** re-run at the corrected head in this environment: 2709 passed / 0 failed with `yfinance` installed (128 sanitizer+validator tests, up from 109 — includes `test_earnings.py`'s 6 tests, all passing here including the worktree-naming-dependent `test_real_repository_model_builds`, which happened to fail in the reviewing session's own differently-named checkout — an environment difference, not a defect); 2703 passed with `yfinance` unavailable (the same 6-test gap, entirely `test_earnings.py`, pre-existing, unrelated to this diff). **MINOR (recurring rhetorical closing-sentence pattern in `capital_priority` rationales), disclosed, not corrected:** a scan of all 26 non-`no_assessment` rationales found the pattern genuinely widespread (16 of 17 `case_for_review` closings, 4 of 9 `maintain_current_weight` closings match a templated skeleton), most visible in the reviewer's own AMZN/MSFT example; direct reading confirms the underlying facts cited in every rationale (AWS's Q2 2026 growth/margin figures vs. Azure's, distinct dollar figures, distinct dated events) remain specific and substantively differentiated per ticker even where the closing sentence's *structure* repeats — stylistic, not a templating defect, and not evidence of non-independent judgment. Per this correction's own authorized resolution path, disclosed here rather than used to justify editing sealed judgment content; worth tightening in a future batch's drafting instructions. Full validation re-run clean at this second-correction head: `classification_validator.py` OK (28 results); `classification_validator.py`+`intelligence_classification_sanitizer.py` test suites 128/128 (up from 109); zero diff on any `intelligence/classification/*.yaml` file or `COHORT_MANIFEST.yaml`; zero diff on every protected path; `intelligence_validator.py`/`relationship_validator.py`/decision catalog/exactly-one-primary-workstream all unaffected. **No Milestone 7 comparison, no policy mutation, no tier/target/holdings/gate/cap/cluster/allocator/margin change of any kind in this second correction either.** Requires its own fresh independent exact-head review before this PR may be considered ready.

  **Third bounded correction (same day, this PR, review `4868430051`).** An independent exact-head delta review of the second correction's own head (`e167834056b671f1066190a671633dec8d08d0ca`) confirmed all five items the second correction targeted genuinely resolved (bare-`conviction` reintroduced-at-strip-level clean, closed-schema bypasses all confirmed rejected, verifier independence confirmed for paragraph/item content, stale test count reconciled, rhetorical-template disclosure accepted as sufficient) and confirmed zero diff on any sealed classification record or the manifest — but found one new MAJOR and one new MINOR (no BLOCKING), both scoped to the sanitizer/documentation only. **MAJOR (no independent backstop for dangling section-title cross-references), resolved:** `_MD_FORBIDDEN_SECTION_HEADER_PATTERNS` (the whole-section-strip title list — "governed policy," "capital-priority discipline," "batch membership," etc.) was applied at paragraph level only inside the *strip* layer (`_paragraph_is_forbidden()`); none of those titles existed in `_INDEPENDENT_VERIFY_PATTERNS`, so a regression in that one strip-layer check, or a future record phrasing a reference slightly differently, had zero independent backstop — a narrower recurrence of the exact defect class the original BLOCKING and prior MAJOR findings were about. Fixed with a genuinely new mechanism rather than a static pattern-list addition: `_strip_forbidden_sections()` now returns, as a factual byproduct (never a decision function), the independently-normalized title of every section it actually removes; `_dangling_reference_findings()` (new) consumes that title list and independently re-implements reference detection — its own separately-authored title normalization (`_normalize_title_for_reference_matching()`), a word-boundary-anchored regex built per title at call time, searched against the complete assembled `.yaml`+`.md` package (catching a dangling reference surviving in a retained YAML `risks[]`/`catalysts[]` item, not just markdown prose) — and never calls, and is never called by, `_paragraph_is_forbidden()`/`_item_text_is_forbidden()`/`_strip_forbidden_sections()`/`independent_policy_scan()`, proven via AST-level call-graph tests. `SanitizedPackage`/`SanitizationRecord` gain a `removed_section_titles` field (titles only, never the stripped section's own prose) so the audit trail is reproducible. 12 regression tests directly matching the review's own required scenarios (quoted/unquoted/above/below/list-item/punctuation/multiple-titles/no-reference/word-overlap-without-phrase-match/verify-only-not-caught-by-strip/real-ETN/all-27-deterministic), plus 4 more proving the independent code path. **MINOR (PR body's "genuinely separate mechanisms" top-line claim overstated relative to the module's own more careful disclosure of the shared `_contains_gate_policy_leak()` exception), resolved:** corrected in the PR body only — CLAUDE.md's and `operations/WORKSTREAMS.yaml`'s own retained text for this PR were independently checked and found to already carry the caveat inline (see the second-correction paragraph immediately above: "an explicit, disclosed exception... not the verify-designated top-level function"), so neither required editing for this finding. Full validation re-run clean at this third-correction head: `classification_validator.py` OK (28 results, file unchanged this round); sanitizer test suite 123/123 (up from 76), sanitizer+validator combined 175/175 (up from 128); full `pytest` suite 2756/2756 in this environment; all 27 real packages pass the fail-closed scan deterministically across repeated runs; zero diff on any `intelligence/classification/*.yaml` file or `COHORT_MANIFEST.yaml`; zero diff on `classification_validator.py`/`test_classification_validator.py` (both untouched this round, as expected); zero diff on every protected path; `intelligence_validator.py`/`relationship_validator.py`/freshness suites (542/542)/decision catalog (95/95)/exactly-one-primary-workstream all unaffected. **No Milestone 7 comparison, no policy mutation, no tier/target/holdings/gate/cap/cluster/allocator/margin change of any kind in this third correction either.** Requires its own fresh narrow independent exact-head delta review before this PR may be considered ready.

  **PR #253 merged and post-merge verified.** Final exact-head delta review `4868793516` (anchored `e167834056b671f1066190a671633dec8d08d0ca` → `15da3585a48e4fc698efdae527eab24c2998ff18`): **DELTA APPROVED — APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE**, 0 BLOCKING / 0 MAJOR / 0 MINOR / 1 non-actionable NOTE (a pre-existing cosmetic `\d`-escape docstring warning). Independently confirmed the dangling-section-title-reference backstop (`_dangling_reference_findings()`) is a genuinely separate mechanism from the strip-decision functions via direct AST-level call-graph inspection, and that the PR body's independence wording was corrected to precisely match the module's own disclosed shared-helper exception. Principal acceptance `issuecomment-5197444291` at exact head `15da3585a48e4fc698efdae527eab24c2998ff18`. Merged via merge commit `7f2ffed80472057472ca17e39ed88ae0a0a2b77e` (parents `228b2dd0c6e68b205d99f1e6a4a0fc483df64a0e` and `15da3585a48e4fc698efdae527eab24c2998ff18`, merge-commit tree byte-identical to the accepted head's own tree — zero drift at merge). Post-merge verification `issuecomment-5197514201`: exact 34-file scope, 27/27 population, four-axis schema with no fifth axis on any record, exact **17 `case_for_review` / 9 `maintain_current_weight` / 1 `no_assessment` (SPGI)** `capital_priority` distribution, zero `economic_role` abstentions, exact 11-name `risk_concentration.unmeasured_flag` set (SNPS, PANW, ISRG, TMO, ICE, SPGI, V, COST, WM, RTX, RKLB, matching `REL-0007`), exact evidence-quality distribution (20 comprehensive / 1 partial — LLY / 6 limited — SNPS, ICE, SPGI, WM, RKLB, TSLA), 175/175 sanitizer+validator tests, 542/542 freshness, 95/95 decision-catalog, 2756/2756 full suite, protected-path isolation, merge-commit CI run `31047918222` `completed`/`success`. **This merge did not itself mark Milestone 6 formally complete** — PR #253's own post-merge-verification comment explicitly named a separate, later Lane G completion-determination filing (`TIER-0006`) as the required next step, matching the `REL-0004`→`REL-0006` (Milestone 4) and `PI-0031`→`PI-0037` (Milestone 3) precedent.

- **August 2026** — **`TIER-0006` accepted: WS-0005 Milestone 6 completion determination — MILESTONE 6 COMPLETE** (`governance/decisions/TIER-0006-ws0005-milestone6-completion-determination.md`). A bounded Lane G (`OPS-0009` §1) unit, on explicit principal authorization, independently re-verified PR #253's full four-round review/correction/principal-acceptance/merge/post-merge-verification chain against live GitHub state (not inferred from any prior summary) and independently re-derived, from the merged repository state itself, all eleven completion criteria this filing defines by combining `TIER-0002`/`TIER-0003`/`TIER-0004`/`TIER-0005`'s already-accepted controls (no separate `REL-0004`-style completion-standard filing existed for Milestone 6, so this filing both derives and evaluates the criteria in one unit — the same combined pattern `PI-0031` used for Milestone 3's own completion standard): population (27/27, zero drift); classification completeness/closed schema (no fifth axis, no score, on any of the 27 records); blindness integrity (the original BLOCKING bare-noun-"gate" leakage and the later dangling-section-title-reference gap both independently confirmed root-cause-fixed and holding at the final review, with the disclosed instructional — not filesystem-sandboxed — shard-isolation limitation accepted as an inherent, already-settled architectural property, not a blocker); judgment integrity/sequencing (judgment axes sealed before `risk_concentration`'s mechanical computation; the corrected 17/9/1 `capital_priority` distribution independently re-tallied exact, re-grounded in `TIER-0002` §3.4's own text after the original 25/2 distribution was found MAJOR-flagged for an ungrounded threshold, not for templating); structural `risk_concentration` computation (11-name unmeasured set independently recomputed exact, matching `REL-0007`); evidence-quality integrity (20/1/6 distribution independently retallied exact); sealing/reproducibility (all 27 `content_sha256` values independently reconfirmed via `classification_validator.py`'s own hash reconciliation, `COHORT_MANIFEST.yaml` bidirectionally consistent); validator/test coverage (`classification_validator.py` `OK (28 results)`, sanitizer 27/27, 175/175 sanitizer+validator tests, 53/2 Intelligence records, 13 relationship records, 95/95 decision-catalog, full suite 2756/2756, all independently re-run this session); review lifecycle (all four review rounds independently re-read in full, every BLOCKING/MAJOR/MINOR finding traced to its own resolution, merge-tree identity confirmed byte-identical to the accepted head); scope isolation (zero diff on every protected path, no Milestone 7 comparison anywhere in PR #253 or this filing); and repository/register synchronization (this filing's own §C updates). **Verdict: MILESTONE 6 COMPLETE.** Sets the `milestone-6-blind-classification` gate's `status` to `complete`, `pr: 253` in `operations/WORKSTREAMS.yaml`, effective only on this decision's own merge — `WS-0005`'s top-level `status` remains `in_progress` since Milestone 7 (baseline reconciliation) and every later milestone remain unauthorized. **Explicitly, repeatedly does not authorize, begin, schedule, or imply Milestone 7 or any later WS-0005 milestone**, does not unblind or compare any sealed classification record against current `target_pct`/`portfolio_role_ref`/tier/gate/cluster membership, does not edit any of the 27 sealed records or the cohort manifest, and does not change any tier/target/holdings/gate/cap/cluster/allocator/margin/ladder/chart/order/trade behavior. **Completion of Milestone 6 does not imply portfolio-policy agreement with, or adoption of, any sealed record's own judgment** — the 27 records remain sealed advisory evidence pending a separately authorized future Milestone 7. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge. **PR #254 merged** — accepted head `306e1a0f716d2fd8d515f1d0496b81ca30af93f4`, merge commit `1107c5b70801ff5e7027efddf6a2aa916030dce2`, independent review `4869309702` (APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE, 0/0/0/1 NOTE), principal acceptance `issuecomment-5198112358`, post-merge verification `issuecomment-5198142601`, merge-commit CI `31052845524` `completed`/`success`. **MILESTONE 6 IS NOW FORMALLY COMPLETE** — the 27 sealed blind classifications are accepted as Milestone 6's completed evidence artifact; this does not constitute portfolio-policy adoption of any classification.
- **August 2026** — **TIER-0007 proposed (not yet accepted): WS-0005 Milestone 7 baseline-reconciliation definition and authorization** (`governance/decisions/TIER-0007-ws0005-milestone7-baseline-reconciliation-authorization.md`). Also records, as a Lane M factual synchronization folded into this same filing: `TIER-0006` (PR #254) independently re-confirmed merged (merge commit `1107c5b70801ff5e7027efddf6a2aa916030dce2`), independently reviewed (`4869309702`, APPROVED FOR PRINCIPAL EXACT-HEAD ACCEPTANCE, 0/0/0/1 non-actionable NOTE), and principal-accepted at its exact head `306e1a0f716d2fd8d515f1d0496b81ca30af93f4` (`issuecomment-5198112358`, explicitly scoped to *not* authorize Milestone 7), with post-merge verification (`issuecomment-5198142601`) confirming merge-tree identity, all eleven Milestone 6 completion criteria, and merge-commit CI (`31052845524`, `completed`/`success`) — its own decision-file/index `status: Proposed` remains a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. **Milestone 6 is therefore formally complete.** On explicit principal authorization ("one small, coherent Lane G governance filing that defines and authorizes the future WS-0005 Milestone 7 baseline-reconciliation implementation... must not perform the reconciliation itself"), `TIER-0007` binds a full specification to the already-existing `milestone-7-baseline-reconciliation` gate text (quoted, not restated) rather than inventing new milestone scope, matching `REL-0001`'s (Milestone 4) and `TIER-0001`'s (Milestone 5) identical "define, then later authorize implementation" pattern. Defines: Milestone 7's purpose (unblind the 27 sealed Milestone 6 records and compare each against the current governed baseline on role, capital priority, target/tier context, structural risk, and evidence quality — analysis only, no policy recommendation or adoption, explicitly deferred to a separately authorized future Milestone 8); mandatory sealed-record integrity checks before any future unblinding (re-run `classification_validator.py`; recompute all 27 `content_sha256` values; reconcile `COHORT_MANIFEST.yaml` bidirectionally; confirm every record's `lifecycle_status: sealed`; confirm no drift since `TIER-0006` via `git diff`; record the exact comparison-source merge SHA) — `intelligence/classification/*.yaml` and `COHORT_MANIFEST.yaml` are immutable evidence inputs, never edited by Milestone 7, with any discovered defect disclosed as a Milestone 7 finding rather than silently corrected; the permitted comparison-input list (`targets.yaml`, `gates.yaml`, current `portfolio_role_ref`/tier context, `holdings.yaml` as descriptive-only current-state evidence — never a new-target input, `issuer_lookthrough.yaml`, `intelligence/relationships/`, Company/Theme Intelligence, current review/evidence-quality state, controlling governance decisions), with mandatory per-claim labeling separating blind-classification conclusion / current baseline policy / factual current portfolio state / governing constraint / reconciliation analysis / unresolved evidence; a fundamentals-and-structure-only chart-evidence boundary (`TIER-0003`'s exclusion of chart evidence from blind classification is not reopened, narrowed, or reinterpreted — charts remain a separate downstream advisory layer under `WS-0012`/`WS-0013`); a required sixteen-field per-ticker output schema (sealed vs. current for each of `economic_role`/`capital_priority`/`risk_concentration`/`evidence_quality`, plus disposition, supporting evidence, uncertainty, and required later governance action — explicitly no new target percentage, score, or ranking); a closed six-value disposition vocabulary (`aligned`, `divergence_requires_review`, `baseline_assumption_stale`, `unresolved_evidence`, `structural_measurement_gap`, `no_policy_conclusion`); required aggregate outputs (counts, recurring mismatches, structural-risk coverage gaps cross-checked against `REL-0007`'s 11-name `unmeasured_flag` finding, evidence-quality limitations, portfolio-level patterns, unresolved questions, potential future policy topics — analytical findings only, never recommendations); a no-retroactive-rewrite rule (any proposed correction to a sealed Milestone 6 record requires its own separate governed process, never performed inside a Milestone 7 unit); and an explicit, enumerated non-authorization boundary covering every target/tier/role/gate/holdings/cap/cluster/issuer-lookthrough/allocator/margin/ladder change, chart-evidence use, buy/sell/hold/trim/wait/stage/sizing instruction, order, Milestone 8/9 work, and live-or-scenario allocation-check execution. **Authorizes exactly one later, separate, bounded Milestone 7 implementation PR** covering all 27 canonical equities in one coherent retained reconciliation artifact (or a small logically partitioned set — never one PR per ticker), effective only after this governance decision's own independent review, principal acceptance, merge, and post-merge verification; internal read-only shards may be used for drafting efficiency, but only one primary authoring session may mutate the repository, and the future PR still requires its own full independent-review/correction/re-review/principal-acceptance/merge/post-merge-verification lifecycle. **The `milestone-7-baseline-reconciliation` gate's own `status: proposed` is deliberately left unedited by this filing** — matching `REL-0001` §K's identical treatment of the `milestone-4-portfolio-relationship-mapping` gate and `TIER-0001`'s identical treatment of the `milestone-5-...` gate: a schema/definition/authorization filing that performs no substantive milestone content is not itself "the milestone beginning." A new, distinctly named gate, `tier0007-milestone7-baseline-reconciliation-authorization`, tracks this filing's own progress instead — `status: in_progress`, `pr: null`, since this filing's own governance PR is itself unmerged, following `TIER-0001`'s own corrected precedent that a filing may not mark its own unmerged work `complete`. Adds two additive `WS-0005` gate entries (`operations/WORKSTREAMS.yaml`) — `tier0006-post-merge-verification` (recording `TIER-0006`'s confirmed merge/review/acceptance state without editing its own prior gate text) and `tier0007-milestone7-baseline-reconciliation-authorization` — and updates the workstream's `active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date` self-reference fields. **Authorizes no reconciliation content, no unblinding of any sealed record, no ticker-level current-policy comparison, no Milestone 8 or 9 work, and no tier/target/role/gate/holdings/cap/cluster/issuer-lookthrough/allocator/margin/ladder/chart/order/trade change of any kind.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.

  **Bounded correction (same day, this PR, review `4869718735`).** Independent exact-head review of the first commit (anchored to `eac680ef8ba3e11592181b6e7063b7d90566e6f0`) returned CHANGES REQUIRED — 0 BLOCKING / 2 MAJOR / 1 MINOR. **MAJOR 1** — the six-value disposition vocabulary above required "exactly one" value with no precedence rule for a ticker qualifying for more than one category simultaneously (a real risk: 11 of 27 tickers already carry `risk_concentration.unmeasured_flag: true`, several of which also independently support a substantive role/capital-priority finding). **Resolved**: the flat six-value vocabulary is superseded by a **primary disposition** (exactly one, closed four-value vocabulary, deterministic precedence — `baseline_assumption_stale` > `divergence_requires_review` > `aligned` > `no_policy_conclusion`; `aligned` explicitly does not mean "maintain current policy," "approved," or "no future change"; `no_policy_conclusion` is not a default/catch-all) plus a **closed secondary-condition flag set** (`unresolved_evidence`, `structural_measurement_gap`; zero to two, independent of the primary value, never a substitute for a reachable primary finding). Aggregate outputs now report primary-disposition and secondary-flag counts separately, plus a "both flags" count — never merged or double-counted. **MAJOR 2** — the controlling gate text's "target-vs-evidence-supported range" phrase, cited above as fully operationalized by the sixteen-field schema, was never actually given a corresponding field — independently confirmed the sealed Milestone 6 evidence (`TIER-0002`'s four-axis framework) contains no numeric range, band, or interval anywhere. **Resolved**: adds a categorical-only `target_context_comparison` field (`categorically_consistent` / `categorically_divergent` / `unable_to_determine` + required non-numeric `rationale`), explicitly distinct from the capital-priority comparison and explicitly barred from ever proposing, estimating, implying, or backsolving a numeric target percentage or range. **The per-ticker schema above described as "sixteen fields" grows to eighteen** (the target-context field, plus splitting one flat "disposition category" field into `primary_disposition`/`secondary_conditions`) — the "sixteen-field"/"six-value disposition" language two paragraphs above is **superseded**, not current, preserved only as an accurate record of what the first commit originally defined. **MINOR** — the `tier0007-...` gate's own `pr:` field, described above as `null`, is **resolved**: `operations/WORKSTREAMS.yaml` now records `pr: 255` on that gate directly, matching the `TIER-0003`/`TIER-0005` precedent; `status` remains `in_progress`. No reconciliation performed, no ticker unblinded, no sealed record or manifest touched, no Milestone 8 authorized in this correction. Full validation re-run clean: `classification_validator.py` OK (28); `relationship_validator.py` OK (13); decision catalog 81/`issues == ()` (unchanged); 27-name sealed population unchanged; focused suite 487/487; full pytest 2756/2756; `git diff --check` clean; zero diff on every protected path. Corrected head `6f934957ca88d1db670eaf3c57a4f509f4bc2dd2`; CI `completed`/`success`. Requires its own fresh independent exact-head delta review before this PR may be considered ready.

  **Second bounded correction (same day, this PR, review `4869945654`).** Independent exact-head delta review of the corrected head (`6f934957ca88d1db670eaf3c57a4f509f4bc2dd2`) confirmed all three findings from the first correction genuinely resolved inside the decision file and `operations/WORKSTREAMS.yaml`, with zero regression on any previously-confirmed section, but found one new MAJOR and one new MINOR. **MAJOR (Finding A)** — the corrected eighteen-field/primary(4)+secondary(2)-disposition/categorical-target-context design was never propagated into the two other places in this same PR that restate the schema in prose: `operations/WORKSTREAMS.yaml`'s `tier0007-...` gate description (still read the superseded sixteen-field/six-value text) and **this very `CLAUDE.md` entry** (same stale schema language, plus `pr: null` directly contradicting `operations/WORKSTREAMS.yaml`'s own already-corrected `pr: 255`) — a live, present-tense self-contradiction between two files at the same head, and exactly the kind of inconsistent-specification risk this filing's own Rationale states it exists to prevent. **Resolved**: this paragraph and the one above it correct the record without rewriting the original entry's prose (preserved verbatim above as an accurate historical record of what the *first* commit defined); `operations/WORKSTREAMS.yaml`'s `tier0007-...` gate description gains a parallel additive paragraph stating the same corrected design. **The per-ticker schema authorized by `TIER-0007` is, as of this correction, eighteen fields — not sixteen — with a four-value primary disposition, a two-value secondary-condition flag set, and a categorical (never numeric) `target_context_comparison`; the `tier0007-milestone7-baseline-reconciliation-authorization` gate records `pr: 255`, not `null`.** `milestone-7-baseline-reconciliation` remains `status: proposed`, `pr: null`, untouched — Milestone 7 implementation has still not begun. **MINOR (Finding B)** — `categorically_consistent` carried no explicit guard against being misread as "the target is confirmed correct," the same shape of risk `aligned` was explicitly guarded against above. **Resolved**: the decision file's `categorically_consistent` bullet now states explicitly that it means only that the existing target/tier context is not categorically inconsistent with the sealed evidence — it does not mean the target is approved, optimal, should be retained, needs no policy review, or that Milestone 8 has adopted it, and carries no policy authority of its own; `categorically_divergent`'s own bullet is likewise clarified as analytical only, naming a divergence without directing an increase, decrease, or replacement figure. No reconciliation performed, no ticker unblinded or inspected, no sealed Milestone 6 record or manifest touched, no Milestone 8 authorized, no portfolio-policy or production change in this correction. Full validation re-run clean: `classification_validator.py` OK (28); `relationship_validator.py` OK (13); `freshness_validator.py` OK; decision catalog 81/`issues == ()` (unchanged — no new decision, no metadata change); 27-name sealed population and `COHORT_MANIFEST.yaml` unchanged; focused suite 487/487; full pytest 2756/2756; `git diff --check` clean; zero diff on every protected path; exactly one primary workstream (`WS-0005`). Exact correction-delta inventory this round: `governance/decisions/TIER-0007-...md`, `operations/WORKSTREAMS.yaml`, and — for the first time in this PR — this `CLAUDE.md` entry; `governance/decisions.yaml` and `test_portfolio_hq_dashboard_decisions.py` unchanged (no metadata field or decision count changed). Requires its own fresh independent exact-head delta review before this PR may be considered ready.
- **August 2026** — **CONTENDER-0001 proposed (not yet accepted): universal contender-universe principle** (`governance/decisions/CONTENDER-0001-universal-contender-universe-principle.md`). Filed alongside `XASSET-0001` (its own separate decision file, same PR) on explicit principal authorization for one small, coherent Lane G governance PR containing two coordinated decision records — architecture and sequencing only. Independently re-verified this session, not assumed: `main` at `f71ea3bb1428445023c4fa582ed953ae409ba070` (PR #255/`TIER-0007`'s own merge commit), zero open PRs, PR #255's full three-round-review/principal-acceptance/merge/CI lifecycle, the 81-decision catalog with zero issues, `WS-0014` as the next unused workstream identifier, and `CONTENDER-####`/`XASSET-####` as genuinely new, unused decision-prefix domains per `governance/decisions/README.md`'s own rule. **Establishes**: the 27 sealed Milestone 6 equity classifications (`TIER-0006`) are Portfolio-HQ's first completed blind-classification cohort, not the permanent or exhaustive contender universe; every genuine, valid investable ticker represented anywhere in the repository — as a current holding, target, gated name, retained/historical Company Intelligence record, comparator, relationship/theme reference, chart-covered ticker, prior holding, disclosed external-opportunity lead, or other governed project reference — is eligible for future, separately authorized contender screening, with current holding/target/tier/gate/classification/canonical-population status never by itself determining inclusion or exclusion; **contender status creates evaluation eligibility only — no holding, target, tier, allocation, policy, order, or trade authority**; raw ticker-string occurrence is not proof of investability — a future normalization unit must disposition every candidate string into genuine-investable / test-fixture / benchmark-reference / alias-or-brokerage-variant / malformed / obsolete-or-delisted / synthetic-row, generalizing the repository's own confirmed WDC/Sandisk (`PI-0032`) precedent rather than treating it as a one-off. Assigns future contender-registry creation, normalization execution, and research-readiness screening to the new `WS-0014` workstream (authorized by `XASSET-0001` §I, not by this filing) — not `WS-0005` (whose Milestone 3-7 scope remains the sealed 27-equity cohort, unaffected) and not `WS-0013` (non-authorizing). **Authorizes no contender registry, no ticker normalization execution, no new ticker research of any kind, no revival of any historical or delisted candidate, no additional blind classification, no ETF or crypto research/classification, and no target/tier/holdings/gate/cap/cluster/allocator/margin/ladder/chart/order/trade change of any kind.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **XASSET-0001 proposed (not yet accepted): cross-asset whole-portfolio allocation architecture** (`governance/decisions/XASSET-0001-cross-asset-whole-portfolio-allocation-architecture.md`). Filed alongside `CONTENDER-0001` (same PR, same authorization). Preserves Portfolio-HQ's governing objective unchanged (maximize long-term compounding/capital appreciation within approved risk, liquidity, concentration, leverage, and survival constraints — never silently overriding reserve requirements, concentration limits, liquidity, drawdown survivability, the 1.8x leverage cap, the 30% buffer floor, or evidence uncertainty). **Requires**, as future doctrine, not designed or executed by this filing: asset-appropriate blind classification — additional equities reuse the existing `TIER-0002` four-axis framework, while ETFs and cryptocurrency each require their own separately governed, asset-appropriate framework (never forced into the company-shaped Company Intelligence/`TIER-0002` schema), and cash/reserve/GLD/debt reduction require governed functional doctrine as competing uses of capital rather than company-style classification; a two-level whole-portfolio allocation architecture — **Level 1** sleeve allocation among individual equities, ETFs, cryptocurrency, GLD/defensive assets, cash/reserve, and debt/leverage reduction, and **Level 2** instrument allocation inside each approved sleeve — with final allocation required to compare opportunity cost across all governed sleeves, and final instrument targets explicitly barred from being declared globally optimal merely by allocating among the existing 27 equities; future overlap/concentration accounting (ETF look-through, cluster/issuer duplication, geographic/currency exposure, crypto cross-correlation, volatility/drawdown concentration, liquidity, cross-sleeve duplicated exposure, margin/debt interaction) extending, not duplicating, `issuer_lookthrough.yaml`/`caps.clusters`/`intelligence/relationships/` — none of it implemented here. **Restates unchanged**: the fundamentals-first chart boundary (`TIER-0003` not reopened — charts remain downstream advisory, never automatically changing membership/tier/sleeve/instrument target/cap/trade). **Binds new requirements to Milestones 7-9's existing, unedited gate text**: every future Milestone 7 artifact must disclose that its reconciliation covers the 27 canonical equity destinations only and that ETF/crypto/cash-reserve/GLD/debt-reduction/broader-contender-universe allocation remain governed separately and unconcluded; Milestone 8 must label any 27-equity-cohort result as equity-scoped and cannot claim final whole-portfolio target readiness before the cross-asset prerequisites below complete; Milestone 9 does not silently convert an equity-only finding into final whole-portfolio policy. `TIER-0007` itself is not modified beyond this additive disclosure requirement. **Authorizes creation of `WS-0014`** ("Contender Normalization and Cross-Asset Synthesis," `status: proposed`, `priority: secondary`, `WS-0005` remaining the sole `priority: primary` workstream, `authorized_scope: "none... no execution unit authorized"`) to own contender normalization, ETF/crypto framework design and classification, cash/reserve/GLD/debt doctrine, overlap/concentration modeling, cross-asset synthesis, sleeve- and instrument-level candidate targets, chart-informed deployment integration (within already-approved policy only), and a final independent audit — a fourteen-item scope list, none of it authorized to begin by this filing, each requiring its own future, separate, explicit principal authorization. Records a fourteen-step dependency-ordered roadmap (this filing → contender normalization/readiness screening → additional-equity cohorts → ETF/crypto framework design → ETF classification → crypto classification → cash/GLD/debt doctrine → overlap/concentration modeling → cross-asset synthesis → sleeve-level targets → instrument-level targets → chart-informed deployment → final independent audit → convergence into `WS-0013`), permitting narrow future batching only where a genuine common mechanism exists (matching `OPS-0008`'s Research Wave Protocol discipline) while requiring separate lifecycle units for framework design vs. classification execution, ETF vs. crypto classification, every completion determination, sleeve-level vs. instrument-level targets, and the final audit. Adds one additive, non-authorizing `WS-0013` gate (`xasset0001-cross-asset-dependency-cross-reference-recorded`) recording `WS-0014` as a required future dependency of `WS-0013`'s own final-allocation-readiness objective alongside its existing `WS-0005`/`WS-0012` dependencies — no change to `WS-0013`'s own `status`, `priority`, `authorized_scope`, `prohibited_scope`, `dependencies`, or `completion_criteria` fields; `WS-0013` remains the non-authorizing final allocation-readiness orchestration and dependency ledger and does not become an execution workstream. **Authorizes no contender registry, no ticker normalization, no new ticker research, no additional blind classification, no ETF or crypto research/classification, no cash/reserve/GLD/debt doctrine content, no overlap/concentration/correlation model implementation, no sleeve- or instrument-level sizing of any kind, no target/tier/holdings/gate/cap/cluster/allocator/margin/ladder/chart/order/trade change, and no allocation check, live or scenario.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **CONTENDER-0002 proposed (not yet accepted): WS-0014 item 1 authorization — contender normalization plus research-readiness screening** (`governance/decisions/CONTENDER-0002-contender-normalization-and-readiness-screening-authorization.md`). Also records, as a Lane M factual synchronization folded into this same filing: PR #256 (`CONTENDER-0001` + `XASSET-0001`) independently re-confirmed merged (merge commit `42949907558afe1577a74e3562e777ba62469d66`, tree byte-identical to its accepted head `7279f1e31046c8a00c07c38b51ff42b5a406e6d1`, zero drift at merge), principal-accepted (`issuecomment-5199829133`) and post-merge verified (`issuecomment-5199866504`, 83 decisions/`issues == ()`, 754/754 focused + 2756/2756 full `pytest`, zero diff on every protected path) — both decision files' own `status: Proposed` remain a known, pre-existing, out-of-scope state matching the established two-step acceptance-recording pattern, not corrected here. On explicit principal authorization ("I authorize one bounded Lane G governance filing that authorizes the first WS-0014 execution unit: contender normalization plus research-readiness screening... The authorized implementation must remain screening and inventory only. It may not research, classify, rank, recommend, revive, target, tier, allocate, buy, trim, sell, or trade any asset"), `CONTENDER-0002` extends the `CONTENDER-####` domain (independently confirmed the correct choice over minting a new prefix, matching the `REL-0001`→`REL-0002`/`TIER-0001`→`TIER-0002`/`CHART-0001`→`CHART-0002` precedent) and additively defines `XASSET-0001` §J's dependency-order step 1 — "genuinely one evidence-gathering pass" — **authorizing definition and future-implementation authorization only; it performs no scan, builds no registry, and researches nothing itself.** Specifies for a later, separate implementation PR: fourteen source locations to scan (`targets.yaml`, `gates.yaml`, `holdings.yaml`, `issuer_lookthrough.yaml`, all 53 Company Intelligence records, both Theme records, all 13 relationship records, the 27 sealed classification records as **read-only reference only, never rescanned as new content**, governance decision prose, batch-comparison artifacts, retained audits, `decision_log.yaml`, `earnings.py`'s `_YAHOO_SYMBOL` alias map, and the governed chart-evidence library — `test_*.py` files explicitly excluded as a candidate source); a canonicalization rule anchored to `targets.yaml`'s own destination symbol, reusing the existing alias map rather than inventing identity resolution; a **closed twelve-value screening-disposition vocabulary** (`evaluation_ready`, `requires_research`, `requires_freshness_review`, `requires_identity_resolution`, `insufficient_evidence`, `duplicate_or_alias`, `benchmark_or_index`, `synthetic_or_test_fixture`, `stale_or_superseded`, `non_investable`, `explicitly_deferred_or_excluded`, `abstain_pending_human_decision`) with a required per-entry field set (symbol, asset type, provenance, investability/research/freshness/classification/current-policy status, existing gate/disposition, duplicate link, reason, review trigger, next required action); a strict **mechanical-versus-judgment boundary** (symbol occurrence, file provenance, existing field values, the known alias map, and each record's own already-computed freshness status are mechanical; any new investment-merit, identity, or research judgment is prohibited — unresolvable cases abstain, never get forced into a more definite-sounding category); a **hybrid output design** — one retained narrative audit artifact under `governance/audits/` plus one small, structured, committed registry (e.g. `intelligence/contenders/registry.yaml`) validated by a new, narrowly-scoped `contender_registry_validator.py` carrying zero import coupling with `allocate.py`/`margin_state.py` — preferred over a purely regenerated report (the `PI-0011` staleness-report pattern) because this unit's output includes point-in-time editorial abstentions, not just re-derivable facts; and explicit stop conditions (ambiguous fixture/reference distinction, an alias mapping to more than one live security, unverifiable investability, conflicting sources, scope-expanding history recovery, any point requiring a new policy judgment, or a competing mutation lane). **Legacy-ticker provenance** (the ~41 previously-tracked, currently-untracked `holdings.yaml` tickers `PHQ-2026-02`'s own reconciliation history discloses but never individually enumerates) is handled conditionally, not guessed: this session directly ran `git rev-parse --is-shallow-repository` and found its own working clone **is a shallow clone**, oldest reachable commit dated 2026-07-31 (the same calendar date `PHQ-2026-02` was accepted) — meaning the pre-migration `holdings.yaml` history is not mechanically reachable from this environment as verified. The future implementation must independently re-verify its own environment's clone depth before attempting recovery, and if history proves unreachable or recovery raises identity ambiguity beyond the mechanical alias rule, **stop and disclose** — legacy-ticker recovery becomes its own later, separately authorized sub-unit, never silently folded in or dropped. Adds one additive `WS-0014` milestone entry (`operations/WORKSTREAMS.yaml`, gate `contender0002-normalization-and-readiness-screening-authorization`, `status: in_progress`, `pr: null` — not marked complete, consistent with this repository's own established discipline that a filing may not mark its own unmerged work complete) and updates `WS-0014`'s `authorized_scope`/`blocker`/`next_action`/`active_branch`/`active_pr`/`last_verified_main_sha`/`last_verified_date`/`authorized_by` fields to record this filing's own bounded scope — `WS-0014`'s own `status` (`proposed`), `priority` (`secondary`), and `dependencies` (`[WS-0005]`) are unedited; no `WS-0013` field is touched. **Authorizes no scan, no registry, no research on any ticker, no classification/ranking/scoring, no revival of any historical/deferred/delisted candidate, no additional equity blind classification beyond the sealed 27, no ETF/crypto research or classification, no target/tier/holdings/gate/cap/cluster/allocator/margin/ladder/chart/order/trade change, no allocation check of any kind, and none of `WS-0014`'s remaining thirteen scope items.** Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
- **August 2026** — **WS-0014 item 1 implementation, draft, not yet accepted** (no new governance decision — `CONTENDER-0002`, merged via PR #257 at merge commit `66ddae32c276c5abd962ed4f6b130e9b567b320d`, is the sole authorization; this filing is the implementation it authorized). Independently re-confirmed `CONTENDER-0002`'s merge, independent delta review (`pullrequestreview-4872945635`, DELTA APPROVED), principal acceptance (`issuecomment-5203295541`), and post-merge verification (`issuecomment-5203363267`) before proceeding — zero open competing PRs, working tree clean. Delivers `intelligence/contenders/registry.yaml` (84 normalized identities), `contender_registry_generator.py`, `contender_registry_validator.py`, their test suites (74 focused tests), and one retained narrative audit (`governance/audits/WS0014_CONTENDER_NORMALIZATION_AND_READINESS_SCREENING_20260806.md`) under the exact scope `CONTENDER-0002` §A-§M bound. Sixteen §B source categories scanned as specified, structured sources parsed programmatically, prose sources (governance decisions, audits, batch-comparison artifacts, `decision_log.yaml`, governed research protocols, `operations/WORKSTREAMS.yaml`) via a deterministic ticker-shaped-token regex. **Disclosed 17th source, not one of §B's own sixteen**: `CLAUDE.md` itself, added on the reasoning that §B's own "at minimum" framing and `CONTENDER-0001` §A's explicit "a prior holding" eligibility language would otherwise silently miss three genuine prior/removed portfolio instruments (**VMC**, **LHX** — both full exits; **HYPE** — removed from crypto targets) and four permanently-ignored dust-coin symbols (**ZORA/WIF/BONK/PEPE**) that exist nowhere else in the repository's committed text. 449 raw prose tokens found; 83 already-anchored (corroboration only), 22 in a small, individually cited curated table (13 of `PI-0033`'s fourteen deferred names minus the four already Intelligence-covered; `QQQ` per `CONTENDER-0002`'s own tier-2 worked example; `SNDK` per its own tier-5 worked example; VMC/LHX/HYPE/ZORA/WIF/BONK/PEPE per the CLAUDE.md-only citations above), 365 auto-excluded as repository-specific acronym/decision-ID/incidental-counterparty-mention false positives (e.g. `TSMC`, `QCOM`, `ARM`, `HPE`, `KKR` — all incidental company-name mentions inside an already-governed record's own narrative, not comparator references) — every one of the 449 accounted for, none silently dropped, per `CONTENDER-0002` §J's bidirectional-reconciliation requirement (independently verified: 0 errors). **Disposition distribution, mechanically assigned via the exact twelve-value §E.2 precedence order**: `synthetic_or_test_fixture` 2 (CASH, RESERVE); `benchmark_or_index` 1 (QQQ); `explicitly_deferred_or_excluded` 20; `requires_research` 8 (SPY/VEA/VWO/GLD/BTC/ETH/SOL — no ETF/crypto framework exists yet per `XASSET-0001` — plus SNDK); `requires_freshness_review` 8 (CEG plus seven retained names — AAPL/ABBV/BRK.B/CVX/LRCX/MA/MRK — each carrying a lapsed pending catalyst per `intelligence_report.collect_staleness_findings`, read not recomputed); `insufficient_evidence` 6 (`SNPS`/`ICE`/`SPGI`/`WM`/`RKLB`/`TSLA` — `PI-0038`'s own disclosed blanket access-failure finding); `evaluation_ready` 39; zero `non_investable`/`duplicate_or_alias`/`stale_or_superseded`/`requires_identity_resolution`/`abstain_pending_human_decision` this generation. **§E.4's own RKLB/TSLA-vs-GNRC/RTX worked example independently reproduced against live repository state exactly**: RKLB/TSLA land on `insufficient_evidence` (`has_current_gate: true`, `has_prior_deferral_superseded: true`); GNRC/RTX share the same supersession reasoning but land on `evaluation_ready` (`has_current_gate: false`, `has_prior_deferral_superseded: true` preserved regardless per §E.3's independence rule). **WDC/SNDK spinoff worked example (§E.2 tier 5) independently reproduced**: WDC never lands on `stale_or_superseded`; SNDK (no prior identity of its own) lands on `requires_research`. `BRK.B`/`BRK-B` resolved to one canonical row via `earnings.py`'s own alias map, never double-counted. **Legacy-ticker gap (§G)**: this session's own working clone independently re-confirmed shallow (oldest reachable commit 2026-07-31); the ~41 pre-`PHQ-2026-02` `holdings.yaml` tickers remain unrecovered; the exact §G gap record was written (`registry_entries_created: 0`) — zero placeholder rows for any unrecovered symbol. **27-cohort/Intelligence isolation**: `intelligence/classification/`, every Company/Theme/Relationship Intelligence record, and every protected path (`targets.yaml`, `holdings.yaml`, `gates.yaml`, `issuer_lookthrough.yaml`, `allocate.py`, `margin_state.py`, `levels.py`) confirmed byte-identical before and after generation, independently verified by a dedicated test. Full validation: `contender_registry_validator.py` `OK (84 entries)`; `classification_validator.py` `OK (28 results)`; `relationship_validator.py` `OK (13 records)`; `intelligence_validator.py` clean; `freshness_validator.py` `OK`; decision catalog unchanged at 84 decisions/`issues == ()` (no new governance decision filed by this implementation); regeneration independently confirmed byte-identical (excluding the timestamp field) across two runs against the same source commit; full repository `pytest` suite 2830 passed, 0 failed (2756 pre-existing baseline plus 74 new focused tests), 1 pre-existing unrelated deprecation warning; `git diff --check` clean; zero diff on every protected path; exactly one `priority: primary` workstream (`WS-0005`). **No ticker was researched, classified, ranked, recommended, revived, targeted, tiered, allocated, bought, trimmed, sold, or traded by this filing** — this is inventory, never policy (`CONTENDER-0002` §I/§M). `WS-0014` items (3)-(14) (`XASSET-0001` §J steps 2-13) remain exactly as unauthorized as `CONTENDER-0002` left them. This session does not review its own PR, mark it ready, merge it, or post principal acceptance. Draft PR; not merged, not independently reviewed, not principal-accepted in this session — effective only on merge.
  **Bounded correction (same day, this PR):** exact-head CI on the original head (`481948c...`) failed — **2829 passed, 1 failed**, `test_this_environment_is_a_shallow_clone` hardcoded `assert out == "true"`, false under GitHub Actions' `fetch-depth: 0` checkout. Root cause ran deeper: `legacy_gap.recovery_status` was a hardcoded string, never actually computed from a live clone-depth check. **First correction** (head `6c22aab...`): added live `detect_clone_depth()` (injectable, normalizes to `"shallow"`/`"complete"`) and replaced the hardcoded test with dependency-injected coverage of both branches — CI reconfirmed green, **2844 passed, 0 failed**. **Independent exact-head review** (`pullrequestreview-4874631727`, anchored to `6c22aaba718c38c21ad580e99f717cefea12844d`) then returned CHANGES REQUIRED — 0 BLOCKING / 2 MAJOR / 2 MINOR / 1 NOTE — confirming population/dispositions/worked cases/protected-path isolation all correct, but finding: (1) the registry schema's "closed, no smuggled policy content" guarantee only checked missing keys at three levels (entry/header/`legacy_gap`), never extra — a `conviction_score`/`recommended_target_pct` key would have silently passed; (2) `legacy_gap`'s complete-clone branch unconditionally skipped `CONTENDER-0002` §G step 2's own **mandatory** recovery-attempt language ("if full history is reachable ... attempt a bounded, mechanical diff") for every complete-clone environment, including this repository's own CI; (3)/(4) the PR body and CLAUDE.md itself were stale post-first-correction. **Second correction** (this paragraph's own head): closed all three schema levels against extra keys via one shared helper mirroring the existing `secondary_flags` pattern (regression tests for `conviction_score`, `recommended_target_pct`, an arbitrary header key, an arbitrary `legacy_gap` key); implemented genuine §G step 2 compliance — `perform_legacy_recovery()` now always attempts the bounded `holdings.yaml` diff across the mechanically-located `PHQ-2026-02` reconciliation commit when history is reachable, recovering real, git-provenanced, `requires_research`-dispositioned entries (routed through the same §E.2 precedence engine as every other identity, with a collision guard against duplicating an already-tracked symbol) or failing closed to an honest `recovery_ambiguous` (zero entries) whenever the commit/parent/diff can't be mechanically established — verified via nine dependency-injected unit tests, since this session's own clone remains genuinely shallow and cannot exercise the real-history path directly (it will run for real against this repository's actual complete history the next time CI executes this same test suite). `legacy_gap.recovery_status` gains a dynamic `"recovered_<N>_of_41"` shape, validated by pattern rather than fixed value; the now-superseded `"reachable_but_recovery_not_attempted_this_generation"` value is removed from the closed vocabulary rather than left as dead code. This environment's committed `registry.yaml` is unaffected in substance (still 84 entries, still `clone_depth_at_generation: shallow`). 24 new focused tests added (112 total in these two files, up from 88 at the reviewed head). Full repository `pytest`: **2868 passed, 0 failed** (2844 prior + 24 new). No ticker research, no disposition change beyond what mechanical §G recovery itself produces, no change to `CONTENDER-0001`/`XASSET-0001`/`CONTENDER-0002`. This session does not review its own PR, mark it ready, merge it, or post principal acceptance — awaiting terminal exact-head CI and a fresh independent exact-head delta review.
  **Second bounded correction (same day, this PR):** exact-head CI on the second-correction head (`be9c951...`) failed — but not because §G step 2 recovery malfunctioned: **1 failed, 2867 passed**, `assert 41 == 0`. GitHub Actions' own `fetch-depth: 0` checkout gave `perform_legacy_recovery()` genuine reachable history for the first time; it correctly found the real `PHQ-2026-02` reconciliation commit and recovered **41 real legacy tickers** — landing almost exactly on `CONTENDER-0002` §G's own "approximately 41" estimate. The design worked; one test's own hardcoded `registry_entries_created == 0` assumption was wrong. **Governing-contract question resolved from the text, not invented**: `CONTENDER-0002` §G's own conditional structure (attempt recovery only if reachable) itself requires environment-dependent output — the committed `registry.yaml` is correctly "deterministic for the same repository state and git-history availability," not required to be byte-identical across a shallow and a complete clone; this session's own clone remains genuinely shallow, so the committed file is unchanged in substance (84 entries). **Fix**: the test is renamed `test_legacy_gap_record_present_and_shape_is_valid` and rewritten to assert the actual invariant (zero entries in the unavailable/ambiguous case; `registry_entries_created` exactly matching the real recovered count, with every recovered entry independently checked for its own git-commit provenance citation, in the recovered case) rather than one environment's particular outcome. No other test assumed a fixed count against the real (non-injected) environment. Documentation corrected to state the per-environment contract explicitly (generator module docstring, validator module docstring's §G bullet, this paragraph). This session cannot directly enumerate the real 41 recovered tickers (shallow clone, CI's own log truncates the failing repr) — the recovery *mechanism* itself remains independently verified via nine dependency-injected synthetic tests proving it can only ever return a real YAML-key-set difference between two real git blobs, never fabricate or placeholder a row. No ticker research, no weakening of the recovery requirement, no change to `CONTENDER-0001`/`XASSET-0001`/`CONTENDER-0002`. This session does not review its own PR, mark it ready, merge it, or post principal acceptance — awaiting terminal exact-head CI and a fresh independent exact-head delta review.
  **Third bounded correction (same day, this PR):** exact-head CI on the prior head (`6df7629...`) failed again — `assert 0 == 41`, `where 0 = len([])`. This was a genuine semantic defect, not merely a test bug: `perform_legacy_recovery()` set `registry_entries_created` to the RAW count of legacy tickers the mechanical diff found (`len(recovered)`), regardless of whether any of them actually became a new registry row. `build_registry()`'s own collision guard correctly prevented all 41 from creating duplicates (this repository's own real CI history confirms every one already carries independent Company Intelligence coverage added later by WS-0005's own batches) — but nothing recomputed the count afterward, so the gap record falsely claimed 41 rows were "created" when the true post-collision number was 0. **Fix**: `perform_legacy_recovery()` no longer constructs `legacy_gap` itself — it returns `(recovered, outcome)` where `outcome` is `"shallow"`/`"ambiguous"`/`"found"`, a raw mechanical signal; only `build_registry()`, which alone applies the collision guard, computes the final `legacy_gap` from the actual post-collision new-row count. A dedicated structured provenance marker, `SRC_LEGACY_RECOVERY` (an 18th source label, distinct from a current `holdings.yaml` citation), replaces the prior fragile prose-substring search for identifying recovered entries. A fourth `recovery_status` value, `"all_recovered_already_tracked"`, honestly distinguishes "the diff succeeded and found removed tickers, but all already have their own entry elsewhere" (zero new rows needed, a clean success) from `"recovery_ambiguous"` (the diff itself could not be resolved, a search failure) — the raw found count is disclosed in this outcome's own `next_action` text so it's never confused with "nothing found." 7 new focused tests (119 total, up from 112), including a mixed new/collision scenario and an end-to-end `build_registry()` determinism test. No change to the mandatory recovery-attempt requirement itself, no ticker research, no change to `CONTENDER-0001`/`XASSET-0001`/`CONTENDER-0002`. This session does not review its own PR, mark it ready, merge it, or post principal acceptance — awaiting terminal exact-head CI and a fresh independent exact-head delta review.

## Guardrails (enforce on me)
- Margin buffer below 30% → recommend immediate de-lever (trim or pay down margin), same severity as the cash-tight guardrail. Never propose *increasing* leverage as a response to a drawdown.
- Trades proposed outside a deposit cycle → remind me of the workflow before helping.
- Proposals to build new analysis/research/thesis systems → cite the Decisions Log first; the backtest showed added layers subtract value.
- Cash tight (recent withdrawals, sub-$500 buffer) → recommend inaction. **Pausing is the system working.**
- No predictive research, price targets, or "opportunity maps" — the tool computes opportunities at runtime on deposit day.
- Never hallucinate prices, dates, or holdings. Live data via the read-only Alpaca API (`alpaca_client.py`; there is no Alpaca MCP). `holdings.yaml` is truth for positions.
- **Verify before acting on external review.** Specific values, gate behaviors, and return values from a code/doctrine review done outside this session (a different chat, no live data or file access) must be confirmed by running the actual code or checking the actual file before committing to them — assume an outside inference is wrong until verified. Demonstrated twice in one session: a reviewer's AAPL price (~$215) was stale vs. the live $314.93, and a reviewer's assumed RSI-of-flat-series value (50) was wrong vs. the code's actual, correct 100.0 (`avg_loss == 0` branch). Applies to claims *about* this repo from outside it, not to the live Alpaca data or files already used as source of truth.
- **Earnings gate: live as of 2026-07-15.** Was blocked by the remote environment's network policy (yfinance/Yahoo domains not allowlisted) → degraded to `earnings:unavailable` flags, requiring manual web-search verification on every flagged buy. Fixed permanently: (1) allowlisted `fc.yahoo.com` + `query1/query2.finance.yahoo.com` in the environment's network policy; (2) `earnings.py` now passes yfinance a `curl_cffi` session pinned to `impersonate="chrome110"` — the default `impersonate="chrome"` (latest) fingerprint was getting connection-reset by this environment's proxy even with the domains allowed; the older fingerprint passes clean. Verified live: AAPL/NVDA/MSFT/GOOGL/TSLA all returned real dates, correctly gating GOOGL/TSLA (earnings exactly 7 days out — right at the blackout line). The manual web-search fallback is no longer needed but stays as a backstop if `earnings:unavailable` ever reappears (e.g. Yahoo API changes).
- **Earnings gate, full-roster audit (2026-07-15):** ran all 65 tickers through the gate to check coverage, found two more issues beyond the fix above. (1) **Network-policy gap, still open** — yfinance's primary source (`get_earnings_dates`) hits bare `finance.yahoo.com`, which was never added to the allowlist (only `query1/query2.finance.yahoo.com` + `fc.yahoo.com` were). Confirmed via the proxy's own relay-failure log: repeated 403 policy denials on `finance.yahoo.com:443`. Every lookup has silently been running on the weaker `calendar` fallback (single upcoming date only, no history) instead of the primary source — the gate still mostly works but on thinner data than intended. Not fixed yet; needs `finance.yahoo.com` added to the environment's network policy alongside the existing two hosts. (2) **BRK.B symbol mismatch — fixed.** Yahoo's symbol is `BRK-B` (dash), not `BRK.B` (dot, our brokerage-convention symbol) — Yahoo returned an empty calendar for the dot form. `earnings.py` now translates via a `_YAHOO_SYMBOL` map before lookup; verified BRK.B → 2026-08-01. (3) **JPM `unavailable` — not a bug, self-resolving.** JPM reported earnings 2026-07-14 (day before this audit); Yahoo's calendar hadn't posted the next date yet, so both primary and fallback correctly had nothing forward-looking to return. Expected to populate within days.

## Open Items
- ~~T1/T2 have no trim rule (return question)~~ — **backtested and closed 2026-07-15** (`reports/t1t2_trim_backtest.md`, Decisions Log), verdict NO CHANGE on TWR/aggregate-MaxDD grounds. Not reopenable without a new regime in the data.
- ~~T1 concentration, capital-impairment question~~ — **decided 2026-07-15: T1/T2 concentration ceiling added, 1.5x tier target, mechanical, no RSI gate, floored at target.** Same treatment as the cluster caps (`allocate.py`'s `plan()`, `targets.yaml`'s `gates.t1t2_trim_mult`). This is a **doctrine decision, not a backtest verdict** — explicitly the same category as the 1.8x leverage cap and 30% buffer floor: those are "no discretion, no timing, no exceptions" ceilings because the cost of a wrong tail call outweighs flexibility's upside, and a single T1/T2 name at 2x+ target under leverage is the same risk shape at smaller scale (NVDA's 2.14x/-66.4% decomposition in `t1t2_trim_backtest.md` is the disqualifying condition trim_backtest.md's leverage override already established — applying it here isn't a new judgment, it's the existing standard).
  **Important precision note, not just a footnote: what got built is NOT what got backtested.** `t1t2_trim_backtest.md`'s Arm B/C tested a *ceiling-style* trim — excess above 1.5x trimmed back down to exactly 1.5x — and measured ~0.02pp cost. The live code instead does a *floor-at-target* trim (down to 1.0x), matching the cluster-cap mechanism exactly, because that's what "same as the cluster caps" means in this codebase. Floor-at-target removes roughly 1.5x more dollars per breach than the tested ceiling-style version, every time, permanently. **The ~0.02pp/"basically free" number does not apply to the rule actually running in production** — it applies to a smaller intervention that was tested but not shipped. This was a deliberate choice (matching existing cap mechanics over inventing a second trim shape) made and disclosed at build time, not an oversight, but it means this rule's cost is **untested, not "tested and cheap."** It's justified purely on doctrine grounds (same as the 1.8x cap and 30% floor: state it, apply it, don't relitigate without a new leverage regime) — not on the backtest's return number. Do not cite this entry as "backtest-verified, 0.02pp cost."
  **Live effect, first `--review` run:** 9 T1/T2 names crossed the ceiling — GOOGL, META, GEV, LLY, V, AMZN, ISRG, WMT, COST (~$1,044 total), confirming the underlying drift was real. That run's book, however, was later found to be stale — margin debt hadn't been re-synced after the trims executed and paid down margin ($2,671.85 → $1,628.64 confirmed via Robinhood screens), so the book was understated by ~$1,041 and several *additional* trims (CRWD, PANW, BABA, SPCX, INTC/semis cap) that appeared on a follow-up run were artifacts of that stale book, not real signals — they disappeared entirely once margin was re-synced to the correct debt/buffer (61.77%, Robinhood-displayed). The original 9-name list itself was unaffected by this correction (all confirmed filled independently via order-history screenshots), but it's a reminder that this rule's *outputs* are only as good as the margin sync discipline the workflow already calls for — a mechanical trim rule interacting with a stale book will manufacture false signals mechanically, same as it would flag real ones.
- **Crypto sleeve over target** (discovered 2026-07-13, corrected-book run): was $954 = 16.5% over by ~$375. **Partially resolved 2026-07-13** — BTC position fully sold ($239 net), sleeve now ~12.4%, ~$134 residual left. ETH/SOL remain staked/illiquid, so no further trim available without unstaking; letting future deposits into other underweight sleeves passively shrink the remaining % is the default path unless it keeps drifting further over. **Rebuild plan added to Standing Queue** — future crypto-sleeve buys route to BTC until it's back to ~25% of the sleeve.
- **`min_lot_dollars` ($25) doesn't scale with book size** — fine at today's ~$5,600 book (~0.45%), but revisit once the book crosses roughly **$25,000** (where $25 drops under ~0.1% of book and stops being a meaningful floor). One-line bump in `targets.yaml` when that trigger hits; not needed now.
- ~~SKHY permanent no-data gap~~ — **resolved 2026-07-14**: turned out not to be permanent, just a post-listing settling period. Alpaca now returns daily bars (3 as of today, since the 2026-07-10 listing) — live-priced via `shares:` as of today. RSI (needs 15 bars) and the 200-SMA trend gate (needs 200) still can't fire yet, purely for lack of history — expect RSI to start working in ~2-3 weeks, trend/earnings-relative gates in several months. Not a bug to chase, just a name that hasn't traded long enough yet.
- **Live-priced book only tracks the regular session, 9:30am–4:00pm ET — frozen outside it, both sides** (confirmed 2026-07-13 post-close, confirmed 2026-07-14 pre-market): Alpaca's free daily bars reflect the official close, not extended-hours trades. During market hours the book tracks Robinhood almost exactly (confirmed within pennies same day). After the 4pm bell, Robinhood keeps moving with after-hours activity while `resolve_holdings()` holds flat at the close print — a gap that opened to ~$8.56 within one minute of close on a volatile after-hours session. Same thing pre-market: at 9:06am ET (24 min before open), a live Robinhood check showed a $23 (0.39%) gap while the book was still anchored to the prior day's close, with JPM reporting earnings live pre-market at the time. Not fixable without a paid real-time/extended-hours feed — unlike the SKHY gap, this one is structural to the free daily-bar feed, not a settling-in period. The gap should close back to penny-level accuracy shortly after the 9:30am open once today's regular-session bar starts updating.

## Formatting
Extremely concise. Bold headers, tables for comparisons, zero preamble. Honesty over comfort — name rationalizations directly.
