# Current consequential numeric-policy evidence inventory

**Status/scope:** result-free, bounded current-state inventory at base `c62a98aad8217def4c10a4b021bef198b8a0fcc9`. It covers current portfolio-construction, margin, allocation-gate, concentration, ladder, and research-decision quantities that can materially affect an investment-facing outcome. It is not exhaustive of cosmetic/engineering literals, policy authority, or independent review. Current protections remain binding.

## NUM-0001 vocabulary

The six parameter classes are reproduced without alteration: **externally imposed; mathematically derived; empirically calibrated; evidence-bounded governance selection; provisional governance guardrail; unsupported/unclassified**. “Empirically calibrated” means evidence directly and uniquely favored the specific number over real tested alternatives. Contextual classes—observed state, calculated output, research assumption, engineering constant, test fixture, and dates/identifiers/formatting—remain separate.

## Current inventory

| Current consequential quantity | Exact canonical owner; duplicates/fallbacks | NUM-0001 classification / binding scope | Evidence, alternatives, uncertainty, and next gap |
|---|---|---|---|
| Flat destination rows; assigned sum **99.25%**, unallocated **0.75%** | Each row: `targets.yaml::destination[*].target_pct`; architecture: PHQ-2026-01/02/04; sum is a calculated output | Governance-approved row choices; sum mathematically derived. Whole current destination; never renormalize. | PHQ-2026-01 retains an 8-case unlevered matrix over 2024-08-26–2026-07-28 (≈23 months), all cases passed its registered checks, but it discloses look-ahead, survivorship, and short-window limits. RISK-0005 makes whole-portfolio V1 non-decision-grade; V2 is unexecuted and input-blocked. |
| Cluster ceilings **25/20/20%** | `targets.yaml::caps.clusters[name=semis|power_infra|oil].pct` and adjacent `.tickers`; carried from prior architecture | Approved safeguards: semis 25%, power infrastructure 20%, oil 20% | Current membership is semis `[ASML, TSM, NVDA, AVGO, KLAC]`, power `[ETN, GEV, PWR]`, oil `[]`. Empty oil is unavailable/not tested, not evidence for 20%. No successor has calibrated these ceilings. |
| Effective issuer **8%**, common driver **40%** | `issuer_lookthrough.yaml::issuer_ceiling_pct` and `::common_driver_ceiling_pct`; fund inputs at `::issuers[*].funds`/`::fund_sources`; consumed by `allocate.py`; PHQ-2026-08 measurement authority | Approved no-add safeguards; look-through holdings are dated observed facts | PR398 refreshed official fund holdings and fixed the authoritative common-driver rollup. It did not establish optimal ceilings. V2 pins a predecessor hash; lawful amendment must preserve it and bind PR398 rather than overwrite an expected hash. |
| Leverage cap **1.8x** | `targets.yaml::margin.leverage_cap`; required/fail-loud consumption in `allocate.py::_resolve_margin_config()`—no numeric fallback | Provisional governance guardrail; hard ceiling | Prior 1.2–2.0 sweep did not distinguish 1.8 from adjacent 1.6–2.0 in that setup. Not empirically calibrated. Successor candidates may be stricter, never looser absent later policy authority. |
| Buffer floor **30%** | `targets.yaml::margin.buffer_floor_pct`; required/fail-loud consumption in `allocate.py::_resolve_margin_config()`—no numeric fallback | Provisional governance guardrail; live cutoff using broker-observed buffer | No alternative floor sweep exists. The research engine intentionally has no broker-buffer formula. A maintenance-excess proxy is not broker-equivalent and cannot calibrate 30%. |
| Correlated deployment/repayment rules | MARGIN-0005 protocol/preregistration; `repayment_lib.py` research functions | Research candidates, not production policy | MARGIN-0005 fully defines original candidates and mechanics. S3 was never opened on the current architecture. R1 has a pre-trade event-order integration gap recorded in the remediation artifact; unit-level arithmetic does not prove integrated accounting. |
| Synthetic `monthly_2000_first_trading_day` | MARGIN-0005 research configuration | Research assumption, not an observed account fact | Comparators must use the same schedule; TWR/net CAGR must remain separate from contributed wealth. Existing MARGIN-0005 sensitivities are a 12-month pause, halved contributions, and one 20% withdrawal; this draft invents no shorter substitute. |
| Buy ladder | LADDER-0001/02 protocol; LADDER-0003 input disposition; retained execution/disposition artifacts | Accepted research process/evidence disposition; no production adoption from result | Corrective execution exists; holdout was previously exposed and the retained disposition found **no qualifying winner**. Older rung studies alone are not the current evidence record. Closed questions require a specific new basis. |
| Rungs **1/2/3 ATR**, practicality **25%**, swing window **60** | `targets.yaml::levels.rung_atr_multipliers`, `::levels.practicality_cap_pct`, `::levels.swing_low_sessions`; matching fallbacks/uses in `levels.py` | Rungs: evidence-bounded retention; practicality/window: provisional/unsupported exact values | Current ladder evidence must be read with LADDER dispositions. Passing behavior tests do not establish economic correctness. |
| Min lot **$25**, trend RSI override **30**, earnings blackout **7 days** | `targets.yaml::gates.min_lot_dollars`, `::gates.trend_rsi_override`, `::gates.earnings_blackout_days`; matching fallbacks in `allocate.py` and the latter two in `levels.py` | Mixed operational/provisional/unsupported choices | Exact values were not uniquely selected by evidence. `trim_rsi` is **not current**: it was removed and has no authorized canonical-destination code path. |
| Margin/cash staleness **2 days** | `allocate.py:STALE_MARGIN_DAYS`; passed to margin-state reporting | Provisional engineering safeguard, currently active | Verified in current source, not transplanted as a July-state assertion. It fails closed/warns on stale observations; no evidence uniquely selects two days. |
| Book-change warning **30%** | `allocate.py:BOOK_CHANGE_WARN_PCT` | Provisional engineering safeguard | Current constant; behavior assurance is not economic calibration. |
| Research thresholds and compute budgets | Each frozen accepted protocol/report is its own canonical owner | Research assumptions, not scientific constants | Bailey et al. motivates multiple-testing/selection controls but supplies none of this repository's numeric cutoffs. Any successor must enumerate cases first, derive its budget, preserve uncertainty controls, and report threshold sensitivity; it may not relabel reused periods “fresh.” |

## Completeness and closed-question boundary

This bounded inventory excludes non-consequential formatting/network constants and does not claim every historical parameter is current. The old 103.25% tier study remains frozen history; the flat 99.25% destination is current. Retired tier and trim rules cannot be silently imported. Software tests establish specified behavior only; economic findings may reject a limit or remain inconclusive, and neither outcome changes policy without a later accepted decision.
