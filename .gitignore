# Secrets — never commit (paper-only, but still).
.env

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Generated (ephemeral scratch output only -- docs/results/ is a tracked
# deliverable, not ephemeral, and is explicitly re-included below)
results/
!docs/results/
!docs/results/**
logs/

# Generated dashboard HTML — regenerated on every run from repository state via
# `python -m portfolio_hq.dashboard build`; never a source of truth, never
# committed (portfolio_hq/ generator + assets ARE committed source).
reports/generated/
